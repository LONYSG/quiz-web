# 테스트

> guide 70절: 핵심 게임 로직은 가능한 한 테스트할 수 있어야 한다.
> 중요한 테스트를 수행했으면 결과를 [06-WORKLOG.md](06-WORKLOG.md) 에 남긴다.

---

## 0. ★ 매 라운드 마지막에 반드시 할 것

```bash
npm run verify
```

= `typecheck` + `vitest` + `smoke` + **`ui-check`**

> ★ **게이트마다 보장하는 것이 다르다. 겹치지 않는다.**
>
> | 게이트 | 보장하는 것 | ★ 보장하지 못하는 것 |
> |-------|-----------|------------------|
> | `typecheck` | 타입이 맞는다 | 실행되는지 (R006 사고) |
> | `vitest` | 순수 함수가 옳다 | 그 함수를 실제로 부르는지 |
> | `smoke` | 프로세스가 뜨고 응답하고 정상 종료한다 | 화면이 옳은지 |
> | `bot` (게이트 아님) | 서버 로직·규칙·동시성 | ★ **클라이언트 코드 전부** (R008 사고) |
> | `ui-check` | ★ 사람이 버튼을 눌렀을 때 실제로 그렇게 동작한다 | IME 조합 / 실기기 / 사람의 판단 |
>
> 세 라운드 연속으로 **"서버는 옳은데 사람이 쓰는 경로에서는 다르게 동작한다"** 는
> 같은 유형의 사고가 났다. 경계와 대책은 [07-DECISIONS.md](07-DECISIONS.md)
> D-021 / D-028 / D-029 에 있다.

★ `ui-check` 는 Chrome 을 찾지 못하면 **실패가 아니라 건너뛰기**다.
건너뛴 경우 출력에 그 사실이 남는다. **건너뛴 것을 통과로 읽지 않는다.**

화면이 바뀐 라운드에는 **7장 브라우저 수동 확인**도 함께 한다.
한글 IME 조합 중 Enter 와 모바일 화면은 자동 검증하지 않는다(D-028).

---

## 1. 실행

```bash
npm run verify        # ★ 게이트: typecheck + vitest + smoke + ui-check

npm test              # 단위 테스트만 (Vitest)
npm run test:watch
npm run typecheck     # 타입 검사만
npm run smoke         # 실행 확인만 (빌드 → 기동 → 응답 → 종료)
npm run smoke -- --no-build   # 이미 빌드된 상태로 기동만 확인
npm run ui-check      # ★ 브라우저 확인 (레이아웃 + 동작)
npm run ui-check -- --layout     # 레이아웃만
npm run ui-check -- --behavior   # 동작만
npm run ui-check -- --shot       # 스크린샷을 tmp/ 에 남긴다
```

**현재 상태: 97건 통과 / 13건 skip** (skip은 Phase 6의 마스킹 테스트)

| 파일 | 건수 | 내용 |
|------|------|------|
| `shared/src/normalize.test.ts` | 42 | 정규화 명세 34건 + 방어 8건 |
| `shared/src/hint.test.ts` | 30 | 힌트 명세 24건 + 방어 6건 |
| `shared/src/ranking.test.ts` | 22 | 동점 공동 순위 + 스킵 임계값 전 구간 |
| `shared/src/mask.test.ts` | 16 (13 skip) | 마스킹 명세 13건은 Phase 6 대기 |

---

## 2. 봇 클라이언트

**브라우저 창을 10개 띄우는 것은 현실적으로 불가능하다.**
Node에서 socket.io-client 로 가상 플레이어 N명을 접속시킨다.
★ Phase 3의 동시 정답 100회 반복 테스트가 이것 위에 세워진다.

```bash
npm run bot -- join      --count 11                  정원 초과 확인
npm run bot -- duplicate                             같은 계정 두 번 접속 (Q-06)
npm run bot -- reconnect                             끊고 다시 붙기 + 표시 유예 5초
npm run bot -- host      --grace 36                  방장 이전 (Q-29)
npm run bot -- chat      --count 10 --messages 20    동시 채팅 부하
npm run bot -- lobby                                 ★ Phase 2 설정/권한/경험률
npm run bot -- countdown                             ★ Phase 2 카운트다운/게임 레코드
npm run bot -- empty                                 ★ 활성 0명 동안 카운트다운 보류

  --url    http://localhost:3000   대상 서버
  --prefix bot                     계정 아이디 접두어 (반복 실행 시 바꾼다)
```

봇은 회원가입·로그인을 직접 하고 쿠키를 들고 있다 (Node fetch 에는 쿠키 저장소가 없다).
각 시나리오는 기대값을 **실제로 단정하고** 종료 코드로 성패를 알린다.

> ★ 반복 실행할 때는 --prefix 를 바꾼다. 같은 접두어를 쓰면 이전 실행의 계정과 방이 남아
> ALREADY_HAS_ROOM 이 난다.
>
> 테스트 계정 정리:
> `docker exec quiz-web-db psql -U quiz -d quizweb -c "DELETE FROM sessions; DELETE FROM rooms; DELETE FROM accounts;"`

---

## 3. Phase별 필수 테스트

### Phase 1 — 계정 / 방  ✅ 전부 통과 (R005)

- [x] 회원가입·로그인 정상·실패
- [x] 비밀번호 4자 미만 거부 / 72바이트 초과 거부
- [x] 닉네임 중복 거부, **대소문자 구분** (`Player` 와 `player` 는 별개)
- [x] 아이디 대소문자 무시 로그인
- [x] 10명 정원 초과 입장 거부 → 봇 11명
- [x] **같은 계정으로 두 번째 접속 시 기존 연결이 끊기고 승계된다**
- [x] 방장 이탈 → 30초 후 권한 이전 (10초 시점에는 이전되지 않음도 확인)
- [x] **이전 후 원래 방장이 돌아와도 권한을 돌려받지 않는다**
- [x] 없는 방 / 종료된 방이 각각 다르게 안내된다
- [x] **1인당 방 1개 제약** — 두 번째 방 생성 거부
- [x] **서버 재시작 후 방을 다시 만들 수 있다** (부팅 정리 절차)
- [x] **접속 종료 표시 5초 유예** — 2.7초 시점 표시 유지, 7.2초 시점 표시 전환
- [x] **활성 인원은 끊김 즉시 반영** (표시와 판정의 두 축 분리, Q-29)
- [x] 재접속 시 색상 유지 + 같은 방 자동 복귀
- [x] 채팅 부하 10명 × 20개 = 200건, 누락 0

### Phase 2 — 로비   (R007에서 전부 통과. 9장 참조)

- [x] 참가자 목록 실시간 동기화 (봇 입퇴장)
- [x] 문제 수 경계: 0 / 1 / 200 / 201
- [x] 카운트다운 경계: 2 / 3 / 60 / 61
- [x] 카운트다운 취소 → LOBBY 복귀 + 설정 잠금 해제
- [x] 카운트다운 중 신규 입장 허용
- [x] **출제 가능 수보다 큰 문제 수로 시작 시 거부 + 안내**
- [x] 비방장의 설정 변경 / 게임 시작 → NOT_HOST
- [x] 경험률 분모가 활성 문제 수(53)와 같다 (0이 아니다)
- [x] `endsAt` 이 서버 기준 절대 시각이다
- [x] games / game_players 레코드 생성 (DB 직접 조회)
- [x] 활성 0명 동안 카운트다운 보류 (D-023의 임시 처리)

### Phase 3 — 핵심 게임 ★ 가장 중요

- [ ] **동시 정답: 봇 10명이 같은 순간에 동일 정답 전송 → 정답자가 정확히 1명.
      100회 이상 반복해 단 한 번도 2명이 나오지 않아야 한다**
- [ ] **`game_questions.winner_account_id` 로 센 정답 수와
      `answer_events.accepted=true` 로 센 정답 수가 일치한다**
- [ ] 타이머 종료 직전 정답: 29.9초 시점 전송을 반복 → `endsAt` 기준으로 처리
- [ ] **`endsAt` 을 넘겨 도착한 정답은 인정되지 않는다** (tick 오차 구간)
- [ ] 정답 확정 직후 도착한 메시지가 판정되지 않는다
- [ ] **★ RESOLVED 구간 입력이 다음 문제 정답과 일치해도 정답 처리되지 않는다 (epoch)**
      *재현*: 봇이 문제 1의 RESOLVED 구간에 "문제 2의 정답" 을 전송하되,
      전송을 5초 이상 지연시켜 문제 2 시작 직후에 서버에 도달하게 한다
- [ ] 30초 타이머 정확도 측정 (오차 100ms 이내)
- [ ] 한 게임에서 같은 문제가 두 번 나오지 않는다
- [ ] 채팅 rate limit이 **정상적인 연타(초당 3개)를 막지 않는다**
- [ ] 한글 IME 조합 중 Enter가 전송되지 않는다 (브라우저 수동)
- [ ] 10명 × 초당 3개 채팅 부하 → 지연·누락 측정

### Phase 4 — 게임 시스템

- [ ] 힌트가 정확히 남은 10초에 push된다
- [ ] **힌트가 문제 시작 시점에 클라이언트로 전송되지 않는다** (네트워크 페이로드 직접 검사)
- [ ] 스킵 임계값: 활성 1~10명 전 구간 (단위 테스트는 완료. 통합에서 재확인)
- [ ] 투표 중 인원 변동 시 임계값 재평가, 도달 시 즉시 스킵
- [ ] **투표자 명단이 클라이언트로 전송되지 않는다**
- [ ] 스킵과 정답 동시 발생 → 하나만 성립
- [ ] **강제 스킵에 epoch가 담겨, 확인창 사이에 문제가 끝났으면 무시된다**
- [ ] 마지막 문제: 정답 / 시간 종료 / 스킵 세 경우 모두 5초 없이 결과 화면
- [ ] **마지막 문제도 경험 기록이 남는다**
- [ ] 강제 종료 시 진행 중이던 문제의 정답이 공개되지 않고 경험 기록도 없다
- [ ] 다시 하기 후 점수 초기화 + **경험 기록 유지**
- [ ] 동점 공동 순위 (통합)

### Phase 5 — 실시간 안정성

- [ ] 중간 참가: 남은 시간·힌트 상태가 정확히 복원된다
- [ ] **재접속 스냅샷에 남은 시간 10초 초과일 때 힌트가 들어 있지 않다**
- [ ] 접속 종료: 게임 계속, 점수 유지, **스킵 분모 즉시 반영**
- [ ] **"접속 종료" 표시가 5초 후에 나타난다** (새로고침으로 깜빡이지 않는다)
- [ ] 재접속: 점수·색상·경험 여부 유지, 중복 플레이어 미생성
- [ ] 새로고침 반복 (브라우저 수동)
- [ ] 백그라운드 탭 → 복귀 시 타이머 재동기화 (**모바일 실기기 필수**)

#### 일시정지 (PAUSED) ★

- [ ] 활성 0명이 되면 **즉시** PAUSED (유예 없음)
- [ ] **PAUSED 중 타이머가 흐르지 않는다** — 남은 시간이 그대로 유지된다
- [ ] **PAUSED 중 문제가 타임아웃되지 않는다** (경험 기록으로 소모되지 않는다)
- [ ] **자동 재개되지 않는다** — 친구가 들어와도 방장이 누를 때까지 멈춰 있다
- [ ] 재개 시 남은 시간이 정확히 이어진다 (멈춘 값 ± 오차)
- [ ] **재개 시 epoch가 증가하지 않는다** — 같은 문제를 이어서 한다
- [ ] 재개 시 전원의 클라이언트 타이머가 새 종료 시각으로 재동기화된다
- [ ] **PAUSED 중 방 삭제 타이머가 정지한다** — 10분이 지나도 방이 사라지지 않는다
- [ ] **PAUSED 중 방장 이전 타이머가 정지한다**
- [ ] 활성 1명 이상이 되고 방장이 미접속이면 30초 후 방장 이전 → 새 방장이 재개 가능
- [ ] PAUSED 중 채팅은 되지만 정답 판정은 되지 않는다
- [ ] PAUSED 30분 초과 → `abandoned`, 정답 미공개, 경험 미기록
- [ ] PAUSED 중 방장 강제 종료 → `force_ended`
- [ ] COUNTDOWN / QUESTION_ACTIVE / QUESTION_RESOLVED 세 상태 모두에서 PAUSED 진입·재개
- [ ] **실사용 시나리오**: 터널을 끄고 새로 띄운 뒤 새 링크로 전원 재접속 → 재개 → 정상 진행

### Phase 6 — 경험 시스템

- [ ] 처음 보는 문제 / 이미 경험한 문제
- [ ] 경험자의 메시지가 판정에서 제외된다
- [ ] **★ 미경험자가 정답을 치면 마스킹되지 않고 정답 처리된다** (마스킹 오판이 판정을 깨뜨리지 않는다)
- [ ] QUESTION_RESOLVED 이후에는 마스킹하지 않는다
- [ ] 발신자 본인 화면에는 원문 + "가려짐" 표시
- [ ] **마스크 길이가 정답 길이와 무관하게 고정이다**
- [ ] 정규화 매칭 — "고 무 줄" 도 마스킹된다
- [ ] **정답이 2글자 이하일 때 부분 포함으로 마스킹되지 않는다** ("달라졌네")
- [ ] 복수 정답 — 긴 것부터 치환
- [ ] **경험자 닉네임이 전원에게 보인다**
- [ ] **★ 과거 채팅의 마스킹이 문제가 바뀌어도 흔들리지 않는다** (재접속 스냅샷)
- [ ] 강제 종료 후 / 재게임 후 경험 기록 유지
- [ ] 중간 참가자의 경험 처리
- [ ] **RESOLVED 구간 입장자에게 경험 기록이 남는다**
- [ ] 전원 경험 문제만 남은 상황 → 조기 종료 (시드를 소량으로 만들어 재현)
- [ ] `mask.test.ts` 의 skip 13건을 전부 통과시킨다

### Phase 7 — UI/UX

- [ ] **실기기**: iPhone Safari, Android Chrome (터널 URL로)
- [ ] 모바일 키보드가 올라온 상태에서 문제·타이머가 가려지지 않는다
- [ ] **전송 후 키보드가 닫히지 않고 연속 입력이 된다** (iOS에서 특히 까다롭다)
- [ ] 단축키 충돌 실측 (Chrome / Edge / Safari)
- [ ] 단축키가 텍스트 입력을 방해하지 않는다
- [ ] 색상 10종 구분 가능성, 색약 시뮬레이션

---

## 4. 실제 사람이 필요한 시점

위 대부분은 봇으로 대체할 수 있다. 진짜 사람이 필요한 것은
**Phase 7의 모바일 실기기 테스트**와 최종 플레이테스트다.
친구들을 부르기 좋은 시점은 **Phase 4 완료 직후**(게임이 처음부터 끝까지 돌아가는 시점)다.

---

## 5. Phase 1 검증 결과 (2026-09-07)

| 항목 | 결과 |
|------|------|
| Vitest | 97건 통과 / 13건 skip |
| typecheck / build | 통과 |
| **비밀값 차단 6항목** | 전부 정상. **git 히스토리 유출 0건** |
| Gemini 키 (로컬 / Secrets) | 양쪽 유효. 로그에 키 노출 0건 |
| Gemini 판정 정확도 | 샘플 6문제 **6/6 기대와 일치** |
| Gemini 복수 정답 배열 | accept 6/6, **평균 4.0개.** "잉글랜드" 함정 회피 |
| Gemini 역검증 | 5/5. **비유일 정답 2건을 ambiguous=true 로 정확히 탐지** |
| **Q-53 터널 URL 유지** | **유지됨.** 100초 차단 후 같은 URL 200 |
| 방화벽 규칙 제거 | 확인됨 (QUIZWEB* 0건) |
| 봇 join 11명 | 10명 입장 / 11번째 ROOM_FULL |
| 봇 duplicate | session.terminated + 첫 연결 끊김 + 방 승계 |
| 봇 reconnect | 활성 즉시 1, 표시 유예 true→false, 색상 유지 |
| 봇 host | 10초 미이전 → 30초 이전 → 복귀해도 미반환 |
| 봇 chat | 200건 전송 / 200건 수신, 누락 0 |
| 방 오류 구분 | ROOM_NOT_FOUND / ROOM_CLOSED / ALREADY_HAS_ROOM |
| 로그인 실패 메시지 | 아이디 없음과 비번 오류가 동일 문구 |
| **부팅 정리 회귀** | 방 8개 → 재시작 → 0개 → **같은 사람이 방 재생성 성공** |

---

## 6. 실행 확인 (smoke)

`npm run smoke` 가 검증하는 것.

| # | 항목 | 왜 보는가 |
|---|------|----------|
| 1 | `npm run build` 성공 | |
| 2 | 서버 산출물 존재 | dist 를 지우고 tsbuildinfo 만 남으면 빌드가 no-op 된다 |
| 3 | 클라이언트 산출물 존재 | 없으면 터널로 접속했을 때 빈 화면이 된다 |
| 4 | 서버 기동 + `/healthz` 200 | ★ 이것이 R005에서 빠져 있던 검증이다 |
| 5 | healthz 필수 필드 | ok / phase / bootedAt |
| 6 | 부팅 정리 절차 실행 | `[boot]` 로그 확인 |
| 7 | `GET /` 가 클라이언트 HTML 서빙 | 오리진 1개 구성이 실제로 동작하는지 |
| 8 | Socket.IO 핸드셰이크 | |
| 9 | 프로세스 종료 + 포트 해제 | 남으면 다음 실행이 EADDRINUSE 로 실패한다 |

> ★ 종료 코드가 0인지는 보지 않는다. Windows 에서 `child.kill()` 은 즉시 종료라
> 자식의 SIGTERM 핸들러가 실행되지 못하고 `code=null / signal=SIGTERM` 이 된다(실측 확인).
> 그래서 "프로세스가 사라지고 포트가 해제되는가" 만 본다.
> 사람이 터미널에서 Ctrl+C 를 누르는 경로는 이 스크립트로 재현할 수 없어 확인하지 못했다.

★ `npm run bot` 도 서버가 안 떠 있으면 **사람과 같은 경로**(`server/dist/index.js`)로
직접 띄운 뒤 테스트하고 끝나면 종료한다. 테스트가 실제 실행 경로와 달라지지 않게 하기 위함이다.
`--no-boot` 을 주면 직접 띄우지 않는다.

---

## 7. ★ 브라우저 수동 확인 절차

> 봇으로 검증할 수 없는 것이 있다. 무엇을 무엇으로 검증하는지는
> [07-DECISIONS.md](07-DECISIONS.md) D-028 의 표가 정본이다.
> ★ 매 라운드 마지막에 0장(`npm run verify`)을 돌리고, 화면이 바뀐 라운드에는 이 절차도 한다.

> ### ✅ 확인 결과
>
> | Phase | 결과 |
> |-------|------|
> | Phase 1 (가입·로그인·방·초대·IME·모바일) | **통과** (2026-09-07, 건우 실측) |
> | Phase 2 (라벨·설정·경험률·카운트다운·접속 종료 배지) | **통과** (2026-09-07, 건우 실측) |
> | Phase 2 재확인 (에러 표시·내보내기·카운트다운·프록시 로그) | **통과** (2026-09-08, 건우 실측) |
> | Phase 2 알림 개선 (토스트·화면 전환 시 정리) | 대기 (R009에서 고친 것) |
>
> ★ 2026-09-08 재확인에서 건우가 UX 문제 2건을 지적했다. 둘 다 결함으로 분류해
> 그 라운드에 고쳤다(D-031 기준, D-032). 아래 (2)번 절차가 그 회귀 검사다.

---

### ★★ 절차를 쓰고 읽는 규칙 (R008에서 실제로 혼란이 있었다)

건우가 절차를 따라가다 두 번 막혔다. 그래서 아래 세 규칙을 세웠다.

1. **조건부로 나타나는 UI 는 나타나는 조건을 함께 적는다.**
   "카운트다운 초를 30으로 둔다" 만 적혀 있었는데, 그 입력란은
   **시작 방식을 "카운트다운" 으로 먼저 골라야 나타난다.** 그것이 적혀 있지 않았다.
2. **★ "무엇이 안 보이는 것이 정상인가" 도 적는다.**
   게임 시작 후 화면에 타이머가 없는 것을 건우가 결함으로 의심했다.
   문제 타이머는 Phase 3 범위라서 없는 것이 정상이다.
   ★ **아직 구현하지 않은 것을 결함으로 오해하는 일이 실제로 일어났다.**
3. **Phase 별 "이 Phase 에는 아직 없는 것" 을 절차 앞에 둔다.** (바로 아래)

---

### ★ 지금(Phase 2) 화면에 없는 것 — 전부 정상이다

| 없는 것 | 언제 생기는가 |
|--------|-------------|
| 문제 지문 | **Phase 3** |
| ★ 30초 타이머 / 남은 시간 | **Phase 3** ← 건우가 결함으로 의심했던 항목 |
| 점수 / 정답 공개 / 해설 | Phase 3 |
| 힌트 / 스킵 투표 / 강제 스킵·종료 | Phase 4 |
| 결과 화면, **게임 시작 후 로비로 돌아가는 길** | Phase 4 |
| 일시정지(PAUSED) / 재개 | Phase 5 |
| "이미 풀어본 퀴즈입니다" 배지, 정답 마스킹 | Phase 6 |
| 경험률이 0%보다 커지는 것 | Phase 6 (기록이 그때부터 쌓인다) |
| 키보드 단축키 | Phase 7 |

★ 게임을 시작한 뒤에는 **방을 나가는 것 말고 할 수 있는 일이 없다.** 정상이다.
다시 하려면 "방 나가기" → 방을 새로 만든다.

---

### 준비 — 터미널 3개

| 창 | 명령 | 무엇을 하는가 |
|----|------|--------------|
| **1번** | `npm run db:up` | DB 컨테이너. 한 번 뜨면 이 창은 비워도 된다 |
| **2번** | `npm run dev` | 서버. 소스를 고치면 자동으로 다시 빌드하고 재시작한다 |
| **3번** | `npm run dev:client` | 클라이언트 개발 서버 |

★ **2번 창에서 이 줄을 반드시 확인한다.**

```
  Phase 2 (로비 설정 / 경험률 / 카운트다운).  문제 출제는 Phase 3 이후.
```

"Phase 1" 이라고 적혀 있으면 **옛 빌드가 떠 있는 것이다.** 그 창을 Ctrl+C 하고 다시 띄운다.
★ R008 검증 중 실제로 이것에 걸려, 옛 빌드를 상대로 테스트하다 실패했다.

★ **3번 창에 `[proxy] ECONNREFUSED …` 가 뜨면 2번 창(서버)이 떠 있지 않다는 뜻이다.**
   이 로그는 무해한 소음이 아니다. 실제로 서버가 없을 때만 나온다 (D-030).

### ★ 접속할 주소 — 혼동하지 말 것

| 목적 | 주소 | 비고 |
|------|------|------|
| **개발 중 화면 확인** | **`http://localhost:5173`** | ★ 이걸 쓴다. 코드를 고치면 즉시 반영된다 |
| 빌드 결과 확인 | `http://localhost:3000` | 서버가 직접 서빙. `npm run build` 를 한 뒤의 화면이다 |
| 친구들과 플레이 | 터널 주소 (`https://….trycloudflare.com`) | 4번 창에서 `npm run dev:tunnel` |

> 5173 과 3000 은 **로그인 세션이 서로 다르다.** 쿠키가 포트별로 갈리기 때문이다.
> 한쪽에서 로그인했는데 다른 쪽에서 로그아웃 상태로 보이면 그건 정상이다.

### 두 명 이상을 흉내내려면

★ **같은 계정으로 두 곳에서 접속하면 먼저 있던 쪽이 끊긴다** (Q-06, 계정당 소켓 1개).
그래서 **서로 다른 계정 + 서로 다른 브라우저 프로파일**이 필요하다.

- 사람 A: 일반 창 (예: Chrome)
- 사람 B: **시크릿 창** 또는 다른 브라우저 (Edge, Firefox)

★ **어느 창이 방장인지 항상 의식한다.** 방장에게만 보이는 버튼이 여러 개 있다.
방장 창을 닫아 접속 종료를 만들면, 남은 창은 방장이 아니므로 방장용 버튼이 보이지 않는다.
(R008에서 "내보내기 버튼이 없다" 는 확인이 이 상황이었을 가능성이 있다)

---

### (1) 가입 → 로그인 → 방 생성  [Phase 1, 재확인 불필요]

1. `http://localhost:5173` 접속
2. **회원가입** 탭 → 아이디 `test1` / 닉네임 `철수` / 비밀번호 `1234` → 가입하고 시작
3. 방 제목에 `테스트 방` 입력 → **만들기**

**정상이면 보이는 것**

- 가입 직후 바로 로그인 상태가 된다
- 주소창이 `/r/…`(16글자 방 ID)로 바뀐다
- `1 / 10명 · 접속 1명 · LOBBY`, 참가자 목록에 `1 철수 [방장] [나]` (닉네임에 색)
- F5 로 새로고침해도 로그인이 유지되고 같은 방으로 돌아온다

---

### (2) ★★ 서버가 거부하는 사유가 화면에 뜨는가  [R008~R009에서 고친 것]

> ★ R008: 서버는 옳게 거부했는데 화면에 아무것도 뜨지 않았다.
> ★ R009: 뜨기는 하는데 **스크롤을 최상단으로 올려야 보였다.** 그래서 토스트로 바꿨다.
>   → **아래 3번을 반드시 스크롤을 내린 상태에서 해야 한다.** 예전 방식으로는 재현되지 않는다.

**절차**

1. "게임 설정" 카드에서 문제 수를 **200** 으로 한다 (또는 `200문제` 프리셋)
2. 아래에 빨간 문구가 나타난다
   `★ 지금 출제할 수 있는 문제는 53개입니다. 이대로 시작할 수 없습니다.`
3. ★★ **페이지를 맨 아래까지 스크롤한다** (채팅창이 보이는 상태).
   그 상태에서 위로 조금만 올려 "게임 시작" 버튼을 눌러도 되고,
   버튼을 누른 뒤 곧바로 맨 아래로 스크롤해도 된다.
   ★ 요점은 **화면 최상단이 보이지 않는 상태**에서 알림이 보이는지다

**정상이면 보이는 것**

- ★ **화면 아래쪽에 주황색 알림**이 떠 있다. 스크롤 위치와 무관하다. 세 줄이다.
  ```
  출제할 수 있는 문제가 부족합니다.
  출제할 수 있는 문제가 53개뿐입니다. 문제 수를 53개 이하로 줄여 주세요. (요청 200개)
  NOT_ENOUGH_QUESTIONS
  ```
- 오른쪽에 "닫기" 버튼이 있다
- ★ **약 7초 뒤 알아서 사라진다**
- ★ 알림이 **채팅 입력창·전송 버튼·로그아웃 버튼을 가리지 않는다.**
  겹쳐 보이더라도 그 아래 버튼을 누를 수 있다(알림이 클릭을 막지 않는다)
- 게임은 시작되지 않고 로비에 그대로 있다

★ **스크롤을 올려야 보이면 결함이다.** (그것이 R009 이전의 상태였다)
★ 아무것도 뜨지 않으면 결함이다. (그것이 R008 이전의 상태였다)

**이어서 확인**

4. ★ "게임 시작" 을 **연달아 세 번** 누른다 → 알림이 **하나만** 있어야 한다. 쌓이면 결함
5. "닫기" → 즉시 사라진다
6. 문제 수를 **3** 으로 바꾼다 → 빨간 문구가 사라진다
7. "게임 시작" → 즉시 시작된다 (아래 (4) 로 이어진다)

---

### (2-2) ★ 성공한 뒤에 이전 알림이 남지 않는가  [R009에서 고친 결함]

> ★ 빈 방 ID 로 실패 → 올바른 ID 로 입장 성공 → 방 안에서도
> "방 ID를 입력해 주세요." 가 계속 떠 있었다.
> 이미 해결된 문제를 화면이 계속 문제라고 말하는 상태였다.

**절차**

1. 방에 들어가 있지 않은 상태에서 "초대 링크로 입장" 의 방 ID 칸을 **비운 채** "입장"
2. 알림이 뜬다: `방 ID를 입력해 주세요.`
3. ★ **알림이 사라지기 전에**(7초 안에) 방 제목을 넣고 "만들기" 로 방을 만든다
   (또는 올바른 방 ID 를 넣고 입장한다)

**정상이면 보이는 것**

- ★ 방에 들어가는 순간 **알림이 사라진다.** 닫기를 누르지 않아도 된다
- ★ 방 안에 "방 ID를 입력해 주세요." 가 남아 있으면 결함이다

---

### (3) ★ 접속 종료자 "내보내기" 버튼  [Q-15 / R008 확인 요청 항목]

> ★ 이 버튼은 **조건 세 개가 모두 맞아야** 나타난다.
>   (가) 내가 **방장**이다
>   (나) 그 사람이 **접속 종료** 상태다 (끊긴 뒤 **5초** 지나야 그렇게 표시된다)
>   (다) 그 사람 줄의 배지 옆
> ★ 셋 중 하나라도 아니면 보이지 않는 것이 정상이다.

**절차 — ★ 방장 창을 열어 둔 채로 해야 한다**

1. 일반 창(방장)에서 방을 만든다
2. 초대 링크를 복사해 **시크릿 창**에서 열고 다른 계정(`test2`/`영희`)으로 가입해 입장한다
3. 방장 창에 `2 영희` 가 보이는지 확인한다
4. ★ **시크릿 창(게스트)을 닫는다.** 방장 창은 그대로 둔다
5. 방장 창을 보며 **5초 이상** 기다린다

**정상이면 보이는 것**

- 5초 전: `접속 1명` 으로 줄어들지만 `영희` 줄은 그대로다 (표시 유예, Q-15)
- ★ 5초 뒤: `영희` 줄에 `접속 종료` 배지가 나타나고 **그 옆에 `내보내기` 버튼**이 생긴다
- ★ 참가자 카드 아래에 파란 안내가 나타난다
  `접속이 끊긴 참가자 옆의 "내보내기" 로 자리를 비울 수 있습니다. (방장만 가능)`
- `내보내기` 를 누르면 `영희` 가 목록에서 사라지고 `1 / 10명` 이 되며
  채팅에 `영희 님을 내보냈습니다.` 가 나온다

**★ 안 보이면 먼저 이것을 확인한다**

- 지금 보고 있는 창이 **방장 창인가?** 게스트 창에는 이 버튼이 없다.
  게스트 창에는 대신 `접속이 끊긴 참가자를 내보내는 것은 방장만 할 수 있습니다.` 가 보인다
- 5초가 지났는가? 배지가 없으면 버튼도 없다
- 그 사람이 **"방 나가기" 로 나갔다면** 목록에서 아예 사라진다. 배지도 버튼도 없는 것이 정상이다
  (내보낼 자리가 이미 비었다)

---

### (4) 게임 설정과 카운트다운  [Phase 2]

> ★ **카운트다운 초 입력란은 처음에 보이지 않는다.**
>   "시작 방식" 에서 **"카운트다운" 을 먼저 눌러야** 그 아래에 나타난다.
>   ★ R008에서 이 사실이 절차에 없어 건우가 막혔다.

**절차**

1. 방장 창의 "게임 설정" 에서 문제 수를 **3** 으로 한다
2. **"시작 방식" 줄에서 `카운트다운` 을 누른다**
   → ★ 그러면 그 아래에 `카운트다운 (3~60초)` 입력란이 **새로 나타난다**
3. 그 입력란에 **30** 을 넣는다
4. `게임 시작` 을 누른다
5. 시크릿 창(게스트)을 새로고침해 본다
6. 방장 창에서 `카운트다운 취소` 를 누른다
7. 다시 `게임 시작` → 이번에는 끝까지 기다린다

**정상이면 보이는 것**

- 2번: 입력란이 없다가 생긴다. **없던 것이 나타나는 것이 정상이다**
- 4번: "게임 시작" 카드가 큰 숫자 `30.0초` 로 바뀌고 0.1초 단위로 줄어든다
- 4번: 게임 설정이 읽기 전용으로 바뀌고 `게임이 시작되어 설정을 바꿀 수 없습니다.` 가 나온다
- ★ 게스트 창에도 **같은 숫자**가 보인다 (0.1~0.2초 이내 차이). 서버 시각 기준이기 때문이다
- 5번: 새로고침해도 남은 시간이 이어진다. ★ 처음부터 다시 시작하면 결함이다
- 6번: 즉시 로비로 돌아가고 설정을 다시 바꿀 수 있다. 게스트 창도 같이 돌아간다
- 7번: `0.1초` 다음에 `시작 중…` 이 잠깐 보인 뒤 점선 테두리 패널이 나온다
  ★ `시작 중…` 은 정상이다. 클라이언트는 0이 되어도 스스로 시작하지 않고 서버 신호를 기다린다

**★ 그 뒤 화면에 없는 것 — 전부 정상이다**

- 문제 지문이 없다 → Phase 3
- ★ **남은 시간(30초 타이머)이 없다 → Phase 3**
- 점수가 없다 → Phase 3
- 로비로 돌아가는 버튼이 없다 → Phase 4

점선 패널에 그 사실이 적혀 있다.
`★ 문제와 30초 타이머는 Phase 3에서 나옵니다. 지금 화면에 문제도, 남은 시간도, 점수도 보이지 않는 것이 정상입니다.`

---

### (5) 경험률 / 초대 링크 / 채팅  [Phase 1~2, 이미 통과]

- 경험률: 닉네임 옆(좁은 화면에서는 아래)에 `53문제 중 0문제 (0.0%)`
  ★ 분모가 0 이거나 `NaN` 이면 결함이다
- 초대 링크 복사: 버튼이 잠시 `복사됨` 으로 바뀌고, 붙여넣으면 지금 접속한 주소 기준의 링크
- ★ 한글 IME: `훈민정음` 을 치고 `음` 이 조합 중일 때 Enter → **아무것도 전송되지 않는다.**
  한 번 더 Enter → `훈민정음` 이 한 번 전송된다. `훈민정` 이 나가면 결함이다
  ★ 이 항목은 자동 검증하지 않는다. 이유는 D-028 에 있다

---

### (6) 휴대폰(LTE) 접속과 좁은 화면

1. `npm run build` → 4번 창에서 `npm run dev:tunnel`
2. 출력된 `https://….trycloudflare.com` 을 휴대폰으로 연다. ★ **Wi-Fi 를 끄고 LTE 로**
3. 새 계정(`test3`/`민수`)으로 가입해 초대 링크로 입장

**정상이면 보이는 것**

- 참가자 목록이 세로로 쌓이고 닉네임·배지가 깨지지 않는다
- 문제 수 입력창이 화면 폭을 다 먹지 않고, 프리셋 버튼 4개가 두 줄로 정렬된다
- 경험률이 닉네임 아래 줄에 보인다
- 가로 스크롤이 생기지 않는다
- 입력창을 탭해 키보드가 올라와도 채팅 로그와 전송 버튼이 가려지지 않는다

---

### 확인이 끝난 뒤 정리

```bash
# 터미널을 각각 Ctrl+C (4번 터널 → 3번 클라이언트 → 2번 서버)

# ★ Phase 2부터는 games / game_players 가 쌓인다. 참조 순서대로 지운다
docker exec quiz-web-db psql -U quiz -d quizweb -c "DELETE FROM answer_events; DELETE FROM game_players; DELETE FROM question_experiences; DELETE FROM games; DELETE FROM sessions; DELETE FROM rooms; DELETE FROM accounts;"

# 확인 — accounts=0 / rooms=0 / games=0 / questions=53
docker exec quiz-web-db psql -U quiz -d quizweb -t -c "SELECT 'accounts='||count(*) FROM accounts; SELECT 'rooms='||count(*) FROM rooms; SELECT 'games='||count(*) FROM games; SELECT 'questions='||count(*) FROM questions;"

# DB 컨테이너는 켜 둬도 무방하다. 끄려면
npm run db:down
```

> ★ 방을 만든 뒤 서버를 강제로 죽였다면 DB에 열린 방과 게임이 남는다.
> 다음 기동 시 부팅 정리 절차가 자동으로 닫으므로 신경 쓰지 않아도 된다.
> 2번 창에 `이전 실행의 잔여 정리: …` 가 보이면 그것이 동작한 것이다.

---

## 8. Phase 0 검증 결과 (2026-09-07)

| 항목 | 결과 |
|------|------|
| Vitest | 97건 통과 / 13건 skip |
| 빌드 (shared / server / client) | 성공 |
| 마이그레이션 | 15개 테이블 생성 |
| 시드 | 53문제 / 정답 표기 139행 |
| **부팅 정리 절차** | 잔여 방 1→0, 게임 `server_restart`, 제약 해소 후 방 생성 성공 |
| **커넥션 풀 유휴 종료** | `dbActiveMs` 가 약 60초에서 멈춤 |
| Q-14 제약 | 두 번째 방 생성이 DB에서 거부됨 |
| 헬스체크 (로컬 / 터널) | 200 / 200 |
| **WebSocket 터널 통과** | `transport=websocket` |
| 시계 오프셋 (로컬) | RTT 1ms / offset +4.5ms |
| 시계 오프셋 (터널) | RTT 133~257ms / offset −21~+49.5ms (RTT 최소 채택이 유의미하게 작동) |
| 터널 종료 시 서버 영향 | 없음 (`bootedAt` 유지) |
| cloudflared 재시작 | **URL 변경됨** |

---

## 9. Phase 2 봇 시나리오 (R007)

```bash
npm run bot -- lobby        # 설정 검증 / 권한 / 경험률 / 출제 가능 수  (36 단정)
npm run bot -- countdown    # 카운트다운 / 중간 입장 / 게임 레코드      (33 단정)
npm run bot -- empty        # 활성 0명 동안 카운트다운 보류            (3 단정)
```

★ 세 시나리오는 **`expect()` 로 단정한다.** 로그만 찍고 통과라고 보고하지 않는다.
하나라도 틀리면 종료 코드가 1이다.

| 검증 항목 | 시나리오 | 근거 |
|----------|---------|------|
| 문제 수 0 / 201 / 소수점 거부 | lobby | Q-10 |
| 카운트다운 2 / 61초 거부 | lobby | Q-11 |
| 경계값(1 / 200 / 3초 / 60초) 통과 | lobby | Q-10·11 |
| ★ 비방장 설정 변경 → NOT_HOST | lobby | guide 44절 |
| ★ 비방장 게임 시작 → NOT_HOST | lobby | guide 44절 |
| ★ 경험률 분모가 0이 아니다 (53) | lobby | Q-12. 0이면 0으로 나누기가 발생한다 |
| ★ 문제 수 200 + 가능 53 → 시작 거부 + 개수 안내 | lobby | Q-21 |
| 카운트다운 시작 → 상태·잠금·전파 | countdown | T01 |
| ★ 카운트다운 중 설정 변경 거부 | countdown | settingsLocked |
| ★ 카운트다운 중 신규 입장 허용 | countdown | Q-11 |
| ★ `endsAt` 이 서버 기준 절대 시각 | countdown | guide 18·46절 |
| ★ 방장 취소 → LOBBY + 잠금 해제 + 레코드 없음 | countdown | T03 |
| 카운트다운 만료 → 게임 시작 (경과 시간 확인) | countdown | T04 |
| games / game_players 레코드 (DB 직접 조회) | countdown | R003 4-3 |
| ★ 진행 중 `final_score` 미기록 | countdown | R003 4-3 |
| 시작 후 설정 변경·재시작 거부 | countdown | |
| ★ 방 소멸 시 열린 게임을 닫는다 | countdown | 부팅 정리 절차와의 정합 |
| ★ 활성 0명 동안 게임이 시작되지 않는다 | empty | D-023 |
| ★ 사람이 돌아오면 시작된다 | empty | D-023 |

> ★ `empty` 시나리오는 **Phase 5에서 실패해야 한다.**
> PAUSED 를 구현하면 "활성 0명이면 즉시 PAUSED" 로 동작이 바뀌기 때문이다.
> 그 실패가 D-023 의 임시 처리를 교체했다는 신호다. 그때 이 시나리오를 고친다.

### UI 레이아웃 실측

Phase 2에서는 헤드리스 Chrome(CDP)으로 **320 / 360 / 390 / 480 / 720px** 에서
버튼·배지 라벨의 줄바꿈과 가로 넘침을 실측했다 (D-022).

★ 그 스크립트는 `ws` 를 전이 의존성으로 쓰기 때문에 정식 도구로 승격하지 않고
`tmp/` 에 두었다. 승격 여부는 Q-61 이다.
사람이 직접 볼 때는 브라우저 개발자 도구의 기기 에뮬레이션(F12 → Ctrl+Shift+M)에서
폭을 360px 로 두고 확인한다.

---

## 10. 브라우저 확인 (ui-check) — R008에서 도구로 승격

```bash
npm run ui-check                # 레이아웃 + 동작 (서버를 직접 띄운다)
npm run ui-check -- --layout    # 레이아웃만
npm run ui-check -- --behavior  # 동작만
npm run ui-check -- --shot      # 스크린샷을 tmp/ 에 남긴다
npm run ui-check -- --url http://localhost:3000   # 이미 떠 있는 서버를 쓴다
```

★ **실제 Chrome 을 실제로 조작한다.** 소켓을 직접 부르지 않는다.
버튼을 누르고, 화면에 무엇이 보이는지 읽는다. 그래서 봇이 잡을 수 없는 것을 잡는다.

★ 서버는 사람이 쓰는 것과 같은 경로(`server/dist/index.js`)로 직접 띄운다 (D-020).
★ 두 사람을 흉내낼 때는 별도 브라우저 컨텍스트를 쓴다. 쿠키가 분리되어 시크릿 창과 같다 (Q-06).
★ 자기가 만든 테스트 계정을 끝나고 지운다.
★ 그리고 **시작할 때도 한 번 쓸어낸다**(`uic%` 접두어). 프로세스가 강제 종료되면
`finally` 가 돌지 않아 계정이 남기 때문이다. ★ 사람이 만든 계정은 건드리지 않는다.

### 레이아웃 (D-022 회귀 방지)

**320 / 360 / 390 / 480 / 720px** 에서 로그인·로비 화면의 버튼·배지 라벨이
줄바꿈되지 않는지, 가로 넘침이 없는지 본다.

★ 줄 수는 `Range.getClientRects()` 의 **top 값 종류**로 센다.
사각형 개수를 세면 "50문제" 처럼 숫자+한글이 섞인 라벨을 한 줄인데도 2줄로 오탐한다.

### 동작 (R008 결함의 회귀 방지)

| 항목 | 왜 보는가 |
|------|----------|
| 게임 시작 버튼이 활성이고 클릭이 전달된다 | 클라이언트가 선제적으로 막으면 서버 검증 경로를 사람이 밟지 않는다 |
| ★ 서버 거부 사유가 화면에 표시된다 | **R008 결함.** 서버는 옳게 거부했는데 화면에 아무것도 없었다 |
| 안내에 실제 출제 가능 개수가 들어 있다 | `detail` 이 표시 경로를 타는지 |
| message / detail / code 세 줄이 모두 있다 | ★ 좁아졌다는 이유로 `detail` 을 빼면 "왜 안 되는지 모른다" 로 되돌아간다 |
| ★★ **스크롤을 내린 상태에서 알림이 화면 안에 보인다** | **R009 결함.** 배너가 문서 흐름에 있어 스크롤을 올려야 보였다. ★ 요소의 화면 좌표가 뷰포트 안에 있는지 잰다 |
| ★ 알림이 채팅 입력창 / 전송 버튼 / 푸터를 가리지 않는다 | 좌표 겹침으로 잰다. 320px 에서 실제로 겹쳤던 적이 있다 |
| ★ 같은 에러 연타 시 알림이 쌓이지 않는다 | 표시 슬롯이 하나인지 |
| 닫기로 즉시 사라진다 / 자동으로 사라진다 | 두 종료 경로 |
| ★★ **화면이 바뀌면 이전 알림이 사라진다** | **R009 결함.** 입장 성공 뒤에도 실패 알림이 남았다 |
| 정상값으로 되돌리면 실제로 시작된다 | 거부가 과도하게 걸리지 않는지 |
| 임시 패널이 Phase 3 범위를 알린다 | "타이머가 없다" 를 결함으로 오해하지 않게 |
| ★ 접속 종료자에게 "내보내기" 버튼이 나타나고 동작한다 | **조건부 UI.** Q-15 확정 기능 |
| ROOM_NOT_FOUND / BAD_REQUEST 가 화면에 표시된다 | 다른 에러 코드도 표시 경로를 타는지 |
| ★ 빈 방 ID 로 입장 시 조용히 무시하지 않는다 | 무반응은 사용자에게 고장과 같다 |

★ **"보인다" 를 DOM 존재로 판정하지 않는다** (R009에서 배운 것).
R008의 배너는 DOM 에 있었지만 스크롤을 내리면 화면 밖이었다. DOM 검사만으로는 통과했을 것이다.
→ `getBoundingClientRect()` 로 요소의 화면 좌표를 구해 뷰포트 안에 완전히 들어오는지 본다.
조작 대상과의 겹침도 같은 방식으로 사각형 교차를 계산한다.

★ **서버에 새 거부 경로를 만들면 여기에 항목을 추가한다.** 봇 테스트만 추가하면
R008과 같은 사고가 반복된다 (D-028 규칙 1).

### ★ 이 도구가 스스로 틀린 적이 세 번 있다

| 오탐 | 원인 | 고친 방법 |
|------|------|----------|
| "50문제" 가 2줄 | 사각형 개수를 셌다 | top 값 종류를 센다 |
| 없는 배지를 있다고 판정 | 안내 문구에도 "접속 종료" 가 있었다 | `.badge.off` 요소를 본다 |
| 방 ID 를 `/` 로 읽음 | 방 없는 화면에도 "초대 링크로 입장" 이 있어 방 생성 전에 통과 | 주소가 `/r/` 로 바뀌는 것을 함께 본다 |
| (R009) 에러 표시 3건이 실패로 나왔다 | 배너를 토스트로 바꿨는데 검사 선택자(`.notice-code`)를 함께 안 고쳤다 | 선택자를 `.toast-code` 로 갱신. ★ 표시 방식을 바꾸면 검사도 함께 고친다 |

★ **텍스트로 화면 상태를 판정하지 않는다.** 구조(선택자·주소)로 판정한다.
도구를 믿는 것도 검증해야 한다.

---

## 11. 개발 중 로그 읽는 법

### ★ `[proxy] ECONNREFUSED` / `ECONNRESET` — 무해한 소음이 아니다

클라이언트 창(`npm run dev:client`)에 이것이 나오면 **서버가 없다는 뜻이다.**

```
[proxy] ECONNREFUSED — 게임 서버(:3000)에 연결할 수 없습니다. 다른 터미널에서 npm run dev 가 떠 있는지 확인하세요.
[proxy] ECONNRESET — 게임 서버와의 연결이 끊겼습니다. 서버가 재시작되었거나 종료되었습니다.
```

★ R008에서 실측으로 확인한 것 (D-030)

| 상황 | 프록시 로그 |
|------|-----------|
| socket.io 연결을 강제로 끊기 | **없음** |
| ★ 실제 브라우저에서 탭 닫기 / 새로고침 | **★ 없음** |
| ★ 서버 프로세스가 죽기 | ★ `ECONNRESET` → 이어서 `ECONNREFUSED` 반복 |

즉 **탭을 닫아서 나는 로그가 아니다.** 이 로그가 보이면 2번 창을 확인한다.

원래는 Vite 가 스택과 함께 여러 줄로 찍어 소음이 컸다(서버 강제 종료 시 20줄).
지금은 한 줄로 바꾸고 5초 안의 반복을 모아 **2줄**로 줄였다.
★ 침묵시키지 않았다. 신호이기 때문이다.
