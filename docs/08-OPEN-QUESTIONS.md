# 미결정 항목

> 아직 결정되지 않은 것만 둔다. **해소되면 이 문서에서 지우고 [07-DECISIONS.md](07-DECISIONS.md) 로 옮긴다.**
> 추측을 확정 사실처럼 쓰지 않는다. (guide 71절)
>
> **★ 확정된 결정을 이 목록으로 되돌리지 않는다.**
> 확정된 규칙의 한계나 주의사항을 남기는 것은 07-DECISIONS.md 의 역할이다.
> 확정된 항목이 미결 목록에 다시 나타나면 "지금 무엇이 정해졌는지" 자체가 흐려진다.

마지막 갱신: 문제 생성 방식 전환 (R011)

---

## 결정 필요

### Q-68. ★ 난이도를 생성 입력으로 지정할 것인가 — 지금은 거의 1~2 뿐이다

**상태**: 결정 필요 (게임 재미에 관한 판단이므로 혼자 결정하지 않는다)

**R011 실측 — 통과 257건의 난이도 분포**

| 난이도 | 1 | 2 | 3 | 4 | 5 |
|--------|---|---|---|---|---|
| 건수 | **152** | 93 | 11 | **1** | 0 |

★ 건우는 "4 이상만 하면 너무 쉽기만 해서 재미없을 수 있으니 2~3도 적절히 섞고 싶다" 고 했다.
★ 그런데 지금은 **2~3을 섞는 것이 아니라 1~2 밖에 안 나온다.** 반대 방향으로 치우쳤다.

원인 추정(근거 포함): 프롬프트가 "정답이 유일하고 애매하지 않은 문제" 를 강하게 요구한다.
★ 그 조건을 가장 안전하게 만족시키는 것이 **누구나 아는 교과서적 사실**이다.
모델이 안전한 쪽으로 수렴한 것으로 보인다.

**선택지**

| 안 | 내용 |
|----|------|
| (A) 난이도를 슬롯 입력으로 지정 | 카테고리처럼 `난이도 3` 을 요청한다. ★ 카테고리 입력이 잘 동작했으므로(불일치 0건) 같은 방식이 통할 가능성이 높다 |
| (B) 프롬프트에 목표 분포만 제시 | "난이도 1~2 를 절반, 3 을 40%, 4 를 10%" 처럼. 지시가 약해 지켜지지 않을 수 있다 |
| (C) 지금 그대로 두고 사람이 선별 | 쉬운 문제가 많으면 골라 쓰면 된다. ★ 그러나 어려운 문제 자체가 없다 |

**권장안**: (A). 근거는 카테고리 입력의 실측 결과다.
★ 다만 "어느 난이도를 몇 퍼센트" 는 게임 재미에 관한 판단이므로 건우가 정한다.

**영향**: 높음. 게임의 체감 재미를 좌우한다.

---

### Q-67. ★ 인접 중분류의 경계를 어디까지 좁힐 것인가

**상태**: 결정 필요 (카테고리 목록은 건우가 확정한다)

**R011 실측에서 나온 반례 두 개** — 대분류가 달라도 같은 문제가 나왔다.

```
world-geo > 대륙·바다   "지구상에서 면적이 가장 넓은 대양은?"  → 태평양
earth     > 해양        "세계에서 가장 넓은 바다는?"          → 태평양

theatre   > 연극 고전    "셰익스피어 4대 비극 중 하나는?"       → 햄릿
world-lit > 시·희곡      "'사느냐 죽느냐'가 나오는 희곡은?"     → 햄릿
```

★ 건우의 원칙 "다른 카테고리 + 같은 정답 → 문제없음" 의 반례다.
★ 원인은 중분류 두 개가 **같은 축을 공유**하는 데 있다(D-040의 규칙 위반).
경계 문장을 넣었지만 충분히 좁지 않았다.

**선택지**

| 안 | 내용 |
|----|------|
| (A) 경계 문장을 더 좁힌다 | `earth > 해양` = 해류·수온·심해 등 **현상**만 / 바다의 크기·이름은 `world-geo`. `theatre` = 공연·무대·제작만 / 희곡 텍스트는 `world-lit` |
| (B) 중분류를 합친다 | 겹치는 둘을 하나로. ★ 그러면 중분류가 넓어져 후보군이 커진다 |
| (C) 그대로 두고 중복 판정에 맡긴다 | 실측에서 정답 일치로 12쌍 중 11쌍을 잡았다. 검수에서 걸러진다 |

**권장안**: (A) + (C). 경계를 좁히면 발생을 줄이고, 중복 판정이 남은 것을 잡는다.
★ (B)는 하지 않는 편이 낫다고 판단한다 — 중복 2쌍 때문에 트리를 좁히면
"트리를 넓혀 규모를 늘린다" 는 구조 자체가 약해진다.

**영향**: 중간. 규모가 커질수록 커진다.

---

### Q-66. ★ 역검증 불일치를 "표기 변형 후보" 로 볼 것인가

**상태**: 결정 필요 (판정 규칙 변경이므로 실측 근거가 더 필요하다)

**R011 실측 — 역검증 불일치 탈락 2건**

```
"캐나다 국기 중앙의 나뭇잎 종류는?" → 단풍잎
   역검증 답 "단풍나무"    (우리 집합에 "단풍나무 잎" 은 있으나 "단풍나무" 는 없다)

"정팔각형 모양의 규제표지는?"      → 일시정지
   역검증 답 "정지"        ★ "정지" 는 사람이 실제로 칠 표기다. 빠져 있었다
```

★ 두 번째는 **오탈락에 가깝다.** 역검증이 잡은 것은 애매함이 아니라 **빠진 표기 변형**이다.
그런데 현재 규칙은 불일치 + 확신 높음이면 탈락시킨다.

★ 첫 번째는 다르다. "단풍나무" 는 잎이 아니라 나무다. 그것을 정답에 넣으면 안 된다.
→ **두 경우를 자동으로 구분할 방법이 지금은 없다.**

**선택지**

| 안 | 내용 |
|----|------|
| (A) 현행 유지 | 불일치는 탈락. ★ 안전하지만 멀쩡한 문제를 버린다 |
| (B) 불일치도 검수 대기로 | 사람이 "빠진 표기" 인지 "다른 대상" 인지 판단한다. ★ 검수 부담이 늘어난다 |
| (C) 부분 문자열이면 검수 대기 | "정지" ⊂ "일시정지" 처럼 포함 관계일 때만 살린다. ★ "단풍나무" ⊄ "단풍잎" 이라 이 표본에서는 정확히 갈린다 |

**★ 이번 라운드에 고치지 않았다.** 근거: R010에서 역검증 판정을 고쳤다가
정상 문제 6건을 전부 오탈락시킨 경험이 있다. **표본 2건으로 판정 규칙을 바꾸지 않는다.**
탈락한 것은 `processed/` 에 남아 있으므로 규칙을 고치면 `rejudge` 로 회복할 수 있다(D-035).

**권장안**: 표본을 더 모은 뒤 (C). 지금은 (A) 유지.

**영향**: 낮음~중간. 생존율 1% 안쪽이다.

---

### Q-65. ★ 카테고리 계층 마이그레이션(0003)의 적용 시점

**상태**: 건우 확정 대기 (기술 판단은 끝났다 — D-041)

카테고리 트리를 `categories` 테이블에 넣는 마이그레이션을 **아직 만들지 않았다.**
건우가 R011 샘플을 보고 목록을 확정한 뒤에 만든다.

★ 확정된 설계는 D-041 에 있다 (저장은 소분류 / 통계는 대분류+중분류 / 화면은 대분류).
★ 그때까지 적재는 **대분류 → 기존 key 임시 매핑**으로 동작한다.
정보를 잃는 매핑이지만 `processed/` JSON 에 원본이 남아 있어 나중에 채울 수 있다.

**건우가 정할 것**: 카테고리 목록에서 뺄 것 / 추가할 것. 그것이 정해지면 0003 을 만든다.

**영향**: 중간. 통계 화면(guide 40절)의 기반이다.

---

### Q-63. ★ Gemini 유료 전환 여부 — **전제가 무너졌다. 다시 판단해야 한다**

**상태**: 결정 필요 (운영·비용 판단이므로 혼자 결정하지 않는다)
**건우 입장 (R011)**: 유료 전환하지 않는다. 무료로 어디까지 가는지 먼저 본다.

**★ R010의 근거가 부실했다는 것이 R011에서 드러났다**

R010은 "호출 4회 / 18,236토큰에서 429" 를 근거로 한도가 매우 좁다고 추정했다.
★ 그 수치가 **실제 소비보다 작았다.** 예산 카운터가 두 곳에서 새고 있었다 —
`pipeline-models.mjs`(생 fetch, 아예 세지 않음), `pipeline-compare.mjs`(saveState 누락).
둘 다 R011에서 고쳤다.

**★ R011 실측 (같은 무료 키, 하루)**

| 지표 | R010 | R011 |
|------|------|------|
| API 호출 | 4회에서 429 | ★ **29회, 429 없음** |
| 토큰 | 18,236 | ★ **164,338** |
| LLM 도달 문제 | 20건 | ★ **270건** |
| 최종 통과 | 10건 | ★ **257건** |

★ **하루 한도는 여전히 확인 불가다.** 429 가 나지 않았으므로 257건/일은 **하한**이다.
"하루 한도는 N이다" 라고 쓸 수 없다.

**★ 그리고 병목이 바뀌었다**

무료 한도가 아니라 **카테고리 트리의 너비**가 제약이다.
소분류 294개에 1,000건을 채우려면 소분류당 3.4건인데,
중분류당 3~5건에서 이미 중복이 12쌍 나왔다.
★ 유료로 바꿔도 이 제약은 그대로다. 돈으로 해결되는 문제가 아니다.

**→ 판단**: 지금 유료 전환의 근거가 없다. 무료로 진행하고,
실제로 429 에 막혀 진행이 멈출 때 다시 올린다.
★ 이 항목을 닫지 않고 남겨 두는 이유는 규모가 커지면 다시 부딪힐 수 있기 때문이다.

**영향**: 중간 (R010에서는 높음이었다)

---

### Q-60. 로컬 `.env` 키와 GitHub Secrets 키가 서로 다른 값이다

**상태**: 확인 필요

**배경**: R005에서 양쪽을 각각 검증했더니 **둘 다 유효하지만 값이 다르다.**
(길이가 각각 39자와 53자였다. ★ 값은 어디에도 기록하지 않았다)

의도한 것이라면 문제가 없다. 다만 다음 상황에서 혼동이 생긴다.

- 한쪽 키를 폐기했을 때 다른 쪽이 계속 동작해 "폐기했는데 왜 되지" 가 된다
- 한쪽에만 할당량이 소진되면 원인을 찾기 어렵다

**확인 필요**: 두 값이 같은 프로젝트의 다른 키인가, 아니면 서로 다른 Google 프로젝트인가.
같은 키로 통일할 것인가.

**영향**: 낮음. 다만 키 폐기·회전 시 혼동을 막기 위해 알고 있어야 한다.

---

## 나중에 정해도 되는 것

### Q-56. 키보드 단축키 실제 배정

**시점**: Phase 7 (스킵 투표가 나오는 Phase 4에는 임시 키가 필요하다)

제약이 강하다. 이 게임은 **채팅 입력창에 포커스가 있는 상태가 기본**이므로
일반 문자 키를 쓸 수 없다. 사실상 조합키 또는 기능키만 가능하다.

제안 (실측 후 확정)

| 기능 | 키 |
|------|-----|
| 메시지·답안 전송 | Enter |
| 스킵 투표 토글 | Ctrl+Space 또는 F2 |
| 방장 강제 스킵 | Ctrl+Shift+S 또는 F3 |
| 방장 강제 종료 | Ctrl+Shift+E 또는 F4 |
| 확인창 확인 / 취소 | Enter / Esc |

조합키와 F키를 모두 받아들이고 화면에는 조합키를 표기한다.
Chrome / Edge / Safari에서 충돌 실측이 필요하다.

---

### Q-58. 검수 관리 페이지를 만들 것인가

**시점**: Phase 4 완료 후

Track D의 검수는 당분간 **JSON 파일 직접 편집**으로 한다.
문제 DB를 본격적으로 채우는 시점(수백~수천 건)에 최소 관리 페이지가 필요할지 판단한다.
`review_queue` 스키마는 이미 확정되어 있으므로 나중에 UI만 얹으면 된다.

---

## 해소된 항목

Q-01 ~ Q-53, Q-55, Q-57 은 전부 결정되었다.
결정 내용과 이유는 [07-DECISIONS.md](07-DECISIONS.md) 와 [01-GAME-RULES.md](01-GAME-RULES.md) 에
반영되어 있다. 과정은 `ai-out/R001.txt` ~ `R007.txt` 에 남아 있다(정본 아님).

특히 R005~R010에 닫힌 것

- **Q-54** Gemini 무료 티어 한도 → **실측 완료.** 어느 차원의 한도인지는 확인 불가이나
  대응(자체 예산 상한 / 페이스 조절 / 상태 파일 / 게이트)을 구현했다 (D-034).
  ★ 남은 판단은 Q-63 으로 남아 있다 (R011에서 전제가 바뀌었다)

- **Q-62** 429 의 종류를 구분해 처리할 것인가 → ★ **건우가 (B)로 확정** (R011).
  429 → **15분** 대기 → **1회만** 재개. 또 429 면 그날 중단한다.
  구현 완료: 대기 시간은 설정값(`LIMITS.rateLimitWaitMs`), 재개 횟수 상한도 설정값,
  재개 시점과 구간별 소비량을 상태 파일에 기록한다.
  ★ **R011에서 429 가 한 번도 나지 않아 이 경로는 실행되지 않았다.**
  코드는 있고 실측은 없다. 15분이 적절한지도 판단 근거가 없다
- **Q-59** Track D 모델 확정 → **실측으로 확정.** 체인 방식 (D-033).
  ★ 세 모델의 품질 차이를 찾지 못했고, 실제로는 가용성 때문에 3.6-flash 가 처리했다

- **Q-61** UI 실측 스크립트 정식화 → **(B) 승인.** `ws` 를 devDependency 로 추가하고
  `scripts/ui-check.mjs` 로 승격했다. `npm run verify` 게이트에 포함되며
  Chrome 이 없으면 건너뛴다. 레이아웃뿐 아니라 **동작 확인까지** 맡겼다 (D-029)

- **Q-53** cloudflared 네트워크 단절 시 URL 유지 → **유지된다.** 실측 검증 완료 (D-015)
- **Q-55** Track D 착수 시점 → 게임 Phase와 라운드 단위로 번갈아 진행
- **Q-57** 응답 시간 통계 범위 → Q-52에서 이미 확정된 사안이었다.
  한계 서술은 D-016 으로 옮겼다
