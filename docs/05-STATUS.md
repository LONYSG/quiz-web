# 구현 상태

> 상태값: `완료` / `진행 중` / `미구현` / `버그 있음` / `보류` / `결정 필요`
>
> **기능 상태가 바뀌면 그 커밋에서 이 문서도 함께 고친다.** 코드만 고치고 문서를 방치하지 않는다.

마지막 갱신: **Track D 착수 (파이프라인 전 구간)** (R010)

---

## 요약

| Phase | 상태 |
|-------|------|
| Phase 0 — 기반 | **완료** |
| Phase 1 — 계정 / 방 | **완료** (★ 브라우저 수동 확인까지 통과) |
| Phase 2 — 로비 | **완료** |
| Phase 3 — 핵심 게임 루프 | 미구현 |
| Phase 4 — 게임 시스템 | 미구현 |
| Phase 5 — 실시간 안정성 | 미구현 |
| Phase 6 — 경험 시스템 | 미구현 |
| Phase 7 — UI/UX | 미구현 |
| Track D — 데이터 파이프라인 | **전 구간 구현·실측 완료.** 대량 확보는 무료 한도에 막힘 (Q-63) |

**알려진 버그: 없음**

> Phase 2에서 고친 것
> - UI 라벨 줄바꿈 2건 (전송 / 방 나가기) — D-022
> - ★ **서버 거부 사유가 로비 화면에 표시되지 않던 결함** — D-027 (R008)
>   버튼·요청·서버 응답은 모두 정상이었고 표시 경로만 없었다.
>   같은 원인으로 로비에서 발생하는 **모든** 서버 에러가 보이지 않았다.
> - 핸들러 예외 시 `INTERNAL` 을 전송하지 않던 문제 — D-027 (R008)
> - 빈 방 ID 로 입장 시 아무 반응이 없던 문제 — D-027 (R008)
> - 개발 중 프록시 로그 소음 — D-030 (R008). ★ 침묵시키지 않고 한 줄로 정리했다
> - ★ **알림이 스크롤 최상단에서만 보이던 문제** — D-032 (R009).
>   화면 고정 토스트로 바꿨다. D-027 이 절반만 이뤄진 상태였다
> - ★ **성공한 뒤에도 이전 에러 알림이 남던 결함** — D-032 (R009).
>   화면이 바뀔 때 지운다. 이미 해결된 문제를 화면이 계속 문제라고 말했다

**★ Phase 3에서 반드시 제거·교체해야 할 임시 코드**

| 표식 | 위치 | 내용 | 무엇으로 교체하는가 |
|------|------|------|-------------------|
| `TEMP-P3-01` | `server/src/game/start.ts` `beginGame()` 끝 | 게임 시작 후 문제를 내지 않고 끝난다 | 04-PROTOCOL.md 3장 **[문제 시작 공통 절차]** 1~9단계 |
| `TEMP-P3-02` | `client/src/Lobby.tsx` `.temp-panel` 블록 | "문제 표시는 Phase 3" 안내 화면 | 실제 문제 화면 |
| `TEMP-P3-03` | `server/src/game/start.ts` `beginGame()` 의 catch | 게임 레코드 생성이 실패해도 게임을 시작한다 | ★ 실패하면 시작하지 않고 로비로 되돌린다. Phase 3에서는 `game_questions` / `answer_events` 가 붙을 곳이 없어 치명적이다 |

> ★ 소스에서 `TEMP-P3` 를 grep 하면 세 자리가 모두 나온다.
> Phase 3 착수 시 grep 결과가 0건이 되는 것이 완료 조건이다.

**Phase 2에 의도적으로 없는 것** (임시 코드가 아니라 Phase 범위 밖)

- 게임 시작 후 로비로 돌아가는 경로 — 결과 화면(GAME_RESULT)이 Phase 4다.
  지금은 방을 나가야 한다
- PAUSED — Phase 5. 카운트다운 중 활성 0명일 때의 동작은 D-023 참조

---

## Phase 0 — 기반

| 항목 | 상태 | 비고 |
|------|------|------|
| Git 저장소 / .gitignore / .gitattributes | 완료 | R003 |
| docs 12개 문서 | 완료 | R004 |
| npm workspaces 모노레포 | 완료 | server / client / shared / pipeline |
| TypeScript 프로젝트 참조 | 완료 | |
| Vitest | 완료 | 165건 통과, 13건 skip(Phase 6 예정) |
| Docker PostgreSQL 17 | 완료 | localhost:5434 |
| 마이그레이션 러너 | 완료 | 순수 SQL + schema_migrations |
| 전체 DB 스키마 | 완료 | `migrations/0001_init.sql` (15개 테이블) |
| 커넥션 풀 설정 | 완료 | 최대 5 / 유휴 60초 / 지연 연결. 실측 검증됨 |
| 부팅 시 정리 절차 | 완료 | 실데이터로 검증 |
| 백업 / 복원 스크립트 | 완료 | `npm run db:backup` / `db:restore` |
| 시드 문제 | 완료 | 53문제 / 정답 표기 139행 |
| 서버 헬스체크 (DB 미접근) | 완료 | |
| Socket.IO 연결 | 완료 | pingInterval 15s / pingTimeout 10s |
| 시계 오프셋 측정 | 완료 | 로컬·터널 양쪽에서 실측 확인 |
| 클라이언트 정적 서빙 | 완료 | 오리진 1개 |
| Cloudflare quick tunnel | 완료 | WebSocket 통과 확인 |
| 터널 실행 스크립트 | 완료 | `npm run dev:tunnel` |

### shared 순수 함수

| 함수 | 상태 | 테스트 |
|------|------|--------|
| `normalizeAnswer()` | **완료** | 42건 통과 (명세 34건 + 방어 8건) |
| `buildNormalizedIndex()` | **완료** | 위에 포함. 마스킹용 인덱스 매핑 |
| `generateHint()` | **완료** | 30건 통과 (명세 24건 + 방어 6건) |
| `computeRanking()` | **완료** | 동점 공동 순위 |
| `skipThreshold()` | **완료** | 활성 1~10명 전 구간 |
| `maskAnswers()` | **미구현** | Phase 6. 테스트 13건 skip 상태로 대기 |

---

## Phase 1 — 계정 / 방

| 기능 | 상태 | 비고 |
|------|------|------|
| 회원가입 | 완료 | argon2id. 아이디 대소문자 무시 / 닉네임 대소문자 구분 |
| 로그인 / 로그아웃 | 완료 | 실패 사유를 구분하지 않는다 (D-019) |
| 세션 (httpOnly 쿠키, 30일 슬라이딩) | 완료 | 토큰 해시만 DB 저장. 15일 규칙 |
| 쿠키 Secure 환경별 분기 | 완료 | localhost(http) 와 터널(https) 양쪽 동작 (D-017) |
| 계정당 활성 연결 1개 (기존 연결 승계) | 완료 | 봇 테스트 통과 |
| 닉네임 변경 (전역 유일, 대소문자 구분) | 완료 | 게임 중 변경 차단 조건 포함 |
| 비밀번호 변경 | 완료 | |
| 비밀번호 재설정 관리 스크립트 | 완료 | `npm run reset-password` |
| 방 생성 (1인당 1개) | 완료 | DB 부분 UNIQUE 가 강제 |
| 초대 링크 생성 | 완료 | `window.location.origin` 기반 |
| 링크로 입장 (미로그인 → 로그인 후 진입) | 완료 | `/r/<roomId>` 경로 |
| 최대 10명 제한 / 만석 안내 | 완료 | 봇 11명 테스트 통과 |
| 없는 방 / 종료된 방 구분 안내 | 완료 | ROOM_NOT_FOUND / ROOM_CLOSED |
| 방장 지정 / 이전(30초 유예) | 완료 | 봇 테스트 통과. 활성 0명이면 타이머 정지 |
| 방장 복귀 시 권한 미반환 | 완료 | 봇 테스트 통과 |
| 접속 종료 표시 5초 유예 | 완료 | 활성 인원 판정은 즉시 (두 축 분리) |
| 방 자동 삭제 (활성 0명 10분) | 완료 | tick 이 처리. PAUSED 중 정지 조건 포함 |
| **접속 종료자 강제 퇴장 (내보내기)** | 완료 | Q-15. ★ **문서에 빠져 있던 항목이다** (R008에서 정정). 방장에게만, 접속 종료 표시 후에만 보인다. ui-check 가 렌더·동작을 자동 확인한다 |
| 로비 채팅 | 완료 | 정답 판정은 Phase 3 |
| 한글 IME 조합 중 Enter 무시 | 완료 | ★ 브라우저 실측 통과 (2026-09-07, 건우) |

### ★ Phase 1 브라우저 수동 확인 (2026-09-07, 건우 실측)

| 항목 | 결과 |
|------|------|
| 가입 / 로그인 / 방 생성 / 새로고침 복귀 | **통과** |
| 초대 링크 복사 + 시크릿 창 입장 | **통과** |
| ★ 한글 IME 조합 중 Enter (PC / 휴대폰) | **통과.** 글자 잘림 없음 |
| 휴대폰 LTE 접속 / 좁은 화면 / 접속 종료 5초 유예 | **통과** |

> ★ 봇으로 검증할 수 없는 항목이라 사람이 직접 확인했다.
> 절차는 [10-TESTING.md](10-TESTING.md) 7장에 있다.

### Phase 1에서 함께 세운 뼈대 (Phase 3가 얹힌다)

| 항목 | 상태 | 비고 |
|------|------|------|
| 방 레지스트리 (메모리) | 완료 | `server/src/rooms/registry.ts`. 전부 동기 함수 |
| 전역 100ms tick | 완료 | `server/src/tick.ts`. 방마다 타이머를 두지 않는다 |
| 인바운드 공통 검사 파이프라인 | 완료 | `socket/guard.ts`. seq → 세션 → 방 → 권한 → 상태 → 스키마 |
| emitRoom / emitRoomPerPlayer | 완료 | `rooms/emit.ts`. Phase 6 마스킹이 의존한다 |
| room.state 스냅샷 생성 | 완료 | `rooms/snapshot.ts`. 게임 필드는 null |
| 시퀀스 부여 | 완료 | `seq.ts`. 모든 인바운드 첫 줄 |
| 봇 클라이언트 | 완료 | `npm run bot`. 8개 시나리오 (Phase 2에서 3개 추가) |
| **브라우저 확인 도구** | 완료 | `npm run ui-check`. ★ 실제 Chrome 을 조작한다. verify 게이트에 포함 (D-029) |
| **공통 알림 표시 경로** | 완료 | `client/src/Toast.tsx`. ★ 화면 종류와 무관하게 한 곳 (D-027 / D-032). 화면 고정 · 7초 자동 만료 · 화면 전환 시 정리 |
| 게임 설정 검증 (shared 순수 함수) | 완료 | `validateRoomSettings`. 테스트 35건 |
| 문제 풀 집계 (경험률 / 출제 가능 수) | 완료 | `db/questions.ts`. 출제 대상 조건을 한 곳에만 둔다 |
| 게임 레코드 계층 | 완료 | `db/games.ts`. Phase 3의 game_questions 가 여기에 붙는다 |

## Phase 2 — 로비

| 기능 | 상태 | 비고 |
|------|------|------|
| 참가자 목록 실시간 동기화 | **완료** (Phase 1) | |
| 문제 경험률 표시 | 완료 | "53문제 중 0문제 (0.0%)" 형식. 분모 0 방어 포함 |
| 경험률 갱신 시점 | 완료 | 로비 진입 / 참가자 변동 시에만. ★ 주기 갱신 없음 |
| 게임 설정 (문제 수 1~200, 시작 방식) | 완료 | 50 / 100 / 200 + "가능한 최대" 프리셋 |
| 카운트다운 초 설정 (3~60) | 완료 | |
| 방장만 변경 / 나머지 읽기 전용 | 완료 | 봇 테스트로 NOT_HOST 확인 |
| 설정 검증 (서버 재검증) | 완료 | `shared/validateRoomSettings` 공유 (D-026) |
| 출제 가능 수 안내 + 서버 재검증 | 완료 | Q-21. 시작 직전 반드시 재조회 |
| 설정 잠금 (settingsLocked) | 완료 | COUNTDOWN 이후 변경 불가 |
| 로비 채팅 | **완료** (Phase 1) | |
| 카운트다운 (3~60초) / 취소 | 완료 | 전역 tick 이 처리. 방마다 타이머 없음 |
| 카운트다운 중 신규 입장 | 완료 | Q-11. 입장자가 game_players 에 들어간다 |
| 클라이언트 카운트다운 표시 | 완료 | 서버 `endsAt` + 시계 오프셋. ★ 0에서 상태 전환 안 함 |
| 게임 시작 (즉시 / 카운트다운) | 완료 | T01~T04 |
| games / game_players 레코드 생성 | 완료 | ★ 진행 중 final_score 미기록 |
| 방 소멸 시 열린 게임 닫기 | 완료 | `rooms/lifecycle.ts`. 두 경로가 같은 함수를 쓴다 |
| 활성 0명 시 PAUSED | **보류** (Phase 5) | 만료 보류로 대신한다. D-023 |
| 문제 표시 / 정답 판정 | **Phase 3** | TEMP-P3-01 참조 |

## Phase 3 — 핵심 게임 루프

| 기능 | 상태 |
|------|------|
| 게임 상태 머신 | 미구현 |
| 게임 시작 / 카운트다운 | **완료** (Phase 2) |
| games / game_players 생성 | **완료** (Phase 2) |
| 문제 표시 / "문제 n / N" | 미구현 |
| 30초 타이머 (전역 100ms tick) | 미구현 |
| 채팅 = 답안 | 미구현 |
| 한글 IME 조합 중 Enter 무시 | **완료** (Phase 1) |
| 서버 정답 판정 | 미구현 |
| **선착순 (resolved 플래그)** | 미구현 |
| **epoch (문제 세대 번호)** | 미구현 |
| 점수 / 정답 공개 / 5초 대기 / 다음 문제 | 미구현 |
| 채팅 rate limit (3초 10개) | 미구현 |
| 판정 자격 게이트 훅 (Phase 6 대비) | 미구현 |
| 문제 선정 인터페이스 (Phase 6 대비) | 미구현 |
| 개인별 페이로드 분리 구조 (Phase 6 대비) | 미구현 |
| 경험 기록 DB 쓰기 | 미구현 |
| answer_events 기록 | 미구현 |

## Phase 4 — 게임 시스템

| 기능 | 상태 |
|------|------|
| 힌트 push (남은 10초) | 미구현 |
| 스킵 투표 / 취소 / 실시간 표시 | 미구현 |
| 스킵 임계값 재평가 (인원 변동 시) | 미구현 |
| 방장 강제 스킵 + 확인창 | 미구현 |
| 방장 강제 종료 + 확인창 | 미구현 |
| 결과 화면 / 동점 공동 순위 | 미구현 |
| 마지막 문제 정답 표시 | 미구현 |
| 다시 하기 / 로비로 | 미구현 |
| 조기 종료 (문제 소진) | 미구현 |

## Phase 5 — 실시간 안정성

| 기능 | 상태 |
|------|------|
| 중간 참가 | 미구현 |
| 접속 종료 처리 (표시 5초 유예) | **완료** (Phase 1). 게임 중 슬롯 유지는 Phase 3 |
| 재접속 / 상태 복구 스냅샷 | 부분 (Phase 1: 구조·로비 필드 완료 / 게임 필드는 Phase 3) |
| **일시정지 (PAUSED)** | 미구현 |
| 재개 (방장) / 타이머 재동기화 | 미구현 |
| PAUSED 30분 포기 | 미구현 |
| 동시 정답 race condition 검증 | 미구현 |
| 타이머 동기화 검증 | 미구현 |

## Phase 6 — 경험 시스템

| 기능 | 상태 |
|------|------|
| 경험 기록 (정답 공개 순간 기준) | 미구현 |
| 경험자 정답 판정 제외 | 미구현 |
| 본인 "이미 풀어본 퀴즈입니다" 배지 | 미구현 |
| 경험자 닉네임 전원 공개 | 미구현 |
| **정답 마스킹** (`maskAnswers`) | 미구현 |
| 경험률 실값 | 부분 (Phase 2: 조회·표시 완료 / 기록이 쌓이는 것은 Phase 6) |
| 경험 기반 문제 선정 (2단계) | 미구현 |

## Phase 7 — UI/UX

| 기능 | 상태 |
|------|------|
| PC 최적화 / 입력창 접근성 | 미구현 |
| 모바일 최적화 (세로 우선) | 부분 (Phase 2: 라벨 규칙 + 320~720px 실측 / 전면 검토는 Phase 7) |
| 예외 상황 UI | 부분 (R009: 서버 에러 토스트 완료 / 나머지는 Phase 7) |
| 키보드 단축키 + 화면 표시 | 미구현 |
| 플레이어 색상 10종 | 부분 (CSS 변수 + 배정·표시 완료 / 색약 검증은 Phase 7) |
| 채팅 UX (스크롤, DOM 관리) | 미구현 |
| 예외 상황 UI | 미구현 |

---

## Track D — 데이터 파이프라인

**상태: 파이프라인 전 구간 구현·실측 완료. 대량 적재는 무료 한도에 막혀 진행 중.**

| 항목 | 상태 | 비고 |
|------|------|------|
| 모델 확정 (Q-59) | **완료** | 체인 방식. D-033. 모델명은 `pipeline/src/config.ts` 한 곳 |
| 429 처리 / 예산 가드 (Q-54) | **완료** | D-034. 게이트·상태 파일·페이스 조절·상한 |
| OpenTDB 어댑터 | **완료** | `pipeline/src/adapters/opentdb.ts`. ★ 소스 무관 인터페이스 |
| 규칙 필터 | **완료** | 차단율 10.8% 실측. ★ 오차단 0건까지 두 번 좁혔다 |
| Gemini 1차 가공 | **완료** | `responseSchema` 로 rejectReason 강제 |
| 역검증 | **완료** | ★ 판정 결함을 실측으로 발견해 고쳤다 (D-034) |
| 규칙 검사 3종 | **완료** | ★ shared 함수 재사용 (normalizeAnswer / generateHint) |
| 재판정 (rejudge) | **완료** | ★ API 호출 없이 규칙 변경 효과를 확인한다 (D-035) |
| 검수 도우미 | **완료** | `npm run pipeline:review`. JSON 직접 편집 + 상태 전환 |
| 적재 스크립트 | **완료** | `npm run pipeline:load`. 트랜잭션 / 중복 방어 3겹 / 재실행 안전 |
| Actions 워크플로 | **완료** (미실행) | harvest 수동 / process 매일. ★ 실제 실행은 미검증 |
| 라이선스 표기 | **완료** | D-036. 화면 2곳 + DATA_LICENSE.md + 문제별 컬럼 |
| 파이프라인 단위 테스트 | **완료** | 33건. 실측에서 나온 실제 케이스 기반 |
| **100건 배치 실측** | **부분완료** | ★ 무료 한도로 20건만 LLM 도달. 아래 실측치 참조 |
| 대량 확보 (1,000건 목표) | 미착수 | ★ Q-63(유료 전환) 결정에 달려 있다 |
| 위키데이터 어댑터 | 미착수 | Q-40 순서상 다음 소스 |

### ★ R010 실측 수치

| 지표 | 값 |
|------|-----|
| 수확 | 130건 (OpenTDB, 요청 3회, 19초) |
| 규칙 필터 차단율 | **10.8%** (오차단 0건 — 전수 검토) |
| LLM 도달 | 20건 (★ 429 로 중단. 목표 100건) |
| 1차 가공 탈락 | 10건 (50% of LLM) |
| 역검증 탈락 | 0건 (판정 수정 후) |
| 규칙 검사 탈락 | 0건 |
| **최종 통과** | **10건** |
| ★ 생존율 | **29.4%** (기록 34건 기준) — R002 추산 25%와 부합 |
| 복수 정답 평균 | **3.1개** (2~5개) |
| LLM 1건당 토큰 | 약 912 (사고 토큰 비중 39.6%) |
| 실제 가공 모델 | 20건 전부 `gemini-3.6-flash` (상위 모델 503) |
| DB 적재 | 10건 → 출제 가능 **63건** (시드 53 + 10) |

★ 이 수치의 표본은 34건이다. 신뢰구간이 넓다. 참고값으로만 쓴다.

### 파이프라인 명령

```bash
npm run pipeline:models        모델 실측 확인 (호출 가능 여부 + responseSchema)
npm run pipeline:compare       모델 품질 비교 (프로덕션 프롬프트)
npm run pipeline:harvest       원본 수확
npm run pipeline:process       가공 (--dry-run 으로 규칙 필터만 볼 수 있다)
npm run pipeline:rejudge       ★ 저장된 배치를 현재 규칙으로 재판정 (API 호출 없음)
npm run pipeline:report        배치 실측 집계
npm run pipeline:review        검수 시트 / 승인 / 반려 / 수집
npm run pipeline:load          DB 적재
npm run pipeline:reset-limit   ★ 429 중단 해제 (사람이 판단해서만)
```

## 보류

- 통계 화면 — 원천 데이터만 기록하고 UI는 만들지 않는다 (guide 40절)
- 관리 페이지 — Phase 4 완료 후 판단
- 커스텀 도메인 — 필요 없음

## 결정 필요

[08-OPEN-QUESTIONS.md](08-OPEN-QUESTIONS.md) 참조.
