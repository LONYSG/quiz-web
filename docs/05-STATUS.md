# 구현 상태

> 상태값: `완료` / `진행 중` / `미구현` / `버그 있음` / `보류` / `결정 필요`
>
> **기능 상태가 바뀌면 그 커밋에서 이 문서도 함께 고친다.** 코드만 고치고 문서를 방치하지 않는다.

마지막 갱신: **Phase 1 완료** (R005)

---

## 요약

| Phase | 상태 |
|-------|------|
| Phase 0 — 기반 | **완료** |
| Phase 1 — 계정 / 방 | **완료** |
| Phase 2 — 로비 | 미구현 |
| Phase 3 — 핵심 게임 루프 | 미구현 |
| Phase 4 — 게임 시스템 | 미구현 |
| Phase 5 — 실시간 안정성 | 미구현 |
| Phase 6 — 경험 시스템 | 미구현 |
| Phase 7 — UI/UX | 미구현 |
| Track D — 데이터 파이프라인 | 설계·프롬프트 검증 완료, 수집·적재 미착수 |

**알려진 버그: 없음**

> Phase 1에서 발견해 고친 것: 없음. (Phase 0에서 고친 2건은 06-WORKLOG.md 참조)

---

## Phase 0 — 기반

| 항목 | 상태 | 비고 |
|------|------|------|
| Git 저장소 / .gitignore / .gitattributes | 완료 | R003 |
| docs 12개 문서 | 완료 | R004 |
| npm workspaces 모노레포 | 완료 | server / client / shared / pipeline |
| TypeScript 프로젝트 참조 | 완료 | |
| Vitest | 완료 | 97건 통과, 13건 skip(Phase 6 예정) |
| Docker PostgreSQL 17 | 완료 | localhost:5434 |
| 마이그레이션 러너 | 완료 | 순수 SQL + schema_migrations |
| 전체 DB 스키마 | 완료 | `migrations/0001_init.sql` (15개 테이블) |
| 커넥션 풀 설정 | 완료 | 최대 5 / 유휴 60초 / 지연 연결. 실측 검증됨 |
| 부팅 시 정리 절차 | 완료 | 실데이터로 검증 |
| 백업 / 복원 스크립트 | 완료 | `npm run db:backup` / `db:restore` |
| 시드 문제 | 완료 | 53문제 / 정답 표기 139행 |
| 서버 헬스체크 (DB 미접근) | 완료 | |
| Socket.IO 연결 | 완료 | pingInterval 15s / pingTimeout 10s |
| 시계 오프셋 측정 | 완료 | 로컬·터널 양쪽에서 실측 확인 |
| 클라이언트 정적 서빙 | 완료 | 오리진 1개 |
| Cloudflare quick tunnel | 완료 | WebSocket 통과 확인 |
| 터널 실행 스크립트 | 완료 | `npm run dev:tunnel` |

### shared 순수 함수

| 함수 | 상태 | 테스트 |
|------|------|--------|
| `normalizeAnswer()` | **완료** | 42건 통과 (명세 34건 + 방어 8건) |
| `buildNormalizedIndex()` | **완료** | 위에 포함. 마스킹용 인덱스 매핑 |
| `generateHint()` | **완료** | 30건 통과 (명세 24건 + 방어 6건) |
| `computeRanking()` | **완료** | 동점 공동 순위 |
| `skipThreshold()` | **완료** | 활성 1~10명 전 구간 |
| `maskAnswers()` | **미구현** | Phase 6. 테스트 13건 skip 상태로 대기 |

---

## Phase 1 — 계정 / 방

| 기능 | 상태 | 비고 |
|------|------|------|
| 회원가입 | 완료 | argon2id. 아이디 대소문자 무시 / 닉네임 대소문자 구분 |
| 로그인 / 로그아웃 | 완료 | 실패 사유를 구분하지 않는다 (D-019) |
| 세션 (httpOnly 쿠키, 30일 슬라이딩) | 완료 | 토큰 해시만 DB 저장. 15일 규칙 |
| 쿠키 Secure 환경별 분기 | 완료 | localhost(http) 와 터널(https) 양쪽 동작 (D-017) |
| 계정당 활성 연결 1개 (기존 연결 승계) | 완료 | 봇 테스트 통과 |
| 닉네임 변경 (전역 유일, 대소문자 구분) | 완료 | 게임 중 변경 차단 조건 포함 |
| 비밀번호 변경 | 완료 | |
| 비밀번호 재설정 관리 스크립트 | 완료 | `npm run reset-password` |
| 방 생성 (1인당 1개) | 완료 | DB 부분 UNIQUE 가 강제 |
| 초대 링크 생성 | 완료 | `window.location.origin` 기반 |
| 링크로 입장 (미로그인 → 로그인 후 진입) | 완료 | `/r/<roomId>` 경로 |
| 최대 10명 제한 / 만석 안내 | 완료 | 봇 11명 테스트 통과 |
| 없는 방 / 종료된 방 구분 안내 | 완료 | ROOM_NOT_FOUND / ROOM_CLOSED |
| 방장 지정 / 이전(30초 유예) | 완료 | 봇 테스트 통과. 활성 0명이면 타이머 정지 |
| 방장 복귀 시 권한 미반환 | 완료 | 봇 테스트 통과 |
| 접속 종료 표시 5초 유예 | 완료 | 활성 인원 판정은 즉시 (두 축 분리) |
| 방 자동 삭제 (활성 0명 10분) | 완료 | tick 이 처리. PAUSED 중 정지 조건 포함 |
| 로비 채팅 | 완료 | 정답 판정은 Phase 3 |
| 한글 IME 조합 중 Enter 무시 | 완료 | 클라이언트 |

### Phase 1에서 함께 세운 뼈대 (Phase 3가 얹힌다)

| 항목 | 상태 | 비고 |
|------|------|------|
| 방 레지스트리 (메모리) | 완료 | `server/src/rooms/registry.ts`. 전부 동기 함수 |
| 전역 100ms tick | 완료 | `server/src/tick.ts`. 방마다 타이머를 두지 않는다 |
| 인바운드 공통 검사 파이프라인 | 완료 | `socket/guard.ts`. seq → 세션 → 방 → 권한 → 상태 → 스키마 |
| emitRoom / emitRoomPerPlayer | 완료 | `rooms/emit.ts`. Phase 6 마스킹이 의존한다 |
| room.state 스냅샷 생성 | 완료 | `rooms/snapshot.ts`. 게임 필드는 null |
| 시퀀스 부여 | 완료 | `seq.ts`. 모든 인바운드 첫 줄 |
| 봇 클라이언트 | 완료 | `npm run bot`. 5개 시나리오 |

## Phase 2 — 로비

| 기능 | 상태 |
|------|------|
| 참가자 목록 실시간 동기화 | **완료** (Phase 1) |
| 문제 경험률 표시 | 미구현 |
| 게임 설정 (문제 수 1~200, 시작 방식) | 미구현 |
| 설정 검증 + 출제 가능 수 서버 재검증 | 미구현 |
| 로비 채팅 | **완료** (Phase 1) |
| 카운트다운 (3~60초) / 취소 | 미구현 |
| 게임 시작 | 미구현 |

## Phase 3 — 핵심 게임 루프

| 기능 | 상태 |
|------|------|
| 게임 상태 머신 | 미구현 |
| 문제 표시 / "문제 n / N" | 미구현 |
| 30초 타이머 (전역 100ms tick) | 미구현 |
| 채팅 = 답안 | 미구현 |
| 한글 IME 조합 중 Enter 무시 | **완료** (Phase 1) |
| 서버 정답 판정 | 미구현 |
| **선착순 (resolved 플래그)** | 미구현 |
| **epoch (문제 세대 번호)** | 미구현 |
| 점수 / 정답 공개 / 5초 대기 / 다음 문제 | 미구현 |
| 채팅 rate limit (3초 10개) | 미구현 |
| 판정 자격 게이트 훅 (Phase 6 대비) | 미구현 |
| 문제 선정 인터페이스 (Phase 6 대비) | 미구현 |
| 개인별 페이로드 분리 구조 (Phase 6 대비) | 미구현 |
| 경험 기록 DB 쓰기 | 미구현 |
| answer_events 기록 | 미구현 |

## Phase 4 — 게임 시스템

| 기능 | 상태 |
|------|------|
| 힌트 push (남은 10초) | 미구현 |
| 스킵 투표 / 취소 / 실시간 표시 | 미구현 |
| 스킵 임계값 재평가 (인원 변동 시) | 미구현 |
| 방장 강제 스킵 + 확인창 | 미구현 |
| 방장 강제 종료 + 확인창 | 미구현 |
| 결과 화면 / 동점 공동 순위 | 미구현 |
| 마지막 문제 정답 표시 | 미구현 |
| 다시 하기 / 로비로 | 미구현 |
| 조기 종료 (문제 소진) | 미구현 |

## Phase 5 — 실시간 안정성

| 기능 | 상태 |
|------|------|
| 중간 참가 | 미구현 |
| 접속 종료 처리 (표시 5초 유예) | **완료** (Phase 1). 게임 중 슬롯 유지는 Phase 3 |
| 재접속 / 상태 복구 스냅샷 | 부분 (Phase 1: 구조·로비 필드 완료 / 게임 필드는 Phase 3) |
| **일시정지 (PAUSED)** | 미구현 |
| 재개 (방장) / 타이머 재동기화 | 미구현 |
| PAUSED 30분 포기 | 미구현 |
| 동시 정답 race condition 검증 | 미구현 |
| 타이머 동기화 검증 | 미구현 |

## Phase 6 — 경험 시스템

| 기능 | 상태 |
|------|------|
| 경험 기록 (정답 공개 순간 기준) | 미구현 |
| 경험자 정답 판정 제외 | 미구현 |
| 본인 "이미 풀어본 퀴즈입니다" 배지 | 미구현 |
| 경험자 닉네임 전원 공개 | 미구현 |
| **정답 마스킹** (`maskAnswers`) | 미구현 |
| 경험률 실값 | 미구현 |
| 경험 기반 문제 선정 (2단계) | 미구현 |

## Phase 7 — UI/UX

| 기능 | 상태 |
|------|------|
| PC 최적화 / 입력창 접근성 | 미구현 |
| 모바일 최적화 (세로 우선) | 미구현 |
| 키보드 단축키 + 화면 표시 | 미구현 |
| 플레이어 색상 10종 | 부분 (CSS 변수 + 배정·표시 완료 / 색약 검증은 Phase 7) |
| 채팅 UX (스크롤, DOM 관리) | 미구현 |
| 예외 상황 UI | 미구현 |

---

## Track D — 데이터 파이프라인

| 항목 | 상태 |
|------|------|
| OpenTDB 전량 수확 (4,500건) | **조사 완료** (R002. 실제 적재는 미착수) |
| 파이프라인 설계 | 완료 (파일 기반으로 재설계, R004) |
| Gemini 가공 프롬프트 | **실측 검증 완료** (R005 1-2). 판정 6/6 · 복수 정답 평균 4개 · 역검증 5/5 |
| 역검증 설계 | 완료 |
| 소스 어댑터 구조 | 설계 완료, 미구현 |
| GitHub Actions 배치 | 미구현 |
| 로컬 적재 스크립트 | 미구현 |
| 검수 방식 (JSON 직접 편집) | 설계 완료 |
| 위키데이터 어댑터 | 미착수 |

**Gemini API 키 검증 완료 (R005).** R002가 전제한 모델(2.5-flash 계열)은 신규 키로 사용 불가여서
gemini-3.6-flash 로 교체했다. 실제 수집·적재는 R006에서 착수한다.

---

## 보류

- 통계 화면 — 원천 데이터만 기록하고 UI는 만들지 않는다 (guide 40절)
- 관리 페이지 — Phase 4 완료 후 판단
- 커스텀 도메인 — 필요 없음

## 결정 필요

[08-OPEN-QUESTIONS.md](08-OPEN-QUESTIONS.md) 참조.
