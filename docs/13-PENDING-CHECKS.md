# 확인 대기 항목 — 건우가 직접 볼 것 / 정해야 할 것

> ★★ **이 문서 하나만 보면 된다.** R014·R015·R016 의 확인 항목을 여기 모았다.
> 라운드마다 흩어져 있으면 나중에 어느 문서를 봐야 하는지 알 수 없다.
>
> ★ 매 라운드 이 문서를 갱신한다. 확인이 끝난 항목은 **결과와 날짜를 적고 "끝난 것" 으로 내린다.**
> ★ 자동으로 확인할 수 있게 된 항목도 내린다 (그 라운드에서 ui-check/봇이 맡았다는 뜻이다).

마지막 갱신: **R021** (미판정 처리 + 656건 적재 + 규모 확장 3차 + 2회차 파일럿)

> ★ R017 은 게임 화면을 건드리지 않았다. 1~5장(브라우저로 볼 것)은 R016 그대로다.
> ★★ **6장이 새로 붙었다** — 브라우저가 아니라 **판단**이 필요한 항목이다.

---

## 0. ★★ 왜 사람이 봐야 하는가 (D-028)

자동 검사는 세 가지를 할 수 없다.

| 할 수 없는 것 | 예 |
|--------------|-----|
| ★ **사람의 판단** | "조잡하다" / "헷갈린다" / "글자가 너무 작다" — 통과·실패로 나눌 수 없다 |
| ★ **실기기 동작** | iOS 소프트 키보드 / 실제 터치 / 모바일 브라우저의 IME |
| ★ **외부 환경** | 터널 URL 이 실제로 바뀌는 상황 / 네트워크가 1~2분 끊기는 상황 |

★ 그 외의 것은 **이미 자동으로 검사한다** (`npm run verify` 의 ui-check 107항목 + 봇 20종).
★ 그래서 아래 목록은 위 세 가지에 해당하는 것만 남긴 것이다.

---

## 1. 준비 (한 번만 하면 된다)

```bash
npm run db:up        # 이미 떠 있으면 생략
npm run build
npm run dev          # → http://localhost:5173
```

- ★ **창을 두 개 연다.** 창 A = 방장 / 창 B = 게스트
  (다른 브라우저 프로필 또는 시크릿 창. 같은 창의 다른 탭은 같은 계정이 되어 소용없다)
- ★ 계정 두 개로 각각 로그인하고, 창 A 가 방을 만들고 창 B 가 초대 링크로 들어온다
- ★ 게임 설정은 **문제 수 3, 즉시 시작**이 편하다. (한 판이 2분 안에 끝난다)
- ★ 브라우저 창은 **최대화**한다. "스크롤 없이 한 화면" 검사의 기준이 1280×720 이다

> ★ 이상한 점이 보이면 **화면에 무엇이 보였는지 그대로** 알려주면 된다.
> 소켓·DOM 좌표는 내가 볼 수 있지만, "조잡하다 / 헷갈린다" 는 건우만 판단할 수 있다.

---

## 2. ★★★ 이것만은 반드시 (A급)

> ★ 규칙이 깨졌는지 / 정보가 새는지 / 조작이 막히는지를 가르는 항목이다.

| # | 무엇을 | 무엇을 볼 것인가 | 왜 자동으로 못 하나 | 출처 |
|---|--------|-----------------|-------------------|------|
| A1 | ★★ 한글 IME 로 정답을 친다 (모바일 키보드 포함) | ★★ **조합 중 Enter 가 전송되지 않는다.** "훈민정" 같은 미완성 문자가 안 나간다 | ★ 실기기 IME. PC 는 R015 에서 확인 끝 — **모바일이 남았다** | R014 |
| A2 | ★★★ 게임 중 두 창을 모두 닫고 창 A 만 다시 연다 | ★★★ **일시정지 화면**이 뜬다 (왜 멈췄는지 / 복귀 인원 / 멈춘 남은 시간 / 방이 사라지기까지 남은 시간) | ★ 두 사람이 필요하고 순서가 까다롭다 | R015 |
| A3 | ★★★ 그 상태로 30초 기다린다 | ★★★ **자동으로 재개되지 않는다.** 멈춘 남은 시간도 줄지 않는다 | ★ 위와 같다 (사람 둘 + 시간) | R015 |
| A4 | ★★ 창 A(방장)에서 `Alt+R` 또는 "재개" | ★★ 멈춘 그 시간부터 이어진다. 창 B 도 함께 움직인다 | ★ 위와 같다 | R015 |
| A5 | ★★ 게임 중 **마지막 사람이 "방 나가기"** | ★★★ **방이 즉시 사라진다.** 다시 들어가면 "없는 방" 안내 | ★ 사람 둘 + 되돌릴 수 없는 동작이다 | R015 |
| A6 | ★★★ 경험자가 채팅에 **정답을 그대로 친다** (다시 하기 후 같은 문제에서) | ★★★ **다른 사람 화면에서는 "가려짐" 칩으로 바뀐다.** 내 화면에는 원문 + "가려져서 전송됨" | ★★ 두 화면을 **동시에** 봐야 한다. 서로 다른 것이 보이는 것이 정상이다 | R016 |
| A7 | ★★ 그때 **미경험자**가 같은 정답을 친다 | ★★ **정상적으로 정답 처리된다** (점수 +1). 가려지지 않는다 | ★ 위와 같다. ★ 마스킹이 판정을 건드리면 여기서 드러난다 | R016 |
| A8 | ★★ 스크롤 없이 한 화면인가 (게임 화면 / 결과 화면) | ★★ **스크롤바가 없다.** 문제 지문·남은 시간·채팅 입력창이 한눈에 보인다 | ★★ **"조잡한가" 는 사람 판단이다.** 수치(1.00배)는 자동으로 지키지만 느낌은 못 잰다 | R016 |
| A9 | ★ 게임 중 새로고침 (모바일에서) | ★ 문제·남은 시간·점수·힌트 상태가 복구된다 | ★ PC 는 R015 에서 확인 끝 — **모바일이 남았다** | R014 |

---

## 3. ★★ 여유 있으면 (B급)

> ★ 동작은 자동 검사가 확인했다. **읽기 편한가 / 손에 익는가**를 보는 항목이다.

| # | 무엇을 | 무엇을 볼 것인가 | 왜 사람이 보나 | 출처 |
|---|--------|-----------------|---------------|------|
| B1 | ★ 단축키를 손으로 써 본다 (`Alt+S` `Alt+K` `Alt+G` `Esc`) | ★ 마우스 없이 한 판이 돌아간다 | ★ "손에 익는가" 는 판단이다 | R015 |
| B2 | ★ 한글을 치는 중에 `Alt+S` 를 누른다 | ★★ 조합 중인 글자가 깨지지 않는다 | ★ 실제 IME 조합 상태가 필요하다 | R015 |
| B3 | ★ 결과 화면의 **문제별 기록**을 읽는다 | ★ 문제 / 정답 / 정답자 / 응답 시간이 읽히는가. ★ 너무 빽빽하지 않은가 | ★ 가독성 판단 | R016 |
| B4 | ★ 경험자 목록과 배지 | ★ 본인에게만 "이미 풀어본 퀴즈입니다" / 목록은 **전원에게** 색깔 닉네임으로 | ★ 색 구별(색약 고려)은 사람이 봐야 한다 | R016 |
| B5 | ★ 조기 종료 안내 | ★ "참가자 전원이 이미 경험한 문제만 남았습니다" 가 이해되는가 | ★ 문장 판단 | R016 |
| B6 | ★ 로비 화면 | ★ 아직 **스크롤이 남아 있다** (실측 1.55배). 얼마나 거슬리는지 | ★ 우선순위 판단이 필요하다 (다음 라운드에서 잡을지) | R016 |
| B7 | ★ 모바일(320px)에서 전체 | ★ 지문·타이머가 넘치지 않는가 / 라벨이 쪼개지지 않는가 / 터치 대상이 충분한가 | ★ 실기기 | R014 |
| B8 | ★ 모바일에서 채팅을 빠르게 연타 | ★★ 도배 안내가 **입력창 바로 위**에 뜬다 (소프트 키보드에 가리지 않는다) | ★★ iOS 소프트 키보드는 에뮬레이션으로 재현되지 않는다 | R014 (D-032 한계) |
| B9 | ★ 터널을 열어 실기기로 접속 | ★ 새 URL 배포 → 재접속 → 일시정지 → 재개가 실제 환경에서 도는가 | ★★ 외부 환경. 로컬호스트로는 재현할 수 없다 | R015 |
| B10 | ★ 카운트다운 중 두 창을 닫고 방장만 돌아온다 | ★ 일시정지 상태에서 **"카운트다운 취소"** 로 로비로 빠져나올 수 있다 | ★ 사람 둘 + 순서 | R015 |

---

## 4. 끝난 것 (다시 볼 필요 없다)

| 무엇 | 결과 | 언제 |
|------|------|------|
| 한글 IME 조합 중 Enter (PC) | ★ 전송되지 않았다. 판정 경로에서도 미완성 문자가 나가지 않았다 | 2026-09-11 건우 실측 |
| 힌트가 남은 10초에 나타난다 | ★ 정확히 나타났다 | 2026-09-11 건우 실측 |
| 확인창을 Enter 만으로 확정 | ★ 눌렸다 | 2026-09-11 건우 실측 |
| 320px 레이아웃 (PC 브라우저 축소) | ★ 넘치거나 쪼개지지 않았다 | 2026-09-11 건우 실측 |
| 게임 중 새로고침 복구 (PC) | ★ 문제·남은 시간·점수·힌트가 복구됐다 | 2026-09-11 건우 실측 |
| 도배 제한 (PC) | ★ 동작했다 → 그 뒤 Q-84 로 **완화**했다 (1초 20개) | 2026-09-11 건우 실측 |
| 정답 표시 / 오답에 "오답입니다" 가 없는 것 | ★ ui-check 와 봇이 맡는다 | R014~R016 자동화 |
| 결과 화면 순위·공동 순위·다시 하기 | ★ 봇 `result` 31항목이 맡는다 | R016 자동화 |
| 마스킹 규칙 자체 (16케이스) | ★ 단위 테스트 19건 + 봇 `mask` 23항목이 맡는다 | R016 자동화 |
| 한 화면 수치 (1280×720) | ★ ui-check 게이트가 맡는다 (게임·결과 1.00배) | R016 자동화 |

---

## 5. ★ 지금 없는 것 — 확인하다 놀라지 않도록 (D-030)

- ★ **개인 통계 화면이 없다.** 결과 화면은 그 게임 한 판의 기록만 보여준다.
  카테고리별 성적·누적 정답률은 09-BACKLOG 다 (원천 데이터는 전부 쌓이고 있다).
- ★ **로비는 아직 한 화면이 아니다** (1.55배). 게임·결과 화면만 1.00배를 달성했다.
- ★ **모바일 최적화는 하지 않았다.** 건우 우선순위대로 웹을 먼저 맞췄다.
  320px 에서 깨지지는 않지만 한 화면도 아니다 (1.17배).
- ★ **초성·기호 우회는 막지 못한다.** "ㄱㅁㅈ" / "고무-줄" 은 가려지지 않는다.
  ★ 완벽한 차단은 fuzzy matching 을 요구하는데 guide 16절이 금지한다.
    친구끼리 하는 게임이므로 "무심코 흘리는 것" 을 막는 수준으로 설계했다.

---

## 6. ★★ 브라우저가 아니라 **판단**이 필요한 것 (문제 데이터)

> ★ 여기 있는 것은 화면을 보는 항목이 아니다. **건우가 방향을 정해야 넘어갈 수 있는 것**이다.
> ★ 근거와 선택지는 [08-OPEN-QUESTIONS.md](08-OPEN-QUESTIONS.md) 에 Q 번호로 정리해 두었다.
> ★★ 실제 문제는 `data/pipeline/samples/R017-full.txt` / `R018-full.txt` / `R019-full.txt` 에서 읽을 수 있다.

### ★ R017 에서 올린 것 중 끝난 것

| # | 무엇 | 결과 |
|---|------|------|
| C2 | 인명 표기 | ★★ **확정.** 한국에서 실제로 부르는 형태로 (D-084). R017 40건 중 21건을 고쳤다 |
| C3 | 소재=정답 79.5% | ★ **확정.** 문제로 보지 않는다 (D-087) |
| C4 | 김홍도 중복 | ★ **확정.** 신규를 격리했다. 인물은 장르 소분류가 맡는다 (D-083) |
| C5·C6 | 소분류당 목표 / 고갈 실측 | ★ **R018 에서 실측했다.** 아래 C10 으로 이어진다 |

### ★ R018 에서 올린 것 중 끝난 것

| # | 무엇 | 결과 |
|---|------|------|
| C1 | 387건 적재 | ★★ **건우 승인. R019 에서 적재했다.** 활성 문제 306 → **693건** |
| C14 | `migrations/0005` | ★ **적용했다.** 함께 `0006`(카테고리 재동기화)도 만들어 적용했다 |
| C10 | 소분류당 10 → 12 | ★ **확정.** R019 는 60개 소분류 × 12로 돌렸다 |
| C11 | "같은 답 다른 지식" | ★★ **확정 = Q-91 (나).** "맞히게 해 주는 지식이 같은가" 로 판단한다 |
| C12 | 기존 문항 규칙 검사 | ★ **확정 = 전수 감사.** 703건을 훑었다. 아래 C15 로 이어진다 |
| C13 | 인물 규칙을 작품까지 | ★ **확정 = "받을 자리가 있을 때만".** 사례를 쌓아 간다 |
| C9 | `daily-rule/4` 숫자 정답 | ★ **그대로 두기로 확정** |

### ★ R020 에서 올린 것 중 끝난 것 (★★ R021 에서 처리했다)

| # | 무엇 | 결과 |
|---|------|------|
| C17 | 손실률을 근거로 남은 소재 수 | ★★ **확정 = 조정하지 않는다** → [D-100](07-DECISIONS.md). ★ 다만 R021 에서 후보 비율이 두 배가 됐다 — 다음 라운드에 다시 잰다 |
| C21 | R020 656건 적재 | ★★ **건우 승인. 적재했다** — 단 판정되지 않은 6건은 빼고 **647건**. 활성 문제 1,374 → **2,018건** |
| C23 | 교체 후보 9건 | ★★ **하나씩 읽고 3건만 바꿨다.** 6건은 판정 근거가 "간결하다" 뿐이어서 기존을 남겼다. `data/pipeline/audit/r021-swap-decision.json` |
| C24 | 숫자 정답의 라운드 안 충돌 | ★★ **확정 = 검사에서 제외** → [D-097](07-DECISIONS.md). 거절했던 3건을 되살렸다 |

### ★★ 지금 정해야 하는 것

| # | 무엇을 정해야 하나 | 왜 내가 못 정하나 | Q |
|---|-------------------|------------------|---|
| C22 | ★★★ **판정되지 않은 8쌍** (R020 잔여) | ★★ **V001 세션이 판정 완료** (`ai-out/V001.txt`, `data/pipeline/dedupe/r020-pending-judged.json`). ★ DB 반영 여부는 GEN/건우가 정한다 | — |
| C25 | ★★★ **R021 701건을 적재할 것인가** | ★ C26 이 끝났으니 이제 정할 수 있다. ★★ 다만 same 185건이 그룹으로 묶이는 방식에 따라 701건 중 일부가 빠질 수 있다 | — |
| C26 | ★★★ **R021 후보 231쌍을 판정해야 한다** | ★★ **V001 세션이 판정 완료** (`ai-out/V001.txt`, `data/pipeline/dedupe/r021-judged.json`). ★ DB 반영은 GEN/건우 몫 | — |
| C27 | ★★★ **2회차 파일럿 34건을 어떻게 할 것인가** | ★ 방식이 된다는 것은 확인했다. **2회차를 언제 시작할지**는 운영 판단이다 | D-098 |
| C28 | ★★ **2회차 짝(`pairedWith`)을 중복 검사에서 면제할 것인가** | ★ 규칙 변경이다. D-090·D-091 과 맞물린다 | D-098 |
| C29 | ★ **남은 138개를 넓이 순으로 섞어 배분할 것인가** | ★ 난이도가 지시와 무관하게 오르고 있다 | Q-95 |
| C30 | ★★★ **R020 잔여 8쌍 목록 자체가 3쌍 누락돼 있었다** (V001 발견) | theatre/2#008·009·010(웨스트엔드·토니상·커튼콜) ↔ db:679·681·674 가 `r020-pending-candidates.json` 에서 빠져 있었다. V001 이 찾아 함께 판정했다(전부 same, keep=b). ★ 원인 불명 — 확인 필요 | — |
| C31 | ★ **Q-91에 "일반어 vs 구체어" 유형 규칙을 추가할지** (V001 제안) | 비누/계면활성제, 도금/아연도금, 공생/상리공생처럼 등록 정답의 정밀도만 다른 경우의 same/different 기준이 아직 명문화돼 있지 않다. 상세는 `ai-out/V001.txt` 7절 | — |
| C32 | ★ **db:764(광년) 문항 서술 오류 의심** (V001 발견) | "이름에 '해'가 들어가지만..."이라 적혀 있는데 실제 단어는 '광년'(년)이다. ★ 확인 필요 — 대조는 안 함 | — |

#### C22 — 판정되지 않은 8쌍 (R020 잔여) — ★★ V001 세션이 판정 완료

```
architecture/3#009 [7]      ↔ kr-language/1#004 · db:52 · db:439   → different (V001)
baseball/1#010     [4]      ↔ db:50                                → different (V001)
classical/2#010    [4]      ↔ db:50                                → different (V001)
theatre/2#001      [레미제라블] ↔ db:160                             → same, keep=a (V001)
world-cinema/4#001 [봉준호]   ↔ db:41                               → same, keep=a (V001)
world-cinema/4#008 [박찬욱]   ↔ db:256                              → same, keep=a (V001)
```

★ 앞의 셋(숫자 정답)은 D-097 예상대로 전부 different 로 나왔다.
★★ **V001 세션이 이 8쌍 목록 자체에서 3쌍이 더 빠져 있음을 발견했다** → C30 참조
(theatre/2#008·009·010, 웨스트엔드·토니상·커튼콜 — 전부 same, keep=b 로 판정).

★ 후보 파일: `data/pipeline/dedupe/r020-pending-candidates.json`
★ 판정 결과: `data/pipeline/dedupe/r020-pending-judged.json` (V001, 11쌍 = 8 + 발견된 3)
★ 그동안 적재에서 빼 두었던 6건(`data/pipeline/audit/r021-hold-for-judge.json`)은
이제 판정이 나왔으므로 GEN 세션이 `scripts/seeds-restore.mjs` 로 되살릴지 정할 수 있다.
★★ **V001 은 DB 에 아무것도 쓰지 않았다.** 반영은 GEN/건우 몫이다.

#### C25·C26 — R021 701건 — ★★ 판정(C26)은 V001 세션이 완료

```
소분류 60 / 소재 701 → 문제 701 성공 / 거절 0
규칙 검사   expose 0 / exposeSuspect 0 / normCollisionInside 0 / variantCollision 0
중복 후보   신규 안쪽 0 / 라운드 간 107 / 기존 DB 124 = 231쌍
판정 결과(V001)   same 185(76.4%, 1차 187건에서 6건 재검토 후 조정) / different 56 / unsure 0
                (unsure 1건은 전체 242쌍 중 나머지 1건은 C22 쪽 발견분에 포함)
```

★ 판정 파일: `data/pipeline/dedupe/r021-judged.json` / 보고서: `ai-out/V001.txt`
★★★ same 비율 76.4%는 R021.txt 가 R020 실측(약 50%)으로 추정했던 값보다 훨씬 높다.
V001 이 표본 재검토로 근거를 확인했지만, ★ GEN/건우가 한 번 더 확인해 보기를 권한다(V001.txt 2·6절).
★★ same 185건이 실제로 몇 개의 중복 그룹으로 묶이는지(연결 요소)에 따라 C25(701건 적재)의
최종 손실 건수가 정해진다 — 이는 아직 하지 않았다.

#### C27·C28 — 2회차 파일럿

★ 결과는 [D-098](07-DECISIONS.md) 에 정리했다. 요점만 —

```
답이 1회차와 같은 24건   접근성 24/24 유지. 중복 후보는 전부 1회차 짝
답이 달라진 10건         접근성 5건 하락. ★ 의도치 않은 진짜 중복 3건
```

★★ **C28 을 정하지 않으면 2회차를 적재할 수 없다.** 짝을 면제하지 않으면 중복 검사가 2회차를 통째로 중복으로 잡는다.

| 안 | 내용 |
|----|------|
| (가) 짝을 면제한다 | ★ `pairedWith` 가 가리키는 1회차 문항과의 충돌만 검사에서 뺀다. 나머지는 그대로 본다 |
| (나) 2회차를 별도 세트로 둔다 | ★★ 한 판에 1회차와 2회차를 섞지 않는다. ★ 게임 쪽 변경이 필요하다 |
| (다) 2회차를 하지 않는다 | ★ 1회차만으로 3,600건을 채운다. 대신 같은 문제가 반복된다 |

★ 내 권장: **(가)**. Q-76 이 이미 한 판의 중복을 막고 있어 게임 쪽을 건드리지 않아도 된다.

#### C29 — 남은 138개의 배분 순서

★ Q-95 참고. 지금까지는 넓은 소분류부터 써 왔고, 그래서 남은 것이 좁다.
★ 난이도 4 이상이 R020 21.2% → R021 **46.4%** 로 뛰었다.

### ★ 눈으로 한 번 봐 주면 좋은 것 (판단은 아니다)

| # | 무엇을 | 어디서 |
|---|--------|--------|
| C7 | ★★ **문제 품질을 직접 읽어 본다** | `ai-out/R018.txt` 8장 (소분류별 1건) / 전문은 `data/pipeline/samples/` |
| C8 | ★ R018 은 난이도 4 이상이 **40.4%** 다 (R017 은 29.0%). 너무 어려운가 | 같은 곳 |
| C9 | ★ `daily-rule/4`(신고·응급 번호)는 **정답이 전부 숫자**다. 그대로 둘 것인가 | `R018-full.txt` |

### ★★ 내가 자체 판단한 것 — 틀렸다고 보면 말해 주면 되돌린다

| 무엇 | 어떻게 했나 | 근거 |
|------|------------|------|
| 중복 대조를 3방향으로 | 라운드 간 대조 추가 (`--against`) | D-086. ★ 그 덕에 중복 1건을 더 찾았다 |
| 스크립트 이름을 라운드 중립으로 | `r017-*.mjs` → `seeds-*.mjs` | 라운드마다 쓰는 도구인데 이름에 라운드가 박혀 있었다 |
| 소재/문제 2단계 분리 | `seeds-ingest` + `seeds-attach` | D-085. 옛 형식도 그대로 받는다 |
| 적재 코드에 `--round` 추가 | 코드만 고치고 **실행하지 않았다** | D-088 |
| `migrations/0005` 파일 작성 | ★ **적용하지 않았다.** 파일만 만들었다 | D-088 |
| 잠복 결함 2건 수정 | 메타 파일(`_*.json`)을 문제 파일로 읽던 것 / 격리 항목을 집계에 넣던 것 | ★ R017 에서는 우연히 드러나지 않았다 |

### ★★ R019 에서 자체 판단한 것

| 무엇 | 어떻게 했나 | 근거 |
|------|------------|------|
| 카테고리 트리와 DB 가 어긋난 것 | ★★ 적재 **전에** 발견해 `0006` 으로 맞췄다 | ★ DB 잎 296 vs 트리 297, 이름 불일치 4건. 적재는 키로만 맞추므로 **조용히 엉뚱한 곳에 들어갈 뻔했다** |
| 라운드 안 같은 답 6건 거절 | 먼저 만든 쪽을 살리고 뒤쪽을 `ok:false` | D-090 |
| 중복 15건 격리 | 기존을 살리고 신규를 내렸다 | ★ DB 를 건드리지 말라는 지시 때문이다. ★★ 9건은 판정이 반대로 보았다 → C20 |
| 정답 노출 4건 직접 수정 | 질문을 고쳐 썼다. 그중 2건은 정답도 바꿨다 | ★ 적재 전이고 내가 만든 것이다. 알고도 두는 쪽이 나쁘다 |
| 가운뎃점 정답에 마침표 표기 추가 | `4·19 혁명` 에 `4.19 혁명` 을 더했다 | D-092. ★ 자판에 없는 글자를 요구하고 있었다 |
| 판정 호출을 묶음으로 | 35쌍 한 번에 → 12쌍씩 3묶음 | D-089. ★ 세 모델 모두 응답이 잘렸다. 모델이 아니라 한도 문제였다 |
| R019 결과는 **적재하지 않았다** | 파일로만 쌓았다 | ★ 지시 그대로다 → C16 |

### ★★ R020 에서 자체 판단한 것

| 무엇 | 어떻게 했나 | 근거 |
|------|------------|------|
| 표기 변형 규칙을 코드로 | `pipeline/lib/answer-rules.mjs` 를 만들고 감사·적재가 함께 쓰게 했다 | D-094. ★ 규칙이 프롬프트에만 있어서 적재가 확인하지 않았다 |
| 적재에 변형 게이트 | 노출이면 건너뛰고, 금지 변형이면 그 변형만 뗀다. `--no-gate` 로 끌 수 있다 | D-094 |
| ★★ **B-2 의 전제가 사실과 달랐다** | 지시는 "변형이 빠지면 4건이 자동 해소된다" 였으나, **문제의 변형 4개가 전부 한국어**였다 (`글자`·`에도`·`남원`·`윷`). B-1 은 한국어 변형을 건드리지 말라고 했다 | ★ 프롬프트가 금지하는 **다른 조항**(상위 개념·불필요한 축약)을 근거로 뗐다. 기록은 `r020-removed-korean-variants.json` |
| 교체 대상이 9건이 아니라 4건 | 하나씩 읽고 4건만 동의했다. 5건은 근거를 적어 그대로 두었다 | ★ 지시가 "판정이 틀렸을 수 있다. 아니라고 보면 그대로 두고 근거를 적어라" 였다 |
| R020 규칙검사 11건 직접 수정 | 정답이 질문에 드러난 문항의 **질문을 고쳐 썼다**. 정답은 바꾸지 않았다 | ★ 적재 전이고 내가 만든 것이다. ★ `world-language/2` 6건은 어근 예시를 한글 음차로 든 것이 원인이었다 |
| 라운드 안 같은 답 9건 격리 | 알 가치가 높은 쪽·해당 소분류의 핵심인 쪽을 남겼다 | D-090. ★ 숫자 2묶음은 C24 로 올렸다 |
| 중복 판정 same 12건 격리 | 기존을 살리고 신규를 내렸다 | ★ R019 와 같은 기준이다. keep=a 9건은 C23 으로 올렸다 |
| 판정 스크립트에 `unjudged` | 실패한 묶음의 쌍을 파일과 화면에 남기게 고쳤다 | D-095. ★ 6쌍이 조용히 빠진 것을 우연히 발견했다 |
| ★ `r017-judged.json` 을 실수로 덮어썼다 | ★★ **즉시 `git checkout` 으로 원복했다.** 커밋 전이었고 손실은 없다 | ★ 문법만 확인하려고 스크립트를 `import()` 했는데 최상위에서 바로 실행되는 스크립트였다. 이후 `node --check` 만 쓴다 |
| R020 결과는 **적재하지 않았다** | 파일로만 쌓았다 | ★ 지시 그대로다 → C21 |

### ★★ R021 에서 자체 판단한 것

| 무엇 | 어떻게 했나 | 근거 |
|------|------------|------|
| 판정 못 한 6건을 **적재에서 뺐다** | 격리해 두고 기록을 남겼다 (`r021-hold-for-judge.json`) | ★★ "판정하지 못한 채 적재하지 마라" 는 선을 지키려면 그 건만 빼는 것이 가장 작은 조치다. 되살리기 쉽다 |
| 교체를 9건이 아니라 **3건만** | 판정 근거가 "간결하다"·"정갈하다" 뿐이면 바꾸지 않았다 | ★ 간결함은 알 가치가 아니다. R019·R020 과 같은 기준이다 |
| 라운드 안 같은 답 3묶음을 **거절 대신 고쳐 살렸다** | 답이 겹치던 쪽의 **각도를 바꿔 답을 다르게** 만들었다 (동인도회사→주식회사, 밀도→무게, 흰색→백기) | ★★ 적재 전이고 내가 만든 것이다. 거절하면 소재를 잃지만 고치면 잃지 않는다 |
| 규칙 검사 8건 질문 수정 | 정답이 드러난 것과 긴 정답을 고쳤다 | ★ R020 과 같다 |
| 무의미한 표기 변형 28개 제거 | 정규화하면 같아지는 변형 | D-099 |
| 적재 게이트에 무의미 변형 검사 추가 | `checkAnswerSet` 에 `normalize` 인자를 더했다 | D-099. ★ 유닛 22케이스 |
| `seeds-check` 에서 숫자 제외 | 라운드 안 충돌 검사만 | D-097 (건우 확정) |
| **2회차를 별도 라운드 `r021p` 로 분리** | C 의 지표와 섞이지 않게 했다 | ★ 되돌리기 쉽다. 적재 여부도 따로 정할 수 있다 |
| 프롬프트 `seed-r2-v1` · `question-r2-v1` 신설 | v2 를 그대로 두고 새로 매겼다 | ★ 지시대로다 |
| ★ Gemini 를 **더 부르지 않았다** | 게이트가 막은 뒤 우회하지 않았다 | ★★ D-056 은 이 프로젝트가 스스로 건 안전장치다. 기술적으로 뚫을 수 있어도 혼자 뚫을 일이 아니다 |
