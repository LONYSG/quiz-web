-- =============================================================================
-- 0005. 소재 기반 생성 문제 소스 등록 (R018 / 작업 D)
--
-- ★★★ 이 마이그레이션은 **아직 적용하지 않았다.**
--   R018 지시가 "코드만 고치고 실행하지 마라" 였고, 다른 세션이 브라우저로 게임을
--   테스트하고 있었기 때문이다. ★ 적재 승인이 난 뒤 `npm run db:migrate` 로 적용한다.
--
-- ★★ 왜 새 source_id 가 필요한가
--   R017부터 문제를 만드는 방식이 또 바뀌었다 (D-077).
--     gemini-gen : 카테고리를 주고 Gemini 가 문제를 여러 개 만든다
--     ★ seed-gen : 소분류 → 소재(seed) → 소재당 문제 1개. 생성 주체는 Opus 다
--
--   구분이 필요한 이유는 라이선스가 아니라 **추적**이다 —
--     · 두 방식의 중복률·난이도·알 가치가 크게 다르다 (R017/R018 실측)
--     · 나중에 "어느 방식으로 만든 문제가 실제로 잘 나갔는가" 를 물으려면
--       문제 단위로 방식을 알 수 있어야 한다
--   ★ source_id 가 없으면 그 질문에 답할 수 없다. 적재 뒤에는 소급이 매우 비싸다.
--
-- ★ 라이선스는 gemini-gen 과 같게 CC-BY-SA-4.0 으로 둔다.
--   근거는 0002 머리말과 같다 — 우리가 저작권자이고, 한 DB 안에서 라이선스가
--   갈라지면 데이터셋 배포 때 문제 단위로 갈라 내야 한다.
--   ★ 화면 표기 의무는 여전히 source_id='opentdb' 문제에만 있다.
-- =============================================================================

INSERT INTO sources (id, name, url, license, attribution, notes) VALUES
  ('seed-gen', '소재 기반 생성', NULL, 'CC-BY-SA-4.0',
   NULL,
   'R017부터의 주 확보 방식. 소분류마다 소재(seed) 목록을 먼저 뽑아 고정하고 소재 하나당 문제 하나를 만든다. '
   || '★ 생성은 Opus, 중복 판정은 Gemini, 역검증은 Sonnet 이 맡는다 — 자기 생성분을 자기가 검증하지 않는다(D-043). '
   || '★ attribution 이 NULL 인 것은 의도된 것이다 — 생성 문제에는 저작자 표시 의무가 없다.')
ON CONFLICT (id) DO NOTHING;
