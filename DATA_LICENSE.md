# 문제 데이터 라이선스

이 저장소는 **코드**와 **퀴즈 문제 데이터**에 서로 다른 라이선스를 적용한다.

## 코드

MIT License. `server/` `client/` `shared/` `pipeline/` `scripts/` `migrations/` 및 설정 파일.

## 퀴즈 문제 데이터

`data/` 아래의 퀴즈 문제 데이터는 **Creative Commons Attribution-ShareAlike 4.0 International
(CC BY-SA 4.0)** 으로 배포한다.

https://creativecommons.org/licenses/by-sa/4.0/

### 출처

| 출처 | `source_id` | 라이선스 | 저작자 표시 의무 | 비고 |
|------|------------|---------|----------------|------|
| [Open Trivia Database](https://opentdb.com/) | `opentdb` | CC BY-SA 4.0 | ★ **있다** | 영어 4지선다 문제를 한국어 주관식으로 번역·가공한 파생물 |
| Gemini 생성 | `gemini-gen` | CC BY-SA 4.0 | 없다 | ★ R011부터의 주 확보 방식. 우리 프롬프트로 직접 생성한 문제다 |
| 직접 작성 | `manual` | CC BY-SA 4.0 | 없다 | 이 프로젝트에서 작성 (시드 포함) |

문제별 출처와 라이선스는 데이터베이스의 `questions.source_id` 와 `questions.license` 컬럼,
그리고 `data/` 아래 JSON 파일의 메타데이터에 기록되어 있다.

### ★ 생성 문제의 라이선스 (R011 판단)

R011에서 문제 확보 방식을 OpenTDB 수확에서 **Gemini 직접 생성**으로 바꿨다.
생성 문제에는 CC BY-SA 의무가 **없다.** 그런데도 같은 라이선스를 붙였다. 근거는 이렇다.

1. 저장소가 Public 이고, `manual`(직접 작성) 소스를 이미 같은 이유로 그렇게 두었다.
   한 데이터베이스 안에서 라이선스가 갈라지면 데이터셋을 배포할 때 문제 단위로 갈라 내야 한다.
2. 우리가 저작권자이므로 CC BY-SA 로 두어도 잃는 것이 없다.

★ **그러나 저작자 표시 의무는 다르다.** 그 의무는 `source_id = 'opentdb'` 문제에만 있다.

### ★ 화면 표기의 조건 (R011)

OpenTDB 문제 **10건**이 아직 출제 대상으로 남아 있다(건우 지시로 지우지 않았고
`is_active` 도 건드리지 않았다). 따라서 화면의 출처 표기는 **유지해야 한다.**

★ 다만 문구를 고쳤다 — `문제 출처: Open Trivia Database` → `일부 문제 출처: …`
근거: 앞으로 문제 대부분이 생성 문제가 된다. 무조건 "문제 출처는 OpenTDB" 라고 쓰면
**사실과 다른 표기**가 된다. 표기를 지우는 것과 사실과 다르게 쓰는 것은 둘 다 틀렸다.

★ 표기를 지울 수 있는 조건 — 다음이 모두 참일 때만 지운다.

```sql
-- 이 결과가 0이어야 한다
SELECT count(*) FROM questions
 WHERE source_id = 'opentdb' AND status = 'approved' AND is_active;
```

★ 이것은 사람이 확인하고 지우는 일이다. 화면이 자동으로 판단하게 만들지 않았다.
근거: 그러려면 게임 서버가 매 화면마다 소스 분포를 조회해야 한다. 표기 하나 때문에
쿼리를 추가하는 것은 과설계다(R009 D-032와 같은 기준). 문제 10건은 곧 사라지지 않는다.

### ShareAlike 의무

Open Trivia Database 문제를 번역·가공한 파생물이므로 동일 라이선스로 공개한다.
이 데이터를 사용하거나 다시 가공하는 경우에도 CC BY-SA 4.0을 유지하고 출처를 표기해야 한다.

★ 생성 문제는 파생물이 아니므로 이 의무의 대상이 아니다. 그럼에도 같은 라이선스로
공개하는 것은 위의 판단에 따른 **우리의 선택**이다.

### 다른 라이선스의 데이터가 추가되는 경우

출처마다 라이선스를 문제 단위로 기록하고 이 문서에 추가한다.
서로 다른 라이선스의 문제들은 하나의 모음(collection)을 이루며,
각 문제는 자신의 라이선스를 유지한다.

> 이 문서는 법률 자문이 아니다. 라이선스 해석에 대한 판단이 포함되어 있다.
