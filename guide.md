# 상식 퀴즈 웹게임 개발 요구사항

## 1. 프로젝트 목적

스타크래프트의 기존 상식 퀴즈/맞히기 맵에서 게임의 핵심 재미만 가져와, 스타크래프트와 무관하게 독립적으로 실행되는 실시간 웹 기반 상식 퀴즈 게임을 개발한다.

이 프로젝트는 친구들끼리 방을 만들고 초대 링크를 공유하여 함께 플레이하는 것을 기본 사용 시나리오로 한다.

핵심 게임 방식은 다음과 같다.

* 최대 10명이 하나의 방에서 플레이
* 실시간 멀티플레이
* PC 웹 + 모바일 웹 지원
* PC와 모바일 사용자가 동일한 방에서 함께 플레이 가능
* 주관식 텍스트 퀴즈
* 별도의 답안 입력창이 없음
* **채팅창에 입력하는 모든 메시지가 동시에 답안 제출로 취급**
* 정답을 가장 먼저 제출한 사람이 1점을 획득
* 문제 경험 이력을 계정별로 저장
* 이미 풀어본 문제는 채팅은 가능하지만 정답 판정에서는 제외
* 게임 중간 참가 및 재접속 지원

구현 시 아래의 게임 규칙을 임의로 단순화하거나 변경하지 않는다.

---

# 2. MVP 범위

우선 다음 기능이 정상적으로 동작하는 프로토타입을 완성한다.

### 필수 기능

1. 회원가입
2. 로그인 / 로그아웃
3. 닉네임 관리
4. 방 생성
5. 초대 링크를 통한 방 입장
6. 최대 10명 제한
7. 로비
8. 로비 채팅
9. 게임 설정
10. 실시간 게임 진행
11. 실시간 공용 채팅
12. 채팅 메시지 기반 정답 제출
13. 서버 기준 선착순 정답 판정
14. 30초 문제 타이머
15. 마지막 10초 힌트
16. 정답 / 시간 종료 처리
17. 스킵 투표
18. 방장 강제 스킵
19. 게임 강제 종료
20. 점수 집계
21. 최종 순위
22. 다시 하기
23. 게임 중간 참가
24. 게임 중 연결 종료 / 재접속
25. 개인별 문제 경험 기록
26. 이미 경험한 문제의 정답 판정 제외
27. 개인별 문제 경험률 표시
28. 플레이어별 게임 세션 색상
29. PC / 모바일 반응형 UI

퀴즈 문제를 어디서 가져오고 어떻게 초기 DB를 구성할지는 별도 논의 사항이므로 이번 구현 요구사항에서는 결정하지 않는다.

---

# 3. 회원 시스템

간단한 계정 시스템으로 구현한다.

회원가입에 필요한 정보:

* 아이디
* 비밀번호
* 닉네임

## 아이디

* 가입 시 결정
* 변경 불가

## 비밀번호

* 변경 가능
* 복잡한 회원/권한 시스템은 필요하지 않음
* 이 프로젝트는 친구들끼리 사용하는 경량 서비스임

## 닉네임

* 변경 가능
* 게임 진행 중에는 변경 불가
* 이미 사용 중인 닉네임은 사용할 수 없음
* 닉네임의 대소문자는 구분한다.

예:

* `Player`
* `player`

는 서로 다른 닉네임으로 취급한다.

---

# 4. 방 시스템

방장이 방을 생성한다.

방에는 최소한 다음 정보가 필요하다.

* 방 ID
* 방 제목
* 방장
* 참가자
* 게임 상태
* 게임 설정
* 현재 게임 정보

## 최대 인원

한 방의 최대 플레이어 수는 10명이다.

* 관전자 기능 없음
* 10명 모두 플레이어
* 방이 가득 차면 신규 플레이어는 입장할 수 없음
* 누군가 나가서 자리가 생기면 다시 입장 가능

## 초대 링크

방장은 방 초대 링크를 복사할 수 있어야 한다.

사용자가 링크를 클릭했을 때:

### 로그인하지 않은 경우

로그인 화면으로 이동한 후 로그인 완료 시 해당 방으로 진입한다.

### 이미 로그인한 경우

바로 해당 방으로 진입한다.

방이 존재하지 않거나 이미 입장할 수 없는 상태라면 적절한 오류 메시지를 표시한다.

---

# 5. 방장

방 생성자가 최초 방장이다.

방장은:

* 게임 설정
* 게임 시작
* 방장 전용 강제 스킵
* 게임 강제 종료
* 재게임 설정 변경

등의 권한을 가진다.

## 방장 이탈

방장이 방을 나가거나 연결 종료 상태로 방을 떠나게 되면 다음 플레이어에게 자동으로 방장 권한을 이전한다.

방장 변경 이후 새 방장은 즉시 기존 방장 권한을 사용할 수 있어야 한다.

게임 도중 방장이 변경되어도 게임을 중단하지 않는다.

---

# 6. 로비

게임 시작 전 대기 화면.

표시해야 할 정보:

* 방 제목
* 현재 참가자
* 현재 인원 / 최대 인원
* 방장 표시
* 문제 수
* 게임 시작 방식
* 초대 링크
* 게임 시작 버튼
* 로비 채팅

## 참가자별 문제 경험률

각 참가자의 계정이 현재 문제 DB에서 이미 경험한 문제의 비율을 표시한다.

예:

철수 — 12.4%
영희 — 37.8%
민수 — 5.1%

이 수치는 정보 제공용이다.

**문제 경험률이 높다는 이유로 게임 시작을 막으면 안 된다.**

---

# 7. 게임 설정

방장이 게임 시작 전에 설정한다.

## 문제 수

방장이 숫자를 직접 입력한다.

예:

* 10
* 20
* 30
* 50

등.

입력값에 대한 적절한 최소/최대값 검증은 구현한다.

## 시작 방식

두 가지를 지원한다.

### 즉시 시작

게임 시작 버튼을 누르면 바로 게임 시작.

### 카운트다운

방장이 N초를 직접 입력한다.

예:

10초 후 시작

게임 시작 전 카운트다운을 표시한다.

게임이 실제로 시작된 이후에는 해당 게임의 설정을 변경할 수 없다.

---

# 8. 다시 하기

게임 종료 후 같은 방에서 다시 플레이할 수 있다.

다시 하기 시:

* 기존 참가자 유지
* 기존 방장 유지
* 직전 게임 설정 유지
* 점수 초기화
* 문제 진행 상태 초기화
* 문제 경험 기록은 초기화하지 않음

방장은 다음 게임 시작 전에 설정을 변경할 수 있다.

---

# 9. 게임 상태

게임은 명확한 상태를 가져야 한다.

개념적으로 다음과 같은 상태가 필요하다.

* LOBBY
* COUNTDOWN
* QUESTION_ACTIVE
* QUESTION_RESOLVED
* GAME_RESULT
* FORCE_ENDED

실제 구현 방식이나 이름은 적절하게 판단해서 결정한다.

중요한 것은 **문제 진행 상태와 정답 판정 타이밍이 서버 기준으로 명확하게 관리되는 것**이다.

---

# 10. 문제 진행

게임이 시작되면 설정된 문제 수만큼 문제를 순서대로 진행한다.

문제 하나의 제한시간은 정확히 30초다.

예:

문제 1 / 30

[문제 내용]

남은 시간: 30

타이머가 감소한다.

---

# 11. 문제 힌트

문제 종료 10초 전부터 힌트를 표시한다.

즉:

* 30~11초: 힌트 없음
* 10~0초: 힌트 표시

문제 내용 자체는 변경하지 않는다.

## 한글 정답

정답의 초성을 표시한다.

띄어쓰기는 제거한다.

예:

정답:

`한글`

힌트:

`ㅎㄱ`

## 영문 정답

첫 번째 알파벳을 보여주고 나머지는 `_`로 표시한다.

예:

`Apple`

→

`A____`

정확한 힌트 생성 규칙은 실제 문제 데이터 구조와 함께 구현하되, 정답 자체가 지나치게 노출되지 않도록 한다.

복수 정답이 존재하는 문제의 힌트 처리 방식은 문제 데이터 구조를 확인한 후 합리적인 방식으로 구현한다. 이 부분은 구현 전에 필요하다면 별도로 확인한다.

---

# 12. 채팅 = 답안 제출

이 게임에서 가장 중요한 핵심 규칙이다.

**별도의 답안 입력창을 만들지 않는다.**

게임 화면에는 공용 채팅 입력창 하나만 존재한다.

플레이어가 메시지를 입력하면:

1. 해당 메시지가 채팅에 표시된다.
2. 동시에 현재 문제의 답안 후보로 처리된다.

예:

철수가

`훈민정음`

을 입력하면:

```text
철수: 훈민정음
```

이라는 채팅 메시지가 표시되면서 동시에 정답 판정을 시도한다.

---

# 13. 일반 채팅도 답안 후보

플레이어가 다음과 같은 말을 입력해도:

* ㅋㅋ
* 뭐냐
* 모르겠는데
* 이거 어려움
* 아 알겠다
* 정답 이거 아니냐

모두 일반 채팅으로 표시한다.

그리고 현재 문제의 정답과 일치하는 경우에만 정답 후보가 된다.

별도의 "채팅 모드 / 답안 모드" 전환 기능은 만들지 않는다.

---

# 14. 메시지 입력

PC:

* Enter로 전송
* 전송 버튼 클릭 가능

모바일:

* 전송 버튼 터치 가능

입력 후 입력창은 비워진다.

PC에서는 가능한 한 입력창을 계속 사용할 수 있도록 UX를 구성한다.

게임 중 플레이어가 매번 입력창을 클릭해야 하는 구조는 피한다.

단, 모바일 브라우저의 키보드 동작 특성상 필요한 경우에는 자연스러운 터치 입력을 허용한다.

---

# 15. 오답

틀린 답에 대해서 별도의 "오답입니다" 메시지를 표시하지 않는다.

그냥 일반 채팅처럼 남는다.

예:

철수: 훈민정은
영희: 훈민정음
민수: 한글

이런 식으로 모두에게 공개된다.

---

# 16. 정답 문자열 비교

정답 판정은 서버에서 수행한다.

기본적인 정규화 규칙:

### 무시

* 띄어쓰기
* 영문 대소문자 차이

### 무시하지 않음

* 특수문자
* 기호
* 기타 실제 문자열 차이

오타 허용이나 유사 문자열 판정은 하지 않는다.

예를 들어:

정답:

`A+B`

입력:

`AB`

이라면 다른 문자열이므로 오답이다.

반대로:

`New York`

과

`newyork`

은 대소문자와 띄어쓰기를 무시했을 때 동일한 것으로 처리한다.

정답 비교는 fuzzy matching이 아니라 **명확한 문자열 비교**를 기본으로 한다.

---

# 17. 복수 정답

하나의 문제는 여러 개의 정답을 가질 수 있다.

예:

문제:

"조선의 첫 번째 왕은?"

허용 정답:

* 태조
* 이성계

둘 중 하나를 입력하면 정답이다.

문제 데이터 구조는 향후 복수 정답을 자연스럽게 지원할 수 있어야 한다.

---

# 18. 선착순 정답 판정

이 게임의 핵심 경쟁 요소.

여러 플레이어가 거의 동시에 정답을 입력할 수 있다.

승자 판정은:

> **서버가 먼저 정상적으로 수신하고 처리한 정답 제출**

을 기준으로 한다.

클라이언트의 시간을 기준으로 하지 않는다.

각 사용자의 PC 시간이 정확한지 신뢰하지 않는다.

인터넷 지연을 추정하여 "실제 키를 먼저 누른 사람"을 보정하려 하지 않는다.

서버가 판단할 수 있는 객관적인 기준으로 하나의 승자를 결정한다.

## Race condition 방지

동시에 여러 개의 정답 요청이 들어와도 한 문제에서 두 명이 동시에 정답자가 되는 일이 없어야 한다.

문제 상태를 서버에서 원자적으로 변경하거나 이에 준하는 방식으로 처리하여:

```text
QUESTION_ACTIVE
        ↓
첫 번째 정상 정답
        ↓
QUESTION_RESOLVED
```

가 한 번만 발생하도록 한다.

이미 문제가 해결된 이후 도착한 메시지는 해당 문제의 정답으로 처리하지 않는다.

가능하다면 서버에서 각 메시지/이벤트의 처리 순서를 명확하게 추적할 수 있도록 한다.

---

# 19. 정답 처리

정답자가 발생하면 즉시:

* 정답자 결정
* 정답자에게 +1
* 정답 공개
* 정답자 표시
* 모든 플레이어에게 결과 표시

예:

```text
정답!

철수

정답: 훈민정음

+1점
```

그 후 5초 대기.

5초가 지나면 다음 문제 시작.

---

# 20. 정답 처리 이후 채팅

정답이 나온 뒤에도 채팅 입력창은 닫지 않는다.

플레이어들은 5초 동안 계속 채팅할 수 있다.

하지만 현재 문제가 이미 종료되었으므로 이 시점의 메시지는 현재 문제의 정답으로 판정하지 않는다.

예:

```text
문제 종료
↓
철수: ㅋㅋㅋ
영희: 나도 알았는데
민수: 다음 뭐냐
↓
5초 후 다음 문제
↓
새 문제 시작
↓
이제부터 입력되는 메시지가 새 문제의 답안 후보
```

특히 이전 문제의 결과 화면에서 입력한 메시지가 다음 문제의 정답과 우연히 일치하더라도 정답 처리하면 안 된다.

---

# 21. 시간 종료

30초 동안 아무도 정답을 맞히지 못하면:

* 시간 종료
* 정답 공개
* 필요하다면 해설 표시
* 5초 대기
* 다음 문제

마지막 문제라면 5초를 기다리지 않고 결과 화면으로 이동한다.

---

# 22. 스킵

두 가지 스킵 방식을 제공한다.

## 일반 스킵 투표

모든 플레이어가 사용할 수 있다.

기능:

* 스킵 투표
* 투표 취소
* 현재 투표 수 실시간 표시
* 누가 투표했는지는 표시하지 않음

스킵 기준:

**현재 활성 플레이어의 80%**

가 찬성하면 스킵한다.

80% 계산 결과가 모든 활성 플레이어의 동의를 요구하게 되는 경우에는 한 명 적은 수를 기준으로 한다.

예:

10명 → 8표
9명 → 8표
8명 → 7표
7명 → 6표

등.

단, 활성 플레이어가 매우 적은 경우의 최소 투표 수는 별도로 안전하게 처리하여 0표만으로 스킵되는 등의 문제가 발생하지 않도록 한다.

## 연결 종료 플레이어

스킵 투표의 분모에는 현재 게임에 실제로 참여 가능한 활성 플레이어만 포함한다.

연결이 끊겨 더 이상 게임에 참여하지 않는 플레이어를 기다리거나 투표에 포함시키지 않는다.

---

# 23. 방장 강제 스킵

방장은 일반 스킵 투표와 별도로 즉시 스킵할 수 있다.

이것은 일반 투표에 포함되지 않는다.

방장 강제 스킵:

```text
방장 단축키
↓
확인창
↓
확인
↓
즉시 스킵
```

확인창은:

* 키보드 방향키
* Enter
* 마우스
* 모바일 터치

로 조작할 수 있어야 한다.

---

# 24. 키보드 단축키

게임은 가능한 한 마우스를 적게 사용하는 방향으로 만든다.

모든 주요 기능에 가능한 경우 키보드 단축키를 제공한다.

예:

* 답안/채팅 전송
* 스킵 투표
* 방장 강제 스킵
* 방장 강제 종료
* 확인창 확인/취소

단축키는 화면에 항상 알기 쉽게 표시한다.

실제 키는 브라우저 기본 단축키와 충돌하지 않는 것으로 선정한다.

구현자가 적절한 단축키를 선정하되, 단축키 때문에 일반적인 텍스트 입력이 방해받으면 안 된다.

---

# 25. 게임 강제 종료

방장은 게임 중 언제든 게임을 강제로 종료할 수 있다.

확인 절차 후 즉시 종료한다.

강제 종료 시:

* 현재 문제 즉시 종료
* 게임 진행 종료
* 당시까지의 점수를 기준으로 결과 화면 표시
* 게임 종료
* 문제 경험 기록은 삭제하지 않음

강제 종료했다고 과거의 문제 경험 이력이 롤백되면 안 된다.

---

# 26. 문제 경험 시스템

이 게임의 중요한 장기 시스템.

각 계정별로 어떤 문제를 이미 경험했는지 기록한다.

경험 기록은 게임과 별개로 장기 보존한다.

즉:

```text
사용자 A
 ├ 문제 1 경험
 ├ 문제 5 경험
 ├ 문제 7 경험
 └ 문제 20 경험
```

처럼 관리할 수 있어야 한다.

향후 통계 기능을 추가할 수 있도록 문제와 사용자 간 경험 관계를 명확하게 기록한다.

---

# 27. 문제 경험의 기준

문제를 실제로 화면에서 경험한 경우 경험한 것으로 처리한다.

특히 중간 참가자의 경우:

### 현재 문제가 진행 중일 때 입장

현재 문제를 보여주고 해당 문제를 경험한 것으로 처리한다.

### 정답 공개 순간에 입장/참여

정답이 공개된 문제를 실제로 화면에서 확인할 수 있었다면 해당 문제를 경험한 것으로 처리한다.

### 이미 종료된 문제 이후 입장

이미 완전히 종료되어 다음 문제로 넘어간 문제는 경험한 것으로 처리하지 않는다.

핵심은 **실제로 해당 문제를 접한 시점**을 기준으로 판단하는 것이다.

---

# 28. 이미 경험한 문제의 처리

현재 문제를 플레이어가 과거에 이미 경험했다면:

* 채팅 가능
* 메시지 전송 가능
* 다른 사람의 채팅도 정상적으로 확인 가능
* 하지만 해당 플레이어의 메시지는 현재 문제의 정답 판정에서 제외
* 점수를 획득할 수 없음

본인에게만 별도의 알림을 표시한다.

예:

> 이미 풀어본 퀴즈입니다.

이 알림은 다른 플레이어에게 표시하지 않는다.

다른 플레이어는 해당 사용자가 그 문제를 과거에 풀었는지 알 수 없어야 한다.

---

# 29. 문제 경험률과 게임 시작

과거 규칙과 달리 다음 규칙을 반드시 적용한다.

**문제 경험률이 높은 플레이어가 있다고 해서 게임 시작을 막지 않는다.**

예를 들어 어떤 플레이어가 전체 문제의 80%를 경험했더라도 정상적으로 게임을 시작할 수 있다.

게임 진행 중 해당 플레이어가 이미 경험한 문제가 나오면:

```text
채팅 가능
+
정답 판정 제외
+
본인에게 "이미 풀어본 퀴즈입니다." 표시
```

로 처리한다.

---

# 30. 문제 출제 시 경험 이력

문제 선정 방식은 문제 데이터 소스가 확정된 후 최종 결정할 수 있도록 한다.

다만 반드시 만족해야 하는 기본 조건은:

* 계정별 문제 경험 여부를 확인할 수 있어야 함
* 현재 플레이어별 경험 여부를 판정할 수 있어야 함
* 이미 경험한 플레이어의 답안은 해당 문제에서 정답 판정하지 않아야 함
* 현재 게임 참가자 전원이 경험한 문제만 계속 선택되어 아무도 정답할 수 없는 상황이 발생하지 않도록 해야 함

문제 선택 알고리즘의 세부 우선순위는 퀴즈 데이터 구조와 함께 결정한다.

---

# 31. 중간 참가

게임 중에도 빈 자리가 있다면 새로운 플레이어가 참가할 수 있다.

중간 참가자는 현재 게임에 즉시 합류한다.

현재 문제가 진행 중이라면:

* 현재 문제
* 남은 시간
* 현재 힌트 상태
* 현재 점수
* 참가자 목록
* 채팅

등 현재 게임 상태를 전달받는다.

현재 문제를 아직 경험하지 않았다면 해당 문제에 정상적으로 답할 수 있다.

이미 경험했다면 채팅만 가능하고 정답 판정에서는 제외한다.

---

# 32. 중간 참가자의 점수

중간 참가자의 시작 점수는 0점이다.

이후 문제부터 정상적으로 점수를 획득할 수 있다.

게임 종료 시 기존 플레이어와 동일하게 최종 순위에 포함한다.

---

# 33. 연결 종료

플레이어가 인터넷 연결 문제 등으로 연결이 끊겨도 게임은 중단하지 않는다.

게임은 계속 진행한다.

해당 플레이어:

* 점수 유지
* 문제 경험 기록 유지
* 게임을 기다리지 않음
* 최종 결과에 포함
* 연결 종료 상태 표시 가능

---

# 34. 재접속

기존 플레이어가 새로고침하거나 연결이 복구되면 기존 플레이어로 게임에 복귀할 수 있어야 한다.

복귀 시:

* 기존 계정/플레이어 식별
* 기존 점수 유지
* 현재 게임 상태 복구
* 현재 문제 복구
* 현재 남은 시간 복구
* 현재 힌트 상태 복구
* 채팅 상태 복구
* 기존 플레이어 색상 유지

게임이 이미 종료된 경우에는 결과 화면으로 복귀한다.

---

# 35. 플레이어 색상

각 게임 세션에서 참가자에게 고유 색상을 하나씩 배정한다.

목적은 빠르게 플레이어를 식별하기 위한 것이다.

### 채팅

**닉네임 자체를 해당 플레이어의 색상으로 표시한다.**

예:

철수: ㅋㅋㅋㅋ
영희: 이거 아는데
민수: 훈민정음

여기서:

* 철수라는 닉네임 자체가 철수의 게임 색상
* 영희라는 닉네임 자체가 영희의 게임 색상
* 민수라는 닉네임 자체가 민수의 게임 색상

별도의 색상 아이콘은 사용하지 않는다.

### 참가자 목록

참가자 목록에서도 닉네임의 색상을 활용한다.

색상은 해당 게임 세션 동안 유지되어야 한다.

재접속한 플레이어도 기존 색상을 유지해야 한다.

색상은 서로 충분히 구분될 수 있어야 한다.

---

# 36. 채팅 UI

게임 채팅은 문제와 별개로 계속 유지된다.

문제가 바뀌었다고 채팅 기록을 초기화하지 않는다.

새 메시지가 아래로 쌓인다.

과거 메시지는 스크롤하여 확인할 수 있다.

화면에 모든 채팅을 무한정 DOM으로 쌓아 성능 문제가 발생하지 않도록 적절히 처리하되, 사용자 입장에서 과거 채팅을 확인할 수 있는 구조를 유지한다.

새 메시지가 들어오면 일반적으로 최신 메시지가 보이도록 한다.

사용자가 과거 메시지를 보고 있는 상황에서는 강제로 현재 위치를 계속 아래로 끌어내리는 UX를 피하는 것이 좋다.

---

# 37. 로비 채팅과 게임 채팅

로비에서도 채팅이 가능해야 한다.

게임 시작 이후에도 채팅 기능은 계속 유지된다.

로비 채팅을 게임 채팅과 동일한 채팅 기록으로 이어갈지, 게임 시작 시 별도의 채팅 세션으로 분리할지는 구현 UX를 검토하여 결정한다.

단, 게임 중 채팅이 문제 변경마다 초기화되어서는 안 된다.

---

# 38. 게임 결과

설정된 문제 수를 모두 진행하면 게임 종료.

최종 결과 화면에는:

* 순위
* 닉네임
* 점수
* 참가자 상태

등을 표시한다.

예:

```text
1위  철수  18점
2위  영희  15점
3위  민수  11점
4위  준호   7점
```

---

# 39. 동점 처리

동점자는 공동 순위로 처리한다.

예:

```text
1위 철수 15점
1위 영희 15점
3위 민수 12점
```

즉 일반적인 경쟁 순위 방식으로:

15점
15점
12점

→ 1위 / 1위 / 3위

가 된다.

---

# 40. 통계 데이터

프로토타입에서 복잡한 통계 화면은 필수가 아니다.

하지만 향후 계정별 통계를 추가할 수 있도록 필요한 원천 데이터는 가능한 범위에서 저장한다.

향후 활용할 수 있는 데이터:

* 플레이한 문제 수
* 정답 수
* 정답률
* 문제별 정답 여부
* 문제별 응답 시간
* 카테고리별 성적
* 게임별 점수
* 게임 참가 기록
* 문제 경험 기록

향후 다음과 같은 기능을 추가할 수 있어야 한다.

```text
과학 정답률 72%
음악 정답률 84%
평균 정답 시간 3.1초
총 풀이 문제 1,240개
```

따라서 지금 당장 통계 UI를 만드는 것보다 **향후 통계 계산에 필요한 데이터가 손실되지 않도록 기록하는 것**이 중요하다.

---

# 41. 모바일 UI

모바일 브라우저에서 정상적으로 게임할 수 있어야 한다.

모바일에서는 터치 조작을 기본으로 한다.

필수 기능:

* 문제 확인
* 타이머 확인
* 채팅 확인
* 채팅 입력
* 전송
* 스킵 투표
* 방장 기능
* 참가자 확인
* 결과 확인

세로 화면을 우선적으로 최적화하되 가로 화면에서도 사용 가능하도록 한다.

---

# 42. PC UI

PC에서는 키보드 중심 플레이를 지원한다.

특히 게임의 핵심이 빠른 정답 입력이므로:

* 입력창 접근성이 좋아야 함
* Enter 입력이 즉시 전송되어야 함
* 입력 후 다시 타이핑하기 편해야 함
* 불필요한 클릭 최소화
* 단축키 제공

등을 중요하게 생각한다.

---

# 43. PC와 모바일의 동일 게임

PC 플레이어와 모바일 플레이어는 동일한 서버의 동일한 게임 상태를 공유해야 한다.

예:

```text
철수 - PC
영희 - PC
민수 - Android
준호 - iPhone
```

모두 하나의 방에서 동일한:

* 문제
* 타이머
* 정답 결과
* 점수
* 채팅
* 스킵 투표
* 게임 상태

를 실시간으로 공유한다.

클라이언트마다 별도의 게임 상태를 가지면 안 된다.

---

# 44. 서버 권한과 클라이언트 신뢰

게임의 중요한 판정은 클라이언트가 결정하지 않는다.

특히 다음은 서버가 authoritative하게 관리한다.

* 게임 시작
* 문제 시작
* 문제 종료
* 타이머 상태
* 정답 판정
* 정답자
* 점수
* 스킵 투표
* 강제 스킵
* 게임 강제 종료
* 방장
* 문제 경험 기록

클라이언트는 서버 상태를 표시하고 사용자 입력을 전달하는 역할을 기본으로 한다.

---

# 45. 실시간 동기화

실시간 게임이므로 모든 참가자가 최대한 동일한 게임 상태를 보도록 한다.

특히:

* 문제 시작 시점
* 타이머
* 정답 처리
* 스킵
* 다음 문제
* 점수
* 참가자 입장/퇴장
* 채팅

은 실시간으로 동기화되어야 한다.

클라이언트가 자체적으로 "30초가 지났으니 다음 문제"라고 판단하는 구조를 기본 판정 로직으로 사용하면 안 된다.

서버가 실제 게임 상태를 결정하고 클라이언트가 이를 표시한다.

---

# 46. 타이머

30초 타이머는 클라이언트에서 단순히 카운트다운하는 방식만 사용하지 말고 서버의 문제 시작 시각/종료 시각을 기준으로 동기화한다.

클라이언트가 잠시 멈추거나 백그라운드로 갔다가 돌아와도 실제 게임 진행 상태와 다시 동기화되어야 한다.

---

# 47. 브라우저 새로고침

새로고침한다고 플레이어가 새로운 참가자로 중복 입장되면 안 된다.

가능한 경우 기존 로그인/게임 세션 정보를 이용하여 기존 플레이어를 복구한다.

특히:

* 기존 점수
* 현재 게임
* 플레이어 색상
* 문제 경험 여부

등이 유지되어야 한다.

---

# 48. 데이터 영속성

다음 데이터는 단순한 메모리 상태로만 관리해서는 안 된다.

장기적으로 저장해야 하는 데이터:

* 계정
* 닉네임
* 문제 경험 기록
* 게임 기록
* 게임별 점수
* 향후 통계 계산에 필요한 정보

반면 현재 진행 중인 실시간 게임 상태는 서버 메모리와 영속 데이터베이스를 적절히 조합해 구현할 수 있다.

구체적인 기술 선택은 구현 환경에 맞게 판단한다.

---

# 49. 예외 상황

다음과 같은 상황을 반드시 고려한다.

### 마지막 플레이어가 나감

게임을 계속할 수 있는지 판단하고, 플레이 가능한 인원이 없다면 게임을 적절히 종료한다.

### 방장이 나감

다음 플레이어에게 방장 권한 이전.

### 게임 중 플레이어 이탈

게임을 중단하지 않음.

### 플레이어 재접속

기존 상태 복구.

### 방이 가득 참

입장 거부 + 명확한 안내.

### 존재하지 않는 방 링크

명확한 오류 화면.

### 게임이 이미 종료된 방

결과 또는 로비 상태를 적절하게 표시.

### 동시에 정답 제출

서버 처리 순서에 따라 정확히 한 명만 정답자로 선정.

### 정답 처리 직후 추가 메시지

이전 문제 정답으로 처리하지 않음.

### 타이머 종료 직전 정답

서버의 실제 문제 상태를 기준으로 처리.

### 스킵과 정답이 동시에 발생

서버에서 하나의 일관된 순서를 보장하여 이미 종료된 문제에 중복 처리가 발생하지 않도록 한다.

---

# 50. 중요한 게임성 우선순위

이 게임에서 가장 중요한 것은 화려한 UI가 아니라 다음 네 가지다.

### 1. 정확한 선착순

친구들이 동시에 정답을 입력해도 서버에서 한 명의 승자를 명확하게 결정해야 한다.

### 2. 실시간성

문제, 채팅, 점수, 타이머, 스킵 등이 모든 참가자에게 빠르게 동기화되어야 한다.

### 3. 단순한 입력

플레이어가 문제를 보고 바로 타이핑할 수 있어야 한다.

**채팅 = 답안**이라는 구조를 절대 별도의 답안 제출 UI로 바꾸지 않는다.

### 4. 문제 경험 시스템

플레이어가 이미 풀어본 문제 때문에 게임의 공정성이 무너지지 않도록 개인별 경험 여부를 기준으로 정답 판정을 제어한다.

---

# 51. 구현 시 임의로 추가하지 말아야 할 것

현재 MVP에서는 다음 기능을 임의로 추가하지 않는다.

* 팀전
* 아이템
* 배팅
* 연속 정답 보너스
* 난이도별 점수
* 복잡한 랭킹 시스템
* 객관식
* 이미지 문제
* AI 문제 생성
* AI 해설 생성
* 복잡한 프로필
* 소셜 기능
* 친구 목록
* 공개 방 검색
* 관전자
* 음성/TTS

이러한 기능은 향후 확장 아이디어로 남겨둔다.

현재는 **실시간 주관식 퀴즈의 핵심 게임 루프를 완성하는 것**을 최우선으로 한다.

---

# 52. 향후 확장을 고려해야 하는 영역

MVP에서 구현하지 않더라도 다음 기능을 나중에 추가하기 어렵지 않도록 구조를 지나치게 하드코딩하지 않는다.

향후 후보:

* 객관식
* 이미지 퀴즈
* 초성 퀴즈
* 순서 맞히기
* 팀전
* 아이템
* 힌트
* 연속 정답 보너스
* 난이도별 점수
* 카테고리 선택/제외
* 개인 통계
* 랭킹
* AI 문제 생성
* AI 오답 보기 생성
* AI 난이도 평가
* AI 해설 생성

단, 미래 기능을 위해 현재 MVP를 과도하게 복잡하게 만들지는 않는다.

---

# 53. 개발 우선순위

개발은 다음 순서로 검증하는 것을 권장한다.

### Phase 1 — 기본 계정/방

* 회원가입
* 로그인
* 닉네임
* 방 생성
* 방 입장
* 최대 10명
* 방장

### Phase 2 — 로비

* 참가자 목록
* 경험률
* 게임 설정
* 초대 링크
* 로비 채팅
* 게임 시작

### Phase 3 — 핵심 게임

* 문제 표시
* 30초 타이머
* 채팅
* 정답 판정
* 서버 선착순
* 점수
* 정답 공개
* 다음 문제

### Phase 4 — 게임 시스템

* 힌트
* 스킵 투표
* 방장 강제 스킵
* 강제 종료
* 결과
* 다시 하기

### Phase 5 — 실시간 안정성

* 중간 참가
* 연결 종료
* 재접속
* 상태 복구
* 동시 정답 race condition
* 타이머 동기화

### Phase 6 — 경험 시스템

* 문제 경험 기록
* 경험률
* 이미 경험한 문제의 답안 판정 제외
* 개인 알림

### Phase 7 — UI/UX

* PC 최적화
* 모바일 최적화
* 키보드 단축키
* 플레이어 색상
* 채팅 UX
* 각종 예외 상황 UI

---

# 54. 최종 개발 원칙

이 프로젝트는 "기능이 많은 퀴즈 서비스"를 만드는 것이 목적이 아니다.

**친구들이 방에 모여서 문제를 보고 채팅창에 미친 듯이 답을 입력하는 재미**가 핵심이다.

따라서 다음을 가장 중요하게 생각한다.

1. 빠른 입력
2. 정확한 서버 판정
3. 실시간 동기화
4. 간단한 UI
5. 최대 10명의 원활한 멀티플레이
6. 모바일/PC 동시 플레이
7. 문제 경험 시스템
8. 재접속 안정성
9. 향후 확장 가능한 데이터 기록

구현 과정에서 요구사항이 모호하거나 서로 충돌하는 부분이 발견되면 임의로 중요한 게임 규칙을 변경하지 말고 먼저 확인한다.

특히 **정답 판정, 문제 경험, 게임 상태 전환, 스킵, 재접속**과 관련된 부분은 게임성에 직접 영향을 주므로 임의의 편의적인 구현으로 변경하지 않는다.

퀴즈 문제의 실제 데이터 소스 및 초기 문제 구축 방법은 별도 논의 후 결정한다.

# 55. 문서화 및 작업 연속성

이 프로젝트는 개발 과정에서 **현재 대화 세션의 메모리에 의존하지 않는 것**을 중요한 개발 원칙으로 한다.

향후 다음과 같은 상황에서도 다른 AI 또는 다른 개발자가 프로젝트를 자연스럽게 이어받을 수 있어야 한다.

* 다른 PC에서 프로젝트를 내려받아 작업
* 새로운 AI 세션에서 작업
* 이전 AI 세션의 대화 내용을 전혀 볼 수 없는 상태
* 프로젝트에 새로 합류한 협업자
* Git 저장소를 clone한 후 처음 프로젝트를 확인하는 개발자
* 기존 개발자가 아닌 다른 AI를 이용해 유지보수
* 장기간 개발을 중단한 후 다시 개발 재개

따라서 **코드와 Git 저장소만 가지고도 프로젝트의 현재 상태를 충분히 파악하고 작업을 재개할 수 있도록 문서화한다.**

---

## 55-1. MD 문서 관리

프로젝트의 중요한 정보는 적절한 Markdown 문서로 관리한다.

문서의 구체적인 파일명과 디렉터리 구조는 구현 환경에 맞게 결정하되, 다음 내용은 반드시 어딘가에 명확하게 기록되어 있어야 한다.

### 프로젝트 개요

* 프로젝트 목적
* 핵심 게임 규칙
* 현재 구현 범위
* 향후 확장 예정 기능
* 현재 개발 단계

### 게임 규칙

* 방 시스템
* 참가자 제한
* 게임 시작
* 문제 진행
* 타이머
* 힌트
* 채팅/답안 시스템
* 정답 판정
* 점수
* 스킵
* 게임 종료
* 재게임
* 문제 경험 시스템
* 중간 참가
* 재접속
* 방장 권한
* 기타 예외 처리

### 기술 관련 정보

프로젝트에 실제 사용된 기술과 중요한 설정을 기록한다.

단순히 "React 사용"처럼 적는 것이 아니라 실제 프로젝트를 이해하는 데 필요한 정보를 기록한다.

예:

* 사용 기술
* 주요 라이브러리
* 데이터 저장 방식
* 실시간 통신 방식
* 인증 방식
* 실행 방법
* 개발 환경
* 환경 변수
* 외부 서비스 의존성
* 주요 설정
* 배포 방법

단, 비밀번호/API Key 등의 실제 비밀값은 문서에 기록하지 않는다.

---

# 56. 현재 개발 상태 기록

프로젝트에는 **현재 어디까지 구현되어 있는지를 확인할 수 있는 문서**가 있어야 한다.

예를 들어 다음과 같은 상태를 명확하게 구분한다.

```text
완료
진행 중
미구현
버그 있음
보류
결정 필요
```

기능별로 현재 상태를 기록한다.

예:

```text
회원가입       완료
로그인         완료
방 생성        완료
로비 채팅      완료
게임 진행      진행 중
스킵 투표      미구현
재접속         버그 있음
통계           보류
퀴즈 데이터    결정 필요
```

이 정보만 읽어도 새로운 개발자가 **현재 프로젝트가 어느 단계인지 바로 파악할 수 있어야 한다.**

---

# 57. 작업 기록

개발 과정에서 중요한 작업은 Markdown 문서에 기록한다.

단순히:

> 로그인 기능 수정

이라고 기록하는 것이 아니라, 필요한 경우 다음 내용을 남긴다.

* 무엇을 작업했는지
* 왜 작업했는지
* 어떤 문제가 있었는지
* 어떤 결정을 내렸는지
* 어떻게 해결했는지
* 어떤 파일/기능에 영향을 주었는지
* 아직 남은 문제가 무엇인지

예:

```text
문제:
재접속 시 기존 플레이어가 신규 플레이어로 생성되는 문제 발생.

원인:
브라우저 재접속 시 기존 게임 참가자 식별 정보가 복구되지 않음.

해결:
기존 플레이어 세션을 식별할 수 있도록 복구 로직 수정.

영향:
게임 중 새로고침 시 기존 점수와 플레이어 색상을 유지할 수 있게 됨.

검증:
10명 방에서 플레이어 재접속 테스트 완료.

남은 문제:
게임 종료 직후 재접속 시 결과 화면 복구 여부 추가 검증 필요.
```

이 정도의 기록을 남겨서 다른 AI가 이전 대화를 보지 않아도 작업 맥락을 이해할 수 있도록 한다.

---

# 58. 의사결정 기록

게임 규칙이나 기술적으로 중요한 사항을 변경할 때는 **왜 변경했는지**를 기록한다.

특히 다음과 같은 사항은 변경 이력을 남긴다.

* 게임 규칙 변경
* 정답 판정 방식 변경
* 문제 경험 시스템 변경
* 스킵 조건 변경
* 데이터 구조 변경
* 실시간 통신 방식 변경
* 인증 방식 변경
* 중요한 UX 변경

예:

```text
변경 전:
문제 경험률이 높은 플레이어가 있으면 게임 시작을 제한.

변경 후:
게임 시작은 제한하지 않음.
이미 경험한 문제에 대해서는 해당 플레이어의 채팅을 허용하되
정답 판정에서는 제외.

변경 이유:
문제 경험이 많은 플레이어 때문에 다른 플레이어가 게임을 시작하지 못하는
상황을 방지하고, 이미 경험한 문제를 알고 있는 플레이어도 채팅에는
참여할 수 있도록 하기 위해 변경.
```

이런 기록이 있어야 미래의 AI가 오래된 문서를 보고 과거 규칙으로 되돌리는 실수를 방지할 수 있다.

---

# 59. 요구사항 변경 관리

초기 요구사항과 실제 구현 과정에서 변경된 사항을 구분한다.

중요한 변경이 발생하면 기존 문서를 무작정 덮어쓰지 말고, **현재 적용되는 최종 규칙과 변경 이유를 추적할 수 있도록 기록한다.**

단, 오래된 규칙과 현재 규칙이 서로 충돌하여 새로운 개발자가 혼란스러워지는 문서 구조는 피한다.

가장 중요한 것은:

> **현재 실제로 적용되는 규칙을 한 곳에서 명확하게 확인할 수 있어야 한다.**

변경 이력은 그 다음으로 과거의 의사결정을 추적하기 위한 용도로 사용한다.

---

# 60. 작업 재개를 고려한 문서

새로운 AI가 프로젝트를 처음 열었을 때 다음 질문에 답할 수 있어야 한다.

1. 이 프로젝트가 무엇인가?
2. 현재 어디까지 개발됐는가?
3. 지금 구현해야 하는 기능은 무엇인가?
4. 현재 게임 규칙은 무엇인가?
5. 이미 완료된 기능은 무엇인가?
6. 알려진 버그는 무엇인가?
7. 현재 진행 중인 작업은 무엇인가?
8. 다음 작업으로 무엇을 해야 하는가?
9. 최근에 어떤 중요한 결정이 있었는가?
10. 어떤 기능이 의도적으로 보류되어 있는가?
11. 실행하려면 무엇이 필요한가?
12. 테스트는 어떻게 하는가?
13. Git의 현재 상태는 어떤가?

문서를 읽고 이 질문에 대부분 답할 수 있어야 한다.

---

# 61. AI 작업 인수인계

이 프로젝트는 사람이 아닌 **다른 AI가 개발을 이어받는 상황도 공식적인 개발 시나리오**로 취급한다.

새로운 AI에게 별도의 장황한 설명을 제공하지 않아도:

```text
Git clone
↓
프로젝트 문서 확인
↓
현재 상태 확인
↓
최근 작업 기록 확인
↓
실행
↓
테스트
↓
다음 작업 진행
```

이 가능해야 한다.

따라서 AI가 작업할 때 현재 대화 내용을 문서 대신 사용하는 것을 피한다.

**중요한 결정이나 작업 상태가 대화에만 존재해서는 안 된다.**

---

# 62. 작업 시작 전 확인

AI가 새로운 작업을 시작하기 전에 프로젝트의 문서를 먼저 확인한다.

최소한 다음 정보를 확인한 후 작업한다.

* 프로젝트 개요
* 현재 게임 규칙
* 현재 구현 상태
* 최근 작업 기록
* 알려진 버그
* 보류된 기능
* 현재 작업 대상

기존 구현과 문서가 서로 다르면 임의로 어느 쪽이 맞다고 판단하지 말고 실제 코드를 확인한다.

필요한 경우 현재 동작과 문서의 불일치를 작업 기록에 남긴다.

---

# 63. 작업 종료 전 문서 갱신

하나의 작업을 완료한 후에는 문서도 함께 업데이트한다.

작업 종료 시 최소한 다음을 확인한다.

* 구현 상태 업데이트
* 새로 발생한 버그 기록
* 해결한 문제 기록
* 중요한 설계 결정 기록
* 테스트 결과 기록
* 아직 남은 작업 기록

코드만 수정하고 문서를 방치하지 않는다.

---

# 64. Git 관리

Git을 단순히 코드 백업 용도로 사용하지 않는다.

**Git 저장소 하나만 받아도 프로젝트의 개발 이력을 이해할 수 있는 상태**를 목표로 한다.

커밋은 하나의 의미 있는 작업 단위로 구성한다.

가능하면 다음과 같이 명확한 작업 단위로 커밋한다.

```text
회원가입 기능 구현
로그인 기능 구현
방 생성 기능 구현
로비 참가자 동기화 구현
채팅 기능 구현
정답 선착순 판정 구현
문제 타이머 동기화 수정
재접속 상태 복구 구현
```

서로 무관한 기능을 하나의 거대한 커밋에 섞지 않는다.

---

# 65. Git 커밋 메시지

커밋 메시지만 봐도 무엇이 변경됐는지 이해할 수 있도록 작성한다.

좋은 예:

```text
feat: 방 생성 및 참가 기능 구현
feat: 채팅 기반 정답 판정 구현
fix: 동시 정답 제출 race condition 수정
fix: 재접속 시 플레이어 상태 복구
docs: 게임 경험 시스템 규칙 업데이트
refactor: 게임 상태 관리 로직 정리
```

실제 커밋 규칙은 프로젝트의 개발 환경에 맞게 정해도 되지만, 일관된 규칙을 유지한다.

---

# 66. 커밋과 문서의 관계

중요한 기능 변경이 발생하면 코드와 관련 문서의 상태가 서로 일치해야 한다.

예를 들어:

```text
코드:
이미 경험한 문제의 정답 판정 제외 구현

문서:
문제 경험 시스템에 해당 규칙 기록
```

처럼 관리한다.

코드는 최신인데 문서는 과거 상태를 설명하거나, 문서에는 구현됐다고 되어 있는데 실제 코드에는 없는 상태를 장기간 방치하지 않는다.

---

# 67. Git 작업 시 주의사항

AI가 기존 프로젝트를 수정할 때 무분별하게 기존 코드를 삭제하거나 대규모로 재작성하지 않는다.

먼저:

1. 현재 구현 확인
2. 관련 문서 확인
3. 기존 동작 확인
4. 변경 범위 파악
5. 필요한 부분만 수정
6. 테스트
7. 문서 갱신
8. Git 변경사항 확인

순서로 작업한다.

특히 다른 개발자가 이미 작성한 코드를 충분히 확인하지 않고 "더 좋은 구조"라는 이유만으로 전체 구조를 재작성하지 않는다.

---

# 68. 작업 중 발견한 문제

작업 도중 현재 작업과 직접 관련 없는 버그나 개선점을 발견할 수 있다.

이 경우 무조건 함께 수정하지 않는다.

현재 작업에 반드시 필요한 문제인지 판단한다.

관련 없는 문제라면:

* 기록
* 우선순위 지정
* 향후 작업으로 남김

처리한다.

이를 통해 하나의 작업이 끝없이 커지는 것을 방지한다.

---

# 69. TODO / 보류 기능

아직 구현하지 않을 기능은 코드 곳곳에 무작정 TODO를 남기는 것만으로 관리하지 않는다.

향후 개발할 기능은 적절한 문서에서 관리한다.

예:

```text
보류:
- 팀전
- 객관식
- 아이템
- AI 문제 생성
- 개인 통계 화면
```

그리고 실제로 개발을 시작할 때 해당 기능의 요구사항을 구체화한다.

---

# 70. 테스트 기록

핵심 게임 로직은 가능한 한 테스트할 수 있어야 한다.

특히 다음은 중요하게 테스트한다.

### 정답 판정

* 정상 정답
* 오답
* 대소문자 차이
* 띄어쓰기 차이
* 특수문자
* 복수 정답
* 동시 정답 제출

### 게임 상태

* 정상 시작
* 카운트다운 시작
* 정답
* 시간 종료
* 스킵
* 강제 스킵
* 마지막 문제
* 강제 종료
* 다시 하기

### 참가

* 정상 입장
* 10명 초과
* 게임 중간 참가
* 기존 문제 경험자 입장
* 연결 종료
* 재접속
* 방장 이탈
* 방장 변경

### 경험 시스템

* 처음 보는 문제
* 이미 경험한 문제
* 문제 경험 기록
* 강제 종료 후 경험 기록
* 재게임 후 경험 기록
* 중간 참가자의 경험 처리

테스트를 수행했다면 중요한 테스트 결과를 작업 기록에 남긴다.

---

# 71. 문서의 신뢰성

프로젝트 문서는 단순한 메모장이 아니다.

**새로운 개발자가 실제 코드와 함께 참고할 수 있는 프로젝트의 공식적인 개발 기록**으로 취급한다.

따라서 문서에 추측성 내용을 확정된 사실처럼 기록하지 않는다.

확정되지 않은 사항은:

* 결정 필요
* 논의 중
* 보류

등으로 명확히 구분한다.

---

# 72. 최종 목표

이 프로젝트의 문서화 목표는 다음과 같다.

> "현재 AI와의 대화 기록을 전부 잃어버려도 Git 저장소와 프로젝트 문서만 보면 다른 AI가 개발을 이어갈 수 있어야 한다."

더 나아가:

> "협업자가 Git 저장소를 clone하고 자기 PC에서 자기 AI를 사용하더라도 기존 개발 맥락을 거의 잃지 않고 자연스럽게 작업을 이어갈 수 있어야 한다."

이를 위해 **코드, 문서, Git 커밋 이력의 세 가지가 서로 일치하는 상태**를 유지한다.

개발 과정에서 중요한 정보가 대화에만 남아 있지 않도록 한다.
