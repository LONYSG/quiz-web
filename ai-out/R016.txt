===========================================
ROUND: 016
DATE: 2026-09-15
TASK: ★★ Phase 6 경험·마스킹 / ★★ Phase 4 결과 화면 / ★★ Phase 7 일부 한 화면 / 확인 대기 문서
STATUS: 완료 — ★ 네 축 전부. 브라우저 수동 확인만 남았다 (docs/13-PENDING-CHECKS.md)
===========================================


┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ ★★ 먼저 말할 것                                                     ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

★★★ **지시한 네 축을 전부 끝냈습니다.** 순서를 바꾸지 않았습니다.
   (1) Phase 6 마스킹  (2) Phase 4 결과 화면  (3) Phase 7 한 화면  (4) 확인 대기 문서

★★ **정답 마스킹이 들어갔습니다.** 지금까지는 경험자가 채팅에 정답을 쓰면 그대로 보였습니다.
   ★ 이제 다른 사람 화면에서는 "가려짐" 칩으로 바뀝니다. **서버에서 지우고 보냅니다** —
     개발자 도구로도 원문이 나오지 않습니다.

★★★ **가장 신경 쓴 것은 마스킹이 정답 판정을 건드리지 않게 만드는 것이었습니다.**
   ★ 마스킹 판정이 틀리면 **승패가 바뀔 수 있는** 자리였습니다 (R003 3-3 이 경고한 지점).
   ★ 코드에서 두 경로를 분리하고, 봇이 그것을 단정합니다 —
     경험자의 정답이 가려지는 **바로 그 문제**에서 미경험자가 정상적으로 정답 처리됩니다.

★★ **"스크롤 없이 한 화면" 을 게임 화면과 결과 화면에서 달성했습니다.**
   ★ 1280×720 기준으로 게임 화면 **1.66배 → 1.00배**, 결과 화면 **1.00배**.
   ★★ 그리고 **ui-check 게이트로 만들었습니다.** 다음 Phase 에서 요소가 늘어 넘치면 그 라운드에 걸립니다.
   ★ 로비는 2.54배 → **1.55배**. 아직 못 맞췄습니다. 이유와 다음 순서를 3-3 에 적었습니다.

★ **`grep -r TEMP-` 가 코드에서 0건**입니다. Phase 4 완료 조건을 충족했습니다.

★ 확인하실 것은 **`docs/13-PENDING-CHECKS.md` 하나만 보시면 됩니다.**
  R014·R015·R016 의 항목을 합쳐 A급 9개 / B급 10개로 정리했습니다.

verify 통과 — vitest **330건**(314 + skip 13 → **skip 0**) / smoke 10/10 / ui-check **107/107**.
봇 시나리오 **22종 전부 통과** (신설 mask 23 / result 31).
태그 `v0.6-phase4`, `v0.7-phase6`. 커밋 5개 push 완료.


═══════════════════════════════════════════════════════════════════════
1. ★★ Phase 6 — 경험 시스템 / 마스킹
═══════════════════════════════════════════════════════════════════════

──────────────────────────────────────────
1-1. 경험자 표시 (A-1)
──────────────────────────────────────────

★ 본인에게만 — "이미 풀어본 퀴즈입니다" 배지. 문제 시작과 동시에 뜹니다.
★ 전원에게 — 문제 하단에 경험자 목록. **guide 28절 폐기 결정(D-011)의 구현**입니다.

★ 판단한 것 — **닉네임에 색을 실어 보냅니다** (`experiencedPlayers`).
  ★ 근거: 이 게임은 사람을 색으로 구분합니다(guide 47절). 참가자 목록·점수판·채팅이 전부 그렇습니다.
    ★ 경험자 목록만 검은 글씨면 "누구인가" 를 다시 찾아 읽어야 합니다.
  ★★ 닉네임으로 색을 역추적하지 않습니다 — 닉네임이 겹치면 엉뚱한 색이 붙습니다.

★ 개인별 분기는 Phase 1 에서 만든 `emitRoomPerPlayer` 를 그대로 씁니다. 새로 만들지 않았습니다.

──────────────────────────────────────────
1-2. 마스킹 구현 (A-2 / A-4)
──────────────────────────────────────────

★ Phase 0 에 시그니처와 테스트 16건이 skip 상태로 있었습니다. **그 skip 을 풀고 전부 통과시켰습니다.**

구현 규칙 (01-GAME-RULES 12장이 정본입니다)

  · ★★ **서버에서 가립니다.** 클라이언트에 원문을 보내고 CSS 로 가리는 방식은 즉시 뚫립니다.
  · ★★ **마스크 길이는 고정**입니다 (센티널 1글자).
      ★ 정답 길이는 힌트보다 강한 정보이고 문제 시작 직후부터 노출됩니다. `*****` 는 금지입니다.
  · ★ 센티널은 유니코드 사설 사용 영역(U+E000)이라 **사용자가 입력할 수 없습니다.**
      ★ 즉 사용자가 "가려진 척" 위조할 수 없습니다.
  · ★ **긴 정답부터** 치환합니다. "태조" 를 먼저 치우면 "태조 이성계" 의 절반이 남습니다.
  · ★ 찾는 중에 치환하지 않습니다. 전부 모은 뒤 원문 순서대로 조립합니다 (인덱스가 어긋납니다).
  · ★ Q-43 — 정규화 길이 2 이하 정답은 **메시지 전체가 정답과 같을 때만** 가립니다.
      ★ 그러지 않으면 정답 "달" 때문에 "달라졌네" 가 가려져 정상 대화가 불가능해집니다.
  · ★ 본인 화면에는 **원문**이 보이고 "가려져서 전송됨" 만 덧붙습니다.
      ★ 자기가 무엇을 썼는지 모르면 대화가 불가능합니다.

★★ 인덱스 매핑에서 결함을 하나 찾았습니다 (A-4).

  ★ `buildNormalizedIndex` 의 `map[e]` 는 **다음 문자의 시작**입니다.
    ★ 그대로 쓰면 매칭 뒤의 공백까지 먹습니다 —
      "고 무 줄 아님?" → `[가려짐]아님?` 처럼 **문장이 붙어 버립니다.**
    → ★ 매칭의 끝을 따로 기록하는 `mapEnd` 를 추가했습니다.
  ★ Phase 0 에서 이미 잡았던 이모지 문제(서로게이트 페어)도 회귀 테스트로 고정했습니다.

──────────────────────────────────────────
1-3. ★★★ 판정과의 분리 (A-3) — 이번 라운드에서 가장 중요한 부분입니다
──────────────────────────────────────────

★★ 위험은 이것이었습니다 — 마스킹 조건("이 사람이 경험자인가")을 잘못 판정하면
  ★ 가려지지 않아야 할 것이 가려지는 데서 끝나지 않고,
  ★★ **그 잘못된 판단이 정답 판정까지 오염시킬 수 있습니다.** 그러면 승패가 바뀝니다.

★ 그래서 코드 구조로 분리했습니다 (D-073).

    단계 2  판정   judgeAnswer(room, player, { rawText: rawNfc, ... })   ← 원문만 본다
    단계 3  마스킹  maskForBroadcast(room, senderId, rawNfc)              ← 결과를 바꾸지 않는다

  · ★★ **순서가 규칙입니다.** 판정이 먼저 끝나고 마스킹은 그 뒤의 **출력 변환**입니다.
      ★ 마스킹을 위로 올리면 분리가 깨집니다. 그 사실을 주석에 못 박았습니다.
  · ★ `maskForBroadcast` 는 판정 결과를 인자로 받지도, 돌려주지도 않습니다.
      ★ 두 경로가 만나는 유일한 지점은 **같은 원문을 읽는다** 는 것뿐입니다.
  · ★ 마스킹은 **동기 코드**입니다. 정답 집합은 이미 메모리에 있습니다.

★★ 봇이 이것을 단정합니다 — 경험자의 정답이 가려지는 **같은 문제**에서
  미경험자가 같은 정답을 쳐서 **정상 정답 처리**되고, 그 메시지는 가려지지 않습니다.

★ 대가: 같은 문자열을 두 번 정규화합니다(판정 1 + 마스킹 1).
  ★ R015 부하 실측에서 판정 지연이 2ms 였고 이번에도 체감 변화가 없습니다.
  ★★ 성능보다 분리가 훨씬 중요합니다.

──────────────────────────────────────────
1-4. 테스트 16케이스 (A-6)
──────────────────────────────────────────

  #1~#13   ★ 단위 테스트 (`shared/src/mask.test.ts`) — skip 을 풀어 전부 통과
           조사 / 띄어쓰기 우회 / 부분 일치 / 반복 / 영문 대소문자 / 복수 정답 /
           긴 것 우선 / ★ 초성·기호 한계(막지 못하는 것을 명시) / Q-43
  ★★★ #14  미경험자의 판정이 영향받지 않는다      → 봇 `mask` [3]
  #15      정답 공개 후에는 마스킹하지 않는다      → 봇 `mask` [4]
  #16      본인 화면에는 원문 + 가려짐 표시        → 봇 `mask` [2]

★ 추가로 넣은 단위 테스트 — 이모지 인덱스 / ★ 마스크 길이가 정답 길이를 드러내지 않는 것 /
  매칭 없을 때 원문 유지 / 빈 입력 방어. **총 19건.**

★ 경험률(A-5)은 봇이 확인합니다 — 게임이 끝난 뒤 로비로 돌아오면 경험률이 올라가 있습니다.
  ★ 갱신 시점은 기존대로입니다(로비 진입 / 참가자 변동 / 게임 종료 후 복귀). 주기 갱신은 넣지 않았습니다.

★ 실측에서 한 번 흔들렸습니다 — 시나리오가 한 번 통과하고 한 번 실패했습니다.
  ★★ 원인은 **뽑힌 문제의 정답이 2글자**였던 것입니다. Q-43 때문에 가려지지 않는 것이 **정상**입니다.
  → ★ 정답 3글자 이상 문제만 남기고 시작하도록 고쳤습니다. 테스트가 무작위에 의존하지 않게 됐습니다.


═══════════════════════════════════════════════════════════════════════
2. ★★ Phase 4 — 결과 화면 / 다시 하기
═══════════════════════════════════════════════════════════════════════

★ `grep -r TEMP-P4` → **0건**. 완료 조건을 충족했습니다.

──────────────────────────────────────────
2-1. 결과 화면 (B-1)
──────────────────────────────────────────

★ 넣은 것
  · 순위 / 닉네임(색) / 점수 — ★ 동점은 공동 순위 (Phase 0 순수 함수)
  · ★★ **문제별 기록** — 문제 / 정답 / 정답자(색) / **응답 시간** / 이미 풀어본 사람 수
  · ★ **사람별 요약** — 정답 수 / 평균 정답 시간 / 최속 기록
  · ★ 종료 사유 네 갈래 (정상 / 강제 종료 / 조기 종료 / 일시정지 만료)
  · ★ 마지막 문제 정답 (Q-17 — 마지막 문제는 5초를 기다리지 않습니다)

★★ **중단된 문제의 정답은 보여주지 않습니다** (D-074).
  ★ 근거: 아무도 정답을 보지 못했고 **경험 기록도 남지 않았습니다**.
    ★★ 여기서 공개하면 "경험 기록 없이 정답만 아는" 사람이 생기고,
      그 문제는 경험 기록이 없으므로 **다음 게임에 다시 나올 수 있습니다.** 그때 그 사람만 유리합니다.
  ★ 목록에서 아예 빼지는 않습니다 — 몇 번까지 진행했는지가 보여야 결과가 이해됩니다.

★★ guide 40절 통계 9종 중 **무엇을 넣을지 판단했습니다** (지시대로 근거를 밝힙니다).

  넣은 것    ★ 문제별 정답 여부(4번) / 문제별 응답 시간(5번) / 게임별 점수(7번)
             ★ 근거: **한 게임 안에서 의미가 닫히는 값**입니다. 이 화면에서 읽을 수 있습니다.
  안 넣은 것  ★ 플레이한 문제 수(1) / 정답률(3) / 카테고리별 성적(6) /
             게임 참가 기록(8) / 문제 경험 기록(9)
             ★ 근거 (1) **여러 게임에 걸친 값**입니다. 한 판의 결과 화면에서 읽을 수 있는 정보가 아닙니다.
                    (2) ★★ 전부 넣으면 **"스크롤 없이 한 화면" 목표와 정면으로 충돌**합니다.
             → ★ 개인 통계 화면의 몫으로 09-BACKLOG 에 두었습니다. **원천 데이터는 전부 쌓이고 있습니다.**

★ 문제별 기록은 **고정 높이 + 내부 스크롤**입니다.
  ★ 근거: 문제 수가 최대 200입니다. 문서를 늘리면 한 화면이 원천적으로 불가능해집니다.

──────────────────────────────────────────
2-2. 다시 하기 / 로비로 (B-2)
──────────────────────────────────────────

★ 서버 동작은 R014 에 이미 있었습니다. 이번에는 **그대로인지 실측으로 단정**했습니다.

  · ★★★ **자동으로 시작되지 않습니다** — 봇이 3초를 관측해 `LOBBY` 유지를 단정합니다
  · ★ 설정값만 복원됩니다 (문제 수 3 유지 / 설정 잠금 해제 / 결과 데이터 삭제)
  · ★ 경험 기록이 다음 게임의 문제 선정에 반영됩니다
  · ★ 두 버튼의 서버 동작은 같고 UI 차이만 있습니다 (Q-48)
  · ★ 비방장에게는 "방장이 다음 게임을 준비 중입니다"
  · ★ 결과 화면에서도 채팅이 유지됩니다
  · ★ 단축키 `Alt+A` / `Alt+L` 이 실제로 동작합니다 (R015 배정)

──────────────────────────────────────────
2-3. 재접속과 테스트 (B-3 / B-4)
──────────────────────────────────────────

★ 결과 화면에서 재접속하면 **문제별 기록과 사람별 요약까지** 복구됩니다 (봇 실측).

★ 봇 `result` 31건이 단정하는 것 —
  정상 완주 / 강제 종료 / ★ 동점 공동 순위(`1,1`) / 응답 시간이 0 초과 30초 이내 /
  ★ 평균과 최속의 계산 일치 / ★★ 중단 문제의 `displayAnswer = null` / ★★ 다시 하기 자동 시작 없음


═══════════════════════════════════════════════════════════════════════
3. ★★ Phase 7 일부 — 스크롤 없이 한 화면
═══════════════════════════════════════════════════════════════════════

──────────────────────────────────────────
3-1. 기준 해상도와 근거 (C-1)
──────────────────────────────────────────

★★ **기준: 1280×720 뷰포트** (데스크톱 우선).

  ★ 근거: 노트북 **1280×800** 에서 브라우저 크롬(탭·주소창·북마크바)을 빼면
    내부 높이가 약 720px 남습니다 (★ **추정**입니다. 크롬 구성에 따라 다릅니다).
  ★ 실사용 조합 중 가장 빡빡한 쪽이므로, 여기서 통과하면 1440×900·1920×1080 은 자동으로 만족합니다.
  ★ 모바일(320px)은 이번 목표가 아닙니다 (건우 우선순위: "웹 UI 가 우선").
    ★ 다만 **깨지지는 않습니다** — 320px 에서도 한 열로 정상 동작하고 1.47배 → 1.17배로 줄었습니다.

──────────────────────────────────────────
3-2. 접근 방법 (C-2) — 세 가지를 함께 썼습니다
──────────────────────────────────────────

★★ **접는 것만으로는 부족했습니다.** 접을 수 있는 것을 다 접어도 1.11배에서 멈췄습니다.

  (1) ★★★ **넓은 화면에서 2열 배치** (1000px 이상) — 세로를 가장 크게 줄입니다 (D-075)
        ★ 왼쪽 = 문제·정답·스킵·방장·점수   /   오른쪽 = 채팅·입력창·단축키
        ★ 근거: 이 게임의 화면은 성격이 둘로 갈립니다 — **보는 것**과 **하는 것**.
          좌우로 놓는 것이 억지 배치가 아니라 자연스러운 분할입니다.
        ★ 로비에서는 왼쪽 열을 다시 2열로 나눕니다(카드가 5개). 게임 중에는 나누지 않습니다 —
          ★ 문제 지문이 좁아지면 안 됩니다.
  (2) ★ **밀도 조정과 접기** — 카드 여백 축소 / 게임 중 라이선스 카드를 한 줄로 / 하단 완충 여백 축소
        ★ 라이선스: 고지 의무는 로비·결과 화면의 카드로 충족되고, 게임 중에도 한 줄 고지는 남습니다.
  (3) ★ **고정 높이 + 내부 스크롤** — 채팅 로그 / 문제별 기록 / 점수판(10명까지 늘어납니다)

★★ 훼손하지 않은 것 (C-3 지시)
  · ★ 문제 지문 / 남은 시간 / 채팅 입력창 — **접지 않았습니다.** 좌표로 화면 안에 있음을 검사합니다
  · ★ 토스트는 그대로 화면 아래 고정이고, 지문·타이머·입력창을 가리지 않습니다 (D-032)
  · ★ 도배 안내는 입력창 바로 위 그대로입니다
  · ★ D-062(새 문제 시작 시 스크롤)는 **남겨 두었습니다.**
      ★ 판단 근거: 한 화면이 된 것은 **1280×720 이상**에서입니다. 좁은 화면에는 여전히 스크롤이 있고,
        그 장치는 거기서 필요합니다. ★ 지우면 좁은 화면에서 R014 결함이 되살아납니다.
  · ★ D-022 라벨 규칙 유지 (ui-check 레이아웃 검사가 그대로 통과합니다)

──────────────────────────────────────────
3-3. ★★ 실측 결과 (C-4)
──────────────────────────────────────────

  화면                     R015      ★ R016     게이트
  ─────────────────────────────────────────────────────────
  게임 화면 (1280×720)     1.66배    ★★ 1.00배   ★ 예
  결과 화면 (1280×720)     —         ★★ 1.00배   ★ 예
  로비 화면 (1280×720)     2.54배    ★ 1.55배    아니오 (참고값)
  게임 화면 (320px)        1.47배    1.17배      아니오 (이번 목표 아님)

★★ **게이트로 만들었습니다** (지시대로).
  ★ 근거: 콘솔에 수치만 남기면 회귀를 막지 못합니다. 화면 요소는 Phase 마다 늘어납니다.
  ★ 그리고 "문서가 짧다" 와 "중요한 것이 보인다" 는 다른 말이라,
    지문·타이머·입력창이 **뷰포트 안에 완전히** 들어오는지 좌표로 따로 봅니다.

★★ **로비는 달성하지 못했습니다 (1.55배). 억지로 맞추지 않았습니다** (D-076).

  걸림돌 둘
    · ★ 남은 높이의 상당 부분이 **하단 완충 여백(160px)** 입니다.
      ★ 로비에는 아직 스크롤이 있어, 스크롤 끝에서 **고정 토스트가 푸터를 덮지 않으려면** 그 여백이 필요합니다 (D-032).
      ★★ 실제로 72px 로 줄였다가 **ui-check 의 겹침 검사가 잡아냈습니다** (toast.top=770 / 푸터.top=787).
    · ★ 나머지를 줄이려면 **안내 문구를 지워야 합니다.** 초대 링크 안내 / 경험률 설명 / 접속 종료 표시 규칙 —
      ★★ 전부 R008·R009 에서 "정보가 도달하지 않아" 생긴 오해를 막으려고 **일부러 넣은 문장**입니다.
      ★ 건우 지시에도 "억지로 맞추려고 정보를 지우지 마라" 가 있었습니다.

  ★ 다음에 손댈 순서 (전부 정보를 지우지 않고 얻을 수 있습니다)
    1. 라이선스 카드를 한 줄로 (게임 중에는 이미 그렇게 합니다)
    2. 참가자 카드의 안내를 접기(토글)
    3. 설정 카드를 2열로


═══════════════════════════════════════════════════════════════════════
4. 확인 대기 문서 정리 (D-1)
═══════════════════════════════════════════════════════════════════════

★ `docs/13-PENDING-CHECKS.md` 를 신설했습니다. **이 문서 하나만 보시면 됩니다.**

  0장  ★ 왜 사람이 봐야 하는가 — 자동 검사가 못 하는 세 가지
         (사람의 판단 / 실기기 동작 / 외부 환경)
  1장  ★ 준비 절차 — **맨 앞에 한 번만** 적었습니다 (창 두 개 / 문제 수 3 / 창 최대화)
  2장  ★★★ 반드시 (A급) **9개**
  3장  ★★ 여유 있으면 (B급) **10개**
  4장  ★ 끝난 것 — 9건 (건우 실측 6건 + 자동화로 내려간 것)
  5장  ★ 지금 없는 것 — 확인하다 놀라지 않도록 (D-030)

★ 합친 방식
  · R014 18항목 + R015 19항목 + R016 신규를 한 곳에 모았습니다
  · ★ 중복을 정리했습니다 — 예: R015 의 일시정지 재현 절차가 R014 의 "게임 중 새로고침" 을 포함합니다
  · ★ 건우가 이미 확인한 6건(IME / 힌트 / 확인창 / 320px / 새로고침 / 도배)은 **4장으로 내렸습니다**
  · ★ 자동화된 것도 내렸습니다 — 결과 화면·마스킹 규칙·한 화면 수치는 이제 게이트가 지킵니다
  · ★ **각 항목에 "왜 자동으로 확인할 수 없는지" 를 적었습니다** (D-028 지시)

★ A급에서 특히 중요한 것
  · ★★★ A6/A7 — 마스킹을 **두 화면에서 동시에** 봐야 합니다. 서로 다르게 보이는 것이 정상입니다
  · ★★★ A2~A4 — 일시정지. 자동 재개가 없다는 것을 30초 기다려 확인하는 항목입니다
  · ★★ A8 — **"조잡한가" 는 건우만 판단할 수 있습니다.** 수치(1.00배)는 자동으로 지킵니다


═══════════════════════════════════════════════════════════════════════
5. 남은 정리 작업 (D-3)
═══════════════════════════════════════════════════════════════════════

★ `grep -r TEMP-` — **코드에서 0건**입니다 (client/server/shared/pipeline/scripts).
  ★ 문서에는 "옛 표식" 으로만 남아 있습니다. 05-STATUS 의 임시 코드 표를 이력으로 정리했습니다.

★ 08-OPEN-QUESTIONS 훑기 — ★ 확정된 것이 미결 목록에 남아 있지 않았습니다.
  · ★ Q-35 / Q-42 / Q-43 / Q-25 / Q-31 / Q-32 / Q-48 은 **규칙이 이미 확정**되어 있었고
    이번에 **구현**이 끝났습니다. 그 사실을 해소 항목에 적었습니다.
  · ★ Q-58(검수 관리 페이지)의 판단 시점이 "Phase 4 완료 후" 였는데 **그 시점이 됐습니다.**
    ★ 다만 지금은 문제가 306건이라 JSON 직접 편집으로 감당됩니다.
      대량 생성을 시작하는 시점에 다시 꺼내는 것이 자연스럽습니다. **건우 판단 사항입니다.**

★ 05-STATUS 훑기 — ★ "미구현" 인데 실제로는 되는 것이 **두 개** 있었습니다.
  · ★ Phase 4 표의 힌트 / 스킵 / 강제 스킵·종료 — **Phase 3 에서 이미 동작**하고 있었습니다.
    ★ 표에 "(Phase 3)" 로 근거와 함께 표시했습니다.
  · ★ Phase 6 표의 경험 기록 / 판정 제외 / 경험 기반 선정 — 같은 이유로 이미 동작 중이었습니다.
  ★★ R008 의 "내보내기" 와 같은 종류의 문서 결함입니다. 이번에 정리했습니다.


═══════════════════════════════════════════════════════════════════════
6. Gemini 게이트 상태 (D-4)
═══════════════════════════════════════════════════════════════════════

★ 호출 1회를 썼습니다 (`node scripts/verify-gemini.mjs --minimal`).

  시각      **2026-09-15 05:54 UTC**
  결과      ★ **200 OK** — 모델 목록 정상 / `gemini-3.6-flash` generateContent 성공
  상태 파일 `data/pipeline/state/daily.json` 의 day 가 **2026-09-10** 입니다 (그날의 기록이 남아 있습니다)
            ★ 그날 기록: 호출 1회 / 429 두 번 / 재개 1회 소진 (Q-62 규칙대로 그날 중단)

★★ **이 관측은 D-034 의 리셋 시각 가설(UTC 07:00~08:00)을 가르지 못합니다.**
  ★ 근거: 마지막 429 로부터 **5일**이 지났습니다. 05:54 UTC 에 성공한 것은
    "그날의 한도가 그 시각에 리셋됐다" 가 아니라 "며칠 전 일이라 이미 리셋됐다" 로도 설명됩니다.
  ★ 가설을 가르려면 **429 를 맞은 당일에** 시각을 옮겨 가며 재시도해야 합니다 (★ 확인 필요).

★ 지금 말할 수 있는 것 — **키는 유효하고 게이트는 열려 있습니다.** 대량 작업을 시작할 수 있습니다.


═══════════════════════════════════════════════════════════════════════
7. 테스트 / 문서 / 커밋
═══════════════════════════════════════════════════════════════════════

──────────────────────────────────────────
7-1. 검증
──────────────────────────────────────────

  npm run verify           ★ 통과
    typecheck              통과
    vitest                 ★ **330건** (314 + skip 13 → 330, ★ **skip 0**)
    smoke                  10 / 10
    ui-check               ★ **107 / 107** (101 → 107. ★★ 한 화면 게이트 포함)

  봇 시나리오 22종 — ★ 전부 통과
    신설  mask 23 / result 31
    회귀  game 58 / countdown 45 / lobby 37 / pause 28 / concur 25 / pausehint 21 /
          collide 20 / full 15 / flood 10 / epoch 10 / abandon 9 / empty 8 / pausehost 8 /
          race 7 / join / duplicate / reconnect / host / chat

──────────────────────────────────────────
7-2. 문서 (D-2)
──────────────────────────────────────────

  00-OVERVIEW       ★ 읽는 순서에 13-PENDING-CHECKS 추가
  01-GAME-RULES     ★ 마스킹과 판정의 분리 / 결과 화면 항목 / ★★ 중단 문제 정답 비공개 /
                    ★ 한 화면 규칙과 "접어도 되는 것 / 안 되는 것"
  04-PROTOCOL       ★ experiencedPlayers / game.result 페이로드 / ★ Phase 6·4 구현과 명세의 대조
  05-STATUS         ★ Phase 4·6 완료, Phase 7 부분. ★ grep -r TEMP- 0건 기록
  06-WORKLOG        R016 항목
  07-DECISIONS      ★★ D-073 ~ D-076
  08-OPEN-QUESTIONS ★ 구현 완료 기록 / Q-58 시점 도래
  09-BACKLOG        ★ 한 화면 진행 상황(달성/남음) + 결과 화면 요소 표
  10-TESTING        ★ 15장 신설 (마스킹 16케이스가 어디서 검증되는지 포함)
  ★ 13-PENDING-CHECKS  **신설**

  새 결정 기록
    D-073  ★★★ 마스킹은 출력 변환이고 정답 판정은 입력 처리다 — 코드 구조로 분리
    D-074  ★★ 중단된 문제의 정답은 결과 화면에서도 공개하지 않는다
    D-075  ★★ 넓은 화면에서 2열로 배치한다 (기준 해상도 1280×720)
    D-076  ★ 로비는 한 화면 목표에서 이번에 제외한다 (근거 공개)

──────────────────────────────────────────
7-3. 커밋과 태그
──────────────────────────────────────────

  eb0f042  feat: Phase 6 — 정답 마스킹 + 경험자 표시 (Q-35 / Q-42 / Q-43)
  be2bdbf  feat: Phase 4 — 결과 화면 본체 (문제별 정답자 / 응답 시간 / 사람별 요약)
  1c85ceb  feat(client): 스크롤 없이 한 화면 — 게임·결과 화면 1.00배 달성
  05ad804  test: Phase 6·4 봇 시나리오 + 한 화면 게이트 (ui-check 107항목)
  ba70cc2  docs: R016 — Phase 6·4·7 일부 반영 / 13-PENDING-CHECKS 신설 / D-073~D-076

  태그  ★ `v0.6-phase4` (결과 화면 커밋 지점) / ★ `v0.7-phase6` (라운드 끝)
        ★ 판단: **둘로 나눴습니다.** 같은 라운드지만 서로 다른 Phase 이고,
          나중에 "결과 화면이 언제 들어왔나" 를 찾을 때 지점이 분명해야 합니다.

★ 비밀값 검사 (저장소가 Public 이므로 매번 합니다)
  · `git status` / `git diff --numstat` 에 `.env` · 키 파일 없음
  · 변경분 전체를 `AIza… / sk-… / ghp_… / -----BEGIN / trycloudflare / ngrok` 패턴으로 검사 — **0건**
  · ★ Gemini 호출 스크립트는 키를 **헤더로만** 보내고 앞 4자리도 출력하지 않습니다 (기존 규칙)


═══════════════════════════════════════════════════════════════════════
8. 새로 발생한 결정 필요 항목
═══════════════════════════════════════════════════════════════════════

★ **새로 올릴 Q 는 없습니다.** 이번 라운드의 판단은 전부 기술적이고 되돌리기 쉬운 것이었고,
  근거와 함께 D-073~D-076 에 기록했습니다.

★ 다만 **건우가 판단해 주셔야 이어갈 수 있는 것**이 세 가지 있습니다. (Q 로 올리지 않고 여기 적습니다)

  (1) ★★ **로비의 한 화면화를 다음 라운드에 잡을 것인가** (지금 1.55배)
      ★ 판단이 필요한 이유: 완전히 맞추려면 안내 문구를 줄여야 할 수도 있습니다.
        ★ 그 문장들은 오해를 막으려고 일부러 넣은 것이라, 지우는 것은 기획 판단입니다.
      ★ 13-PENDING-CHECKS B6 에서 "얼마나 거슬리는지" 를 봐 주시면 그것이 답이 됩니다.

  (2) ★ **모바일을 언제 잡을 것인가** — 지금은 깨지지 않는 수준(1.17배)입니다.
      ★ 실기기 확인 항목이 R014 부터 쌓이고 있습니다 (13-PENDING-CHECKS B7/B8/B9).

  (3) ★ **Q-58 검수 관리 페이지** — "Phase 4 완료 후" 라는 판단 시점이 됐습니다.
      ★ 제 의견: 지금은 필요 없습니다(306건). 대량 생성을 시작할 때가 자연스럽습니다.


═══════════════════════════════════════════════════════════════════════
9. 자체 판단 항목 / 다음 라운드 제안
═══════════════════════════════════════════════════════════════════════

──────────────────────────────────────────
9-1. 자체 판단한 것 (기술적이고 되돌리기 쉬운 것만)
──────────────────────────────────────────

  1. ★★★ 마스킹과 판정의 코드 분리 방식 (D-073) — 순서를 규칙으로 못 박고 주석에 적었습니다
  2. ★ `mapEnd` 추가 (D-073 구현 중 발견) — 매칭 끝을 다음 문자의 시작으로 잡으면 공백을 먹습니다
  3. ★ `experiencedNicknames` → `experiencedPlayers` (색 포함) — 닉네임 역추적은 동명이인에서 엉킵니다
  4. ★★ 중단 문제의 정답 비공개 (D-074) — 규칙의 일관성 문제라 판단했습니다
  5. ★ 결과 화면에 넣을 통계 선별 — "한 게임 안에서 의미가 닫히는 것만" 이 기준입니다
  6. ★★ 2열 배치와 기준 해상도 (D-075) — 접기만으로는 1.11배에서 멈췄습니다
  7. ★ 로비를 이번 목표에서 제외 (D-076) — 근거를 전부 적었습니다
  8. ★ 한 화면 검사를 **게이트**로 승격 — 콘솔 기록만으로는 회귀를 막지 못합니다
  9. ★ 태그를 둘로 나눔 (v0.6 / v0.7)
 10. ★ 봇 `mask` 시나리오가 무작위에 의존하지 않도록 조건 고정 (정답 3글자 이상)

──────────────────────────────────────────
9-2. 이번 라운드에 얻은 교훈
──────────────────────────────────────────

★★ **Phase 0 에서 미리 써 둔 테스트가 두 번 저를 잡았습니다.**
  ★ 매칭 끝 인덱스(#3)와 짧은 정답 처리(#12)입니다.
  ★ 구현하면서 "되는 것 같다" 로 넘어갔다면 둘 다 조용히 틀린 채로 들어갔을 것입니다.
  ★ **구현보다 먼저 쓴 테스트**의 값어치가 이번에 실제로 드러났습니다.

★ **패치 스크립트는 파일로 씁니다.** 히어독으로 넘기면 `\n` 이 진짜 줄바꿈이 되어 파일이 깨집니다.
  ★ 이전 라운드에도 같은 교훈이 있었는데 이번에 또 밟았습니다. 이제 예외 없이 파일로 씁니다.

──────────────────────────────────────────
9-3. 다음 라운드 제안 (★ 순서는 건우가 정해 주십시오)
──────────────────────────────────────────

  (가) ★★ **브라우저 확인 한 번** — 13-PENDING-CHECKS 의 A급 9개.
       ★ 20~30분이면 끝납니다. ★ 마스킹은 두 화면을 동시에 봐야 해서 저로서는 확인할 수 없습니다.
       ★★ Phase 3~6 이 전부 실물로 도는 것을 처음 보시게 됩니다.

  (나) ★★ **Track D 대량 생성** — Gemini 게이트가 열려 있고, Q-81 구조(Sonnet 역검증)도 준비돼 있습니다.
       ★ 지금 게임은 306문제로 돌아갑니다. ★ 200문제짜리 게임을 하면 거의 다 소진됩니다.
       ★ 게임 쪽이 Phase 6 까지 끝났으므로 **데이터가 다음 병목입니다.**

  (다) ★ **Phase 7 마무리** — 로비 한 화면 / 모바일 / 색약 검증 / 채팅 UX.

★ 제 의견: **(가)를 먼저** 하시고, 그 결과를 보고 (나)/(다) 순서를 정하는 것이 낫습니다.
  ★ 근거: 지금 쌓인 확인 항목이 세 라운드분입니다. ★ 결함이 있다면 **더 쌓이기 전에** 아는 것이 좋습니다.

===========================================
