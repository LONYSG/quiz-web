===========================================
ROUND: 020
DATE: 2026-09-21 14:50
TASK: 695건 적재 + 표기 변형 정리 + 규모 확장 2차
STATUS: 부분완료
===========================================

★ STATUS 를 "부분완료" 로 적은 이유는 하나다.
  작업 A·B·C 는 모두 끝났으나, C-3 의 **중복 판정 6쌍이 Gemini 429 로 판정되지 못했다.**
  ★ 규칙대로 재시도하지 않았다. C22 로 올렸다.
  ★★ R020 결과는 지시대로 **적재하지 않았으므로** 이 미판정이 지금 서비스에 영향을 주지는 않는다.


===========================================
0. 한눈에
===========================================

| 작업 | 결과 |
|------|------|
| A. R019 적재 | ★★ 완료. 683건 적재 — 활성 문제 **693 → 1,374건** / 소분류 247 → **261개** |
| B. 표기 변형 정리 | ★★ 완료. 359개 변형 제거 — `foreignVariant` **260 → 0** / `expose` **6 → 0** |
| C. 규모 확장 2차 | ★★ 완료(판정 6쌍 제외). 60개 소분류 / 소재 685 → **생존 656건** |

```
손실률 곡선   0.5%  →  3.1%  →  3.5%  →  4.2%
             R017     R018     R019     R020
대조 대상      306      505      693    1,374    ★ 두 배가 됐는데 손실률은 0.7%p 올랐다

알 가치 1~2    0건      0건     12건      0건    ★★★ 하한을 없앤 효과다
알 가치 평균  3.93     3.76     4.19     4.66
```

★★ **이번 라운드에서 가장 중요한 수치는 손실률이 아니라 "알 가치 1~2 = 0건" 이다.**
건우가 정한 **상한 12 / 하한 없음**이 R019 의 유일한 품질 결함을 없앴다.


===========================================
1. 작업 A — R019 695건 적재
===========================================

```
R019 생존 695건
  − 알 가치 1~2 인 12건 (C16 지시대로 제외)
  = 683건 적재

활성 문제   693 → 1,374건   (+687 = 683 적재 + 4 교체 투입, −2 노출 내림, −4 교체로 밀려남)
채운 소분류 247 → 261개     (297개 중 87.9%)
무결성 결함 0
```

★ 뺀 12건은 **지우지 않고 격리**했다. 사유가 남아야 나중에 판정 품질을 평가할 수 있다.

★ 3,000건 목표 기준 **45.8%** 에 왔다.


===========================================
2. 작업 B — 표기 변형 정리
===========================================

### 2-1. B-1 — ★★ "표본을 직접 읽어라" 는 지시가 사고를 막았다

지시대로 지우기 전에 **제거 대상 목록을 먼저 뽑고 4글자 이하 표본을 손으로 읽었다.**
★★ 그 과정에서 **내 분류기가 틀린 것을 4건 찾았다.**

```
모델 T        ← 포드 모델 T          한국어 변형인데 라틴 문자 'T' 하나 때문에 "영문 원어" 로 잡혔다
모델 T        ← T형 포드              같은 이유
몽키 D. 루피  ← 몽키 D 루피           같은 이유
하임리히법    ← 하임리히 manoeuvre    한국어가 섞여 있다
```

→ `classifyVariant()` 에 **"한글이 들어 있으면 한국어 변형"** 이라는 조건을 넣었다.

★ 약어도 같은 위험이 있었다. 첫 판은 `DDoS` 와 `LoL` 까지 지울 뻔했다.
  ★★ 둘 다 **사람이 실제로 치는 표기**다. R019 에서 억울함 방지 규칙으로 일부러 넣은 것도 있었다.
  → 대문자 비율과 길이로 약어를 걸러 내도록 고쳤다.

★ 만약 목록을 안 뽑고 220건을 한 번에 지웠다면, **고쳐야 할 것이 아니라 멀쩡한 것 4~6개를 지웠다.**

### 2-2. B-1 결과

```
제거한 변형   359개 (문제 220건)
백업          data/pipeline/audit/r020-removed-variants.json   ★ 359행 전부
감사 foreignVariant   260 → 0
```

### 2-3. B-2 — ★★★ 지시의 전제가 사실과 달랐다

브리핑은 이렇게 적혀 있었다.

> "변형 때문인 4건(db:49·92·203·228)은 **B-1 로 변형이 빠지면 자동으로 해소된다.** 확인만 하라"

★★ **확인해 보니 그렇지 않았다.** 문제를 일으킨 변형 4개가 **전부 한국어**였다.

```
db:49   음절      ← 글자     한국어
db:92   에도 막부 ← 에도     한국어
db:203  남원시    ← 남원     한국어
db:228  윷놀이    ← 윷       한국어
```

★ 그런데 같은 브리핑의 B-1 은 **"★★ 한국어 변형은 건드리지 마라"** 고 못박고 있다.
★ B-1 을 돌린 뒤 감사를 다시 돌려 **4건이 모두 그대로 남아 있는 것**을 실측으로 확인했다.

**내가 한 것** — 지시를 그대로 따르지도, 마음대로 바꾸지도 않았다.
프롬프트가 금지하는 **다른 조항**을 근거로 떼었다.

```
프롬프트 B:  "정답보다 넓은 상위 개념을 넣지 않는다"
             "불필요하게 축약한 형태를 넣지 않는다"   ← 빈센트 반 고흐 → 고흐 와 같은 갈래
```

`에도`(막부가 아니라 지명) / `남원`(시가 아니라 지명) / `윷`(놀이가 아니라 도구) / `글자`(음절보다 넓다)
— 네 개 모두 이 조항에 걸린다. 근거는 `data/pipeline/audit/r020-removed-korean-variants.json` 에 남겼다.

★ 대표 정답이 그대로 드러난 2건은 지시대로 **`is_active` 를 내렸다.** 지우지 않았고 사유를 남겼다.

```
db:33   무궁화   "무궁화의 학명에 쓰이는 우리나라의 국화 이름은?"
db:121  5        "정규 5인제 농구 경기에서 … 한 팀의 선수 수는 몇 명인가?"
```

### 2-4. B-2 결과 — 감사 최종

```
scanned 1,390건 (활성 1,374)

expose                1     ★★ 활성 문제 기준으로는 0 이다
                              db:33 은 이미 is_active 를 내려 둔 것이다 (C15 처리)
exposeSuspect         4     ★ 전부 오탐이거나 이미 내린 것이다
                              db:121 → 내려 둔 것
                              db:93(금/Au) · db:374(은/Ag) · db:632(은/올림픽 메달)
                                → 정답이 한 글자라 다른 낱말 안에 우연히 들어갔다
foreignVariant        0     ★★ 260 → 0
hintEqualsAnswer      0
noAnswerRow           0
primaryMissingInRows  0

manyVariants         26     ★ 변형 3개 이상. 110 → 26 으로 줄었다 (gemini-gen 22)
timeDependentSuspect 18     ★ 시간이 지나면 답이 바뀔 수 있는 것. 이번에 손대지 않았다
hintNull             35     ★ 정답이 짧아 힌트를 만들 수 없는 것. 정상 범위다
longAnswer            6
```

### 2-5. B-3 — 교체 9건을 **하나씩 직접 읽었다**

지시가 *"판정이 틀렸을 수 있다. 아니라고 판단되면 그 건은 그대로 두고 근거를 적어라"* 였다.

★ **먼저, 9건이 아니라 8건이었다.**
`football/1#011`(골든골)은 같은 대조인데 판정이 엇갈렸다 — `xr3 keep=a` / `db14 keep=b`.
★ 한 판정이 같은 비교에서 반대 답을 낸 것이므로 **근거로 쓸 수 없다고 보고 뺐다.**

★ 읽어 본 8건 중 **4건에 동의**해 바꿔 끼웠다.

| 신규 | 기존 | 왜 바꿨나 |
|------|------|----------|
| `animals/1#005` 기린 | db:36 | ★★ 기존은 "목이 가장 긴 동물은?" 이다. **맞혀도 남는 것이 없다.** 신규는 "목뼈가 사람과 똑같이 일곱 개인 동물은?" — 하나를 알려 준다 |
| `world-geo/1#011` 러시아 | db:284 | 기존은 "국토가 가장 넓은 나라는?" 이다. 신규는 시간대가 열한 개라는 사실을 함께 준다 |
| `world-history-west/4#007` 삼권분립 | db:139 | 기존은 정의를 그대로 읊는 문장이다. 신규는 몽테스키외와 그 목적을 함께 준다 |
| `music-theory/3#002` 팀파니 | db:237 | 기존은 악기 설명을 나열한다. 신규는 "음정을 조율할 수 있는 유일한 북" 이라는 한 가지를 묻는다 |

★ 나머지 4건은 **그대로 두었다.** 근거를 적는다.

| 신규 | 기존 | 왜 안 바꿨나 |
|------|------|-------------|
| `football/1#007` VAR | db:410 | ★ 기존이 더 낫다. 신규는 약어의 뜻을 묻는데, 기존은 그 제도가 무엇을 하는지를 묻는다 |
| `animation/1#007` 드래곤볼 | db:145 | ★ 둘의 알 가치 차이가 없다. 이유 없이 DB 를 바꿀 필요가 없다 |
| `calendar/3#007` 동지 | db:112 | ★ 기존이 "밤이 가장 긴 날" 을 정확히 묻는다. 신규는 팥죽 풍습 쪽이라 절기 소분류에는 기존이 맞다 |
| `classical/3#012` 인터미션 | db:683 | ★ 신규가 조금 간결할 뿐이다. 판정의 `keepReason` 도 "간결하다" 뿐이었다 |

★★ **판정이 "간결하다" 만을 근거로 든 건은 바꾸지 않았다.**
간결함은 알 가치가 아니다. 바꾸는 쪽도 DB 쓰기다.

★ 되돌릴 수 있게 `scripts/seeds-restore.mjs` 를 새로 만들었다 (`seeds-discard.mjs` 의 역).
★ 기록: `data/pipeline/audit/r020-swapped.json` — 밀려난 DB 행의 원본까지 담았다.

### 2-6. B-4 — ★★ 규칙을 프롬프트에서 코드로 옮겼다 (D-094)

★ 220건이 나온 진짜 원인은 **모델이 규칙을 어긴 것이 아니다.**

```
프롬프트:      "외국어 원어 표기와 한자 표기를 복수 정답에 넣지 마라"
적재 스크립트:  그 규칙을 확인하지 않는다                      ← 여기가 비어 있었다
```

→ `pipeline/lib/answer-rules.mjs` 한 곳에 규칙을 두고 **감사와 적재가 같은 구현을 쓰게** 했다.

```
classifyVariant()       한자·가나·영문 원어 → drop
                        ★ 한국어 변형과 약어(DDoS·LoL·VAR·H2O) → ok
findAnswerInQuestion()  낱말 경계 기준 노출 검사

적재 동작   노출이면      → 그 문항을 건너뛴다
            금지 변형이면 → 그 변형만 떼고 적재한다
            --no-gate     → 끌 수 있다. ★ 다만 껐다는 사실이 로그에 남는다
```

★ 유닛 20케이스로 확인했다(`scripts/answer-rules-test.mjs`). ★★ 그중 4케이스가 위 2-1 에서 찾은 내 오분류다.

### ★★ 2-7. 검사를 파일로 남기다가 구멍을 하나 더 찾았다

유닛 검사를 파일로 남기려고 기대값을 적다가 **두 건이 어긋났다.**
★ 코드가 아니라 **내 기대가 틀린 줄 알았는데, 재 보니 코드 쪽에 구멍이 있었다.**

```
"무궁화의 학명에 쓰이는 …"  → 정답 무궁화   기대 word / 실제 substring
"… 에도를 본거지로 …"       → 정답 에도     기대 word / 실제 substring
```

★★ 원인은 **한국어 조사**다. `무궁화` 뒤가 `의` 라서 낱말 경계로 보이지 않았다.
  → 사람이 보면 명백한 노출인데 **경고(`exposeSuspect`)로 내려가 그친다.**
  ★ `db:33`·`db:228` 을 손으로 판단해야 했던 이유가 바로 이것이었다.

**고치기 전에 영향을 먼저 쟀다.** 조사 23개를 경계로 인정하고 다시 돌렸다.

```
R020 파일 656건   새로 잡히는 것 0건
DB      1,390건   새로 잡히는 것 1건 — db:33 (이미 내려 둔 것)
★★ 오탐 0건
```

★ `금속은` 의 `속` 처럼 **조사 뒤가 다시 경계가 아니면 걸리지 않는다.** 그래서 오탐이 없다.
→ ★ 넣었다. 감사 `expose` 가 0 → 1 로 보이는 것은 **검출력이 올라간 결과**이고,
  그 1건은 이미 내려 둔 `db:33` 이다. **활성 문제 기준 노출은 여전히 0 이다.**


===========================================
3. 작업 C — 규모 확장 2차 (60개 소분류)
===========================================

### 3-1. 결과

```
소분류 60 / 소재 685
  → 문제 677 성공 / 거절 8
  → 격리 21 (라운드 안 9 + 중복 판정 12)
  → ★ 생존 656건

손실률 4.2%
```

★ 대분류 분포 — 7개 대분류를 골고루 채웠다.

```
한국 9 · 인문·사회 11 · 자연과학 11 · 기술·의학 7 · 문화·예술 11 · 스포츠·게임 6 · 생활·상식 5
```

### 3-2. ★★★ 상한 12 / 하한 없음 — 실제로 작동했다 (D-096)

| | R019 (12 강요) | **R020 (하한 없음)** |
|---|---|---|
| 소분류 | 60 | 60 |
| 소재 | 720 | **685** |
| 소분류당 평균 | 12.0 | **11.4** |
| 12 전에 멈춘 소분류 | 0 | **21개** |
| ★★ 알 가치 1~2 | **12건** | ★★ **0건** |
| 알 가치 평균 | 4.19 | **4.66** |
| 난이도 평균 | 2.64 | 2.91 |

★★ **소재를 35개 덜 만들고 바닥 문항을 12건에서 0건으로 없앴다.**

★ 멈춘 사유는 소분류마다 `shortfallReason` 에 적었다. 성격이 뚜렷하게 셋으로 갈린다.

```
(1) 시간이 지나면 답이 바뀐다
    kr-geo/1(9)     "남은 후보가 도청 소재지·시 승격 기준·인구 순위다. 인구 순위는 시간이 지나면 바뀐다"
    energy/2(11)    "어느 나라의 발전 비중 같은 수치인데 해마다 바뀐다"
    football/3(10)  "구단 우승 횟수·선수 기록인데 중분류 경계가 금지하고 답도 바뀐다"
    society/1(10)   "입학사정관제·고교학점제 — 제도가 자주 바뀐다"

(2) 이름만 묻고 끝난다
    kr-food/3(10)     "핫도그·튀김·계란빵·회오리감자 — 무엇인지 묻고 답하면 끝이다"
    plants/1(11)      "주목·느티나무·회화나무 — 어떤 나무인가를 묻고 끝난다"
    war-history/2(11) "마른·갈리폴리 — 전투 이름만 묻게 된다"
    invention/1(11)   "수력방적기·뮬 방적기 — 기계 이름만 묻게 된다"

(3) 아는 사람만 안다
    kr-music/2(9)       "특정 가수의 특정 곡. R019 의 마음의 소리·무빙과 같은 성격이다"
    theatre/2(10)       "개별 작품의 수록곡이나 배역"
    popular-music/2(10) "특정 앨범의 수록곡이나 차트 기록"
    comics/2(11)        "개별 히어로의 설정 세부"
    myth/3(10)          "세크메트·소베크·닌후르사그 — 대상 자체가 마이너해진다"
```

★★ (3) 이 D-093 이 경계한 **"어렵게 만들라고 하면 모델이 마이너한 대상으로 간다"** 바로 그것이다.
  ★ 하한이 없으니 그쪽으로 가는 대신 **멈출 수 있었다.**

### 3-3. ★ 난이도를 올리지 않았다 (D-093)

```
난이도 평균    R019 2.64  →  R020 2.91
난이도 4 이상  R019 15.8% →  R020 21.2%
```

★★ **이것은 난이도를 올린 결과가 아니다.** 지시대로 1회차 기준을 그대로 썼다.
★ 올라간 만큼은 **알 가치 1~2 인 바닥 문항 12건이 빠진 자리**다.
  바닥 문항은 거의 전부 난이도 1~2 였다.

### 3-4. 규칙 검사 — ★ 내가 만든 결함 11건을 잡아 고쳤다

```
1차   expose 1 / exposeSuspect 11 / normCollisionInside 7
2차   expose 0 / exposeSuspect  1 / normCollisionInside 0
```

★★ `world-language/2`(라틴어·그리스어 어근) 6건이 한 원인이었다.
**어근의 예시를 한글 음차로 들면 정답이 그대로 드러난다.**

```
(고치기 전) 텔레비전·텔레폰·텔레스코프에 공통으로 들어 있으며 '멀리 떨어진' 을 뜻하는 … 어근은?  → 텔레
            ^^^^                                                                           ★ 드러난다
(고친 뒤)   '멀리 떨어진' 을 뜻해, 먼 곳의 영상을 받아 보는 장치나 먼 곳의 사람과
            말하는 장치의 영어 이름 앞머리에 공통으로 붙는 그리스어 어근은?                → 텔레
```

★ 나머지 5건(`world-cinema/4#012` 칸 / `fashion/3#009` 저고리 / `physics/2#010` S극 /
`biology/2#001` 계)도 **질문만 고쳤다. 정답은 바꾸지 않았다.**

★ 남은 `exposeSuspect` 1건은 오탐이다 — `combat-athletics/3#010` 의 답 `10` 이 질문의 `100미터` 안에 들어 있다.

### 3-5. 라운드 안 같은 답 9건 (D-090)

```
"유엔"     calendar/1#012  vs  symbols/1#010                          ← 깃발 도안 쪽을 남겼다
"라틴어"   chemistry/2#008 vs classical/2#011 vs world-language/2#010 ← 셋이었다
"바이러스" biology/2#012   vs  medicine/1#002
"탄소"     chemistry/2#005 vs  chemistry/2#012                        ← 같은 소분류 안이다
"감자"     kr-lit/3#002(김동인) vs world-food/2#011(아일랜드 대기근)
"7"        architecture/3#009  vs  board-game/4#007
"4"        baseball/1#010 vs classical/2#010 vs human-body/2#001      ← 셋이었다
```

★★ **R019 는 라운드 안 중복이 0이었는데 R020 은 9건이다.**
60개 소분류를 두 번 돌리자 **서로 다른 대분류가 같은 답에 이르는 일**이 실제로 늘었다.

★ 숫자 2묶음(`7`·`4`)은 **보수적으로 거절**했지만 규칙 해석이 필요하다 → C24.

### 3-6. 3방향 대조

```
신규 안쪽   222,778쌍 → 후보  0쌍     ★ 3-5 에서 이미 다 거절했다
r017 대조   132,932쌍 → 후보  9쌍
r018 대조   125,584쌍 → 후보  6쌍
r019 대조   458,916쌍 → 후보  6쌍
기존 DB     928,520쌍 → 후보 45쌍

판정 66쌍 → same 30 / different 30 / ★ 미판정 6
same 30쌍은 R020 쪽 21건에 해당한다
  → 12건 격리 (기존이 낫다고 본 것 + 판정이 엇갈린 2건)
  →  9건 교체 후보로 보류 (C23. ★ 적재하지 않으므로 이번엔 아무것도 하지 않았다)
```

### 3-7. ★★ 판정 6쌍이 조용히 빠졌다 (D-095)

후보 66쌍 중 **60쌍만 판정된 채** `[요약] same 30 / different 30` 이 찍혔다.
★ 묶음 6개 중 마지막 묶음이 실패했는데 **출력 파일에도 요약에도 아무 표시가 없었다.**
`counts.pairs` 는 66 이라고 적혀 있었고 판정 결과는 60개였다.

★★ 빠진 6쌍 중 3쌍은 정답 문자열이 DB 와 **완전히 같았다.**

```
theatre/2#008 웨스트엔드   ↔  db:679 웨스트엔드
theatre/2#009 토니상       ↔  db:681 토니상
theatre/2#010 커튼콜       ↔  db:674 커튼콜
theatre/2#001 레미제라블   ↔  db:160 레 미제라블
world-cinema/4#001 봉준호  ↔  db:41  봉준호
world-cinema/4#008 박찬욱  ↔  db:256 박찬욱
```

★ 우연히 같은 쌍이 라운드 간 대조에도 걸려 있어서 눈치챘다.
  ★★ **걸려 있지 않았다면 중복이 그대로 통과했다.**

**고친 것** — `scripts/seeds-dupe-judge.mjs` 가 `unjudged` 와 `skippedChunks` 를
파일에 남기고 화면에 오류로 크게 찍도록 했다.

★ 교훈은 "묶음을 버려도 된다" 가 아니라
  **"버린 것을 세어서 보여 주지 않으면 버린 줄 모른다"** 다.

**다시 돌리려 했으나 Gemini 429 가 났다.**
★ 규칙대로 재시도하지 않았다. 1차 판정 60쌍은 살리고, 미판정 6쌍을 명시해 남겼다.

```
data/pipeline/dedupe/r020-judged.json        ★ pendingUnjudged 6건이 들어 있다
data/pipeline/audit/r020-judged-1st.json     1차 판정 원본
data/pipeline/audit/r020-judge-2nd-429.json  429 기록
```

### 3-8. ★ C-5 — 적재하지 않았다

★★ 지시 그대로 **파일로만 쌓았다.** DB 에 아무것도 쓰지 않았다.

```
data/pipeline/generated/r020/   60개 파일
data/pipeline/seeds/*.json      소재와 사용 기록
```


===========================================
4. 손실률 곡선 (Q-94 / C17)
===========================================

| | R017 | R018 | R019 | **R020** |
|---|---|---|---|---|
| 소재 | 200 | 194 | 720 | **685** |
| 거절 | 0 | 1 | 10 | **8** |
| 격리 | 1 | 5 | 15 | **21** |
| **손실률** | 0.5% | 3.1% | 3.5% | **4.2%** |
| 대조 대상(기존 DB) | 306 | 505 | 693 | ★★ **1,374** |

★★ **R019 의 해석이 맞았다.**
대조 대상이 693 → 1,374 로 **두 배**가 됐는데 손실률은 3.5 → 4.2%, **0.7%p** 올랐을 뿐이다.
비례했다면 7% 가 됐어야 한다.

→ ★ 손실률은 대조 대상의 **수**가 아니라
  **"흔한 지식이 이미 얼마나 쓰였는가"** 를 따라간다는 R019 의 진단이 한 번 더 확인됐다.

★ 다만 성격이 바뀌었다.

```
R019 격리 15건   전부 라운드 간·DB 대조에서 나왔다. 라운드 안 0건
R020 격리 21건   라운드 안 9건 + 판정 12건     ★★ 라운드 안이 처음으로 생겼다
```

★★ **소분류를 많이 돌릴수록 "라운드 안 충돌" 이 새로운 손실원이 된다.**
대조 대상이 늘어서가 아니라 **한 번에 다루는 분야가 넓어서** 생긴다.

★★★ **브리핑의 "남은 237개 소분류" 는 사실과 다르다.**
297개 중 이미 소재를 만든 곳이 R017 20 + R018 20 + R019 60 + R020 60 = 159개다(겹침 1).
→ ★ **남은 소분류는 138개다.**


===========================================
5. 품질 분포 (C-4)
===========================================

| 지표 | R017 | R018 | R019 | **R020** |
|------|------|------|------|----------|
| 접근성 평균 | 3.49 | 3.16 | 3.62 | **3.64** |
| 난이도 평균 | 3.04 | 3.24 | 2.64 | **2.91** |
| 난이도 4 이상 | 29.0% | 40.4% | 15.8% | **21.2%** |
| 알 가치 평균 | 3.93 | 3.76 | 4.19 | ★★ **4.66** |
| ★★ 알 가치 1~2 | 0건 | 0건 | **12건** | ★★ **0건** |
| `acceptedAnswers` 빈 배열 | 89.0% | 91.5% | 90.4% | **76.1%** |
| 소분류당 소재 | 10.0 | 9.7 | 12.0 | **11.4** |
| 12 전에 멈춘 소분류 | — | — | 0 | **21개** |

★★ **핵심 지표인 알 가치 1~2 가 12건 → 0건이다.** 건우가 정한 하한 없음 규칙의 직접 효과다.

★ 알 가치 5 의 비율이 크게 올랐다 (R019 276/687 = 40% → R020 449/656 = **68%**).
  하한을 없애자 **"12번째를 채우려고 고른 소재"** 가 통째로 사라졌기 때문이다.

★ `acceptedAnswers` 빈 배열이 90.4% → **76.1%** 로 내려갔다. ★ **확인 필요**로 남긴다.
  추정 원인 — R020 은 `유엔/국제연합`·`조기/조기 게양`·`빨강/빨간색` 처럼
  **한국어 표기 차이**를 더 많이 넣었다.
  ★★ D-094 의 게이트가 막는 것은 외국어·한자 원어 표기이고 한국어 변형은 막지 않으므로
  이 변화 자체는 규칙과 부딪히지 않는다. 다만 **늘어난 것이 맞는 방향인지는 내가 정할 일이 아니다.**


===========================================
6. ★★ 내가 자체 판단한 것 — 틀렸다고 보면 되돌린다
===========================================

| 무엇 | 어떻게 했나 | 되돌리기 |
|------|------------|---------|
| 표기 변형 규칙을 코드로 | `pipeline/lib/answer-rules.mjs` / 적재 게이트 (D-094) | ★ 쉽다. `--no-gate` 가 있다 |
| ★★ B-2 의 전제가 사실과 달라 **다른 근거로 처리** | 위 2-3. 기록을 따로 남겼다 | ★ 백업 파일에서 복원 가능 |
| 교체를 9건이 아니라 **4건만** | 8건을 읽고 4건 동의 / 4건 근거 적고 유지 | ★ `seeds-restore.mjs` |
| R020 규칙검사 11건 **질문만 수정** | 정답은 바꾸지 않았다 | ★ 파일 수정이고 커밋에 남는다 |
| 라운드 안 같은 답 9건 격리 | 알 가치가 높은 쪽·소분류의 핵심인 쪽을 남겼다 | ★ `seeds-restore.mjs` |
| 중복 판정 same 12건 격리 | 기존을 살리고 신규를 내렸다 | 〃 |
| 판정이 엇갈린 2건을 교체 후보에서 제외 | R019 `football/1#011` 선례를 따랐다 | 〃 |
| 판정 스크립트에 `unjudged` 추가 | D-095 | ★ 코드만 바뀌었다 |

### ★ 실수 하나 — 즉시 되돌렸다

문법만 확인하려고 `seeds-dupe-judge.mjs` 를 `import()` 했는데,
★ 이 스크립트는 **최상위에서 바로 실행된다.** 인자가 없어 기본값 `r017` 로 돌아가
`data/pipeline/dedupe/r017-judged.json` 을 덮어썼다.

→ ★ 커밋 전이었으므로 `git checkout` 으로 **즉시 원복**했다. **손실 없다.**
→ ★ 이후로는 `node --check` 만 쓴다.


===========================================
7. ★★ 건우가 정해야 하는 것 (13-PENDING-CHECKS)
===========================================

★★ 지시대로 이 보고서에서 **확인을 요청하지 않는다.** 13-PENDING-CHECKS 에 쌓아 두었다.
아래는 그 목록이다.

| # | 무엇 |
|---|------|
| **C21** | ★★★ **R020 656건을 적재할 것인가** — 적재하면 활성 문제 1,374 → **2,030건**(목표의 68%) |
| **C22** | ★★★ **판정되지 않은 6쌍** — 적재 전에 반드시 판정해야 한다 |
| **C23** | 판정이 "신규가 낫다" 고 본 9건을 바꿔 끼울 것인가 (R020분) |
| **C24** | ★★ **숫자만으로 된 정답의 라운드 안 충돌**에 D-090 을 그대로 적용할 것인가 |
| **C17** | 손실률 곡선(4.2%)을 근거로 남은 **138개** 소분류의 소재 수를 얼마로 잡을 것인가 (Q-94) |

★ C24 만 조금 설명한다. 답이 `4` 인 문제가 셋 나왔다 —
**야구 심판진 수 / 교향곡 악장 수 / 심장의 방 개수**.
★★ **답이 `4` 라는 것 말고는 공통점이 없다.** 묻는 지식도 분야도 다르다.
★ 나는 D-090 을 그대로 적용해 거절했지만, 이것은
D-091("정답이 같아도 지식이 다르면 중복이 아니다")과 부딪힌다.
★ 되돌리기는 `seeds-restore.mjs` 로 쉽다.


===========================================
8. 문서·기록
===========================================

### 새로 적은 결정

```
D-093  ★★★ 1회차는 난이도를 올리지 않는다 — 2회차에 각도를 세부화한다
         ★ 2회차 지시를 미리 적어 두었다:
           "대상은 1회차와 같은 수준으로 유지하고, 묻는 각도만 세부적으로 한다"
           1회차급  "목이 가장 긴 동물은?"                       → 기린
           2회차급  "목뼈 개수가 사람과 똑같이 일곱 개인 동물은?" → 기린
D-094  표기 변형 규칙을 프롬프트에서 코드로 옮겼다 — 적재 게이트
D-095  판정 묶음이 실패하면 어느 쌍이 빠졌는지 반드시 남긴다
D-096  소분류당 상한 12 / 하한 없음 — 억지로 채우지 않는다
```

### 내린 미결정

```
Q-92  생성자↔판정자 일치율 46%   → 건우 확정: 결함으로 보지 않는다
Q-93  넓은 소분류의 난이도 하락    → D-093 · D-096 으로 옮겼다
Q-94  손실률 곡선                 ★ 남아 있다. 네 번째 점(4.2%)을 더했다
```

### 갱신한 문서

```
docs/05-STATUS.md          Track D 를 R020 으로
docs/06-WORKLOG.md         R020 항목
docs/07-DECISIONS.md       D-093 ~ D-096
docs/08-OPEN-QUESTIONS.md  Q-92·Q-93 내림 / Q-94 갱신
docs/13-PENDING-CHECKS.md  C15·C16·C18·C19·C20 닫음 / C21~C24 추가
pipeline/README.md         현재 상태 + 네 라운드 실측표
```

### 새 파일 / 고친 스크립트

```
pipeline/lib/answer-rules.mjs       ★ 새로. 감사와 적재가 함께 쓴다
scripts/seeds-restore.mjs           ★ 새로. seeds-discard 의 역
scripts/pipeline-load.mjs           변형 게이트 + --no-gate
scripts/db-rule-audit.mjs           공용 규칙 모듈을 쓰도록
scripts/seeds-dupe-judge.mjs        unjudged / skippedChunks
scripts/answer-rules-test.mjs       ★ 새로. 유닛 20케이스 (API 0회)
```

### 감사·백업 기록

```
data/pipeline/audit/r020-removed-variants.json         359개 변형 원본
data/pipeline/audit/r020-removed-korean-variants.json  ★ B-2 전제가 달랐던 4건
data/pipeline/audit/r020-deactivated.json              db:33 · db:121
data/pipeline/audit/r020-swapped.json                  교체 4건 + 밀려난 원본
data/pipeline/audit/r020-swap-candidates.json          ★ C23 교체 후보 9건
data/pipeline/audit/db-rule-audit-r020.json            최종 감사
data/pipeline/dedupe/r020-candidates.json              후보 쌍
data/pipeline/dedupe/r020-judged.json                  ★ pendingUnjudged 6건 포함
tmp/r020/STATE.md                                      중단·재개 상태
```

★ 비밀값은 커밋에 넣지 않았다. `.env` 는 추적 대상이 아니고, 새로 만든 파일에 키가 없음을 확인했다.
