# ai-out — 라운드별 설계·조사 기록

이 폴더는 프로젝트 진행 과정에서 라운드 단위로 작성된 **시점별 기록(append-only)** 이다.

## 읽는 사람이 가장 먼저 알아야 할 것

> **현재 유효한 규칙과 상태의 정본(正本)은 `docs/` 이다.**
> **`ai-out/` 과 `docs/` 의 내용이 충돌하면 언제나 `docs/` 가 우선한다.**

`ai-out/`의 파일은 "그 시점에 무엇을 조사했고, 어떤 선택지가 있었고, 왜 그렇게 결정했는가"를
남기기 위한 것이다. 결정이 나중에 뒤집히더라도 과거 파일은 수정하지 않는다.
정정이 필요하면 다음 라운드 파일에서 정정한다.

따라서 `ai-out/`의 오래된 문서에는 **현재는 유효하지 않은 서술이 그대로 남아 있다.**
현재 규칙을 알고 싶다면 `docs/`를 봐야 한다.

## 규칙

- 파일명은 `R001.txt`, `R002.txt`, ... 3자리 순번이며 라운드마다 새 파일을 만든다.
- 작성된 파일은 **수정하지 않는다.** append-only.
- 인코딩은 UTF-8, 개행은 저장소 정책(LF)을 따른다.
- 각 파일 맨 앞에 ROUND / DATE / TASK / STATUS 헤더가 있다.

## 라운드 색인

| 파일 | 주요 내용 |
|------|-----------|
| R011.txt | ★★ **문제 확보 방식 전환 — OpenTDB 폐기, Gemini 직접 생성.** 카테고리 트리 설계(대 7 / 중 63 / 소 294)와 ★ **샘플 257건 출제**(중분류 63개 전부, 건우 판단용). 카테고리를 생성 입력으로 만들어 불일치 0건(D-039). 접근성·난이도 분리(D-038). ★★ 429 가 하루 종일 한 번도 나지 않았다 — R010의 "호출 4회에 429" 는 **내 예산 카운터가 새고 있어서** 나온 잘못된 관측이었다(models 는 생 fetch, compare 는 saveState 누락). 둘 다 고쳤다. ★ 병목이 무료 한도에서 **카테고리 트리 너비**로 이동 → Q-63 전제 붕괴. ★ 실측이 내 설계를 두 번 반박했다 — 중복 판정의 질문 유사도는 구분에 실패(실제 중복 쌍 0.13~0.50), 단계별 게이트는 호출 배수 목표를 넘겼다(1.64배). 둘 다 그대로 보고하고 고쳤다. ★ 프롬프트를 실측 중 한 번 고쳤다(g1→g2): 탈락 10건 중 5건이 "표기 변형을 질문에 써 버린" 한 원인 → 재실측 0건. ★ 난이도가 거의 1~2 뿐이라는 것이 드러났다(Q-68). 라이선스 표기를 "일부 문제 출처" 로 정정(D-042) |
| R010.txt | **Track D 착수 — 파이프라인 전 구간 구현.** ★ 모델 실측으로 Q-59 종결(세 모델 품질 차이 없음 / 503 가용성 때문에 실제로는 3.6-flash 가 처리 → 체인 방식, D-033). ★ 429·예산 가드를 가공 코드보다 먼저 만들었고 첫 배치가 2회 호출 만에 429 → 게이트가 설계대로 동작(D-034). 규칙 필터 오차단을 전수 검토로 두 번 좁혀 20.8%→10.8%, 오차단 0건. ★★ 역검증이 정상 문제 6건을 전부 오탈락시킨 판정 결함을 발견해 수정 → ★ API 호출 0회 재판정으로 회복(D-035), 생존율 11.8%→**29.4%**(R002 추산 25% 부합). 검수→적재→게임까지 1회 통과(출제 가능 63건). 라이선스 화면 표기 추가(D-036). ★ 100건 배치는 무료 한도로 20건만 도달 — **부분완료**. Q-62/Q-63/Q-64 신설 |
| R009.txt | **UX 지적 2건 처리.** ★ 알림이 스크롤 최상단에서만 보이던 문제 → 화면 아래 고정 토스트(위치 근거: Phase 3의 문제 지문·남은 시간을 가리지 않는다). ★ 성공한 뒤에도 이전 에러가 남던 **결함** → 화면 전환 시 한 곳에서 정리(D-032). ui-check 를 43항목으로 확장하며 ★ "보인다" 를 DOM 존재가 아니라 **화면 좌표**로 판정. ★ DB 정리 추정을 실측으로 반박(정리 로직은 동작했고 남은 계정은 사람이 만든 것) + 강제 종료 대비 시작 시 청소 추가. ★ UI 지적 분류 기준 D-031 |
| R008.txt | **브라우저 확인에서 나온 결함 처리.** ★ 게임 시작 무반응의 원인은 disabled 가 아니라 **표시 경로 부재**였다(로비 화면에서 모든 서버 에러가 보이지 않았다) → 표시 경로를 셸 한 곳으로 통합(D-027) + 에러 코드 11종 도달 전수 점검. "내보내기" 는 구현·동작 정상이었고 **05-STATUS 에 행이 없던 문서 결함**을 정정. ★ 프록시 ECONNRESET 은 실측 결과 "탭 닫기 로그" 가 아니라 **서버 사망 신호**여서 침묵시키지 않고 20줄→2줄로 정리(D-030). Q-61 승인 → `npm run ui-check` 승격 + 동작 확인 확장(31항목, 결함 2건을 수정 전에 재현). ★ 세 라운드 연속 "사람 경로" 사고에 대한 구조적 대책(D-028) |
| R007.txt | **Phase 2 (로비 설정 / 경험률 / 카운트다운 / 게임 레코드) 구현.** UI 라벨 줄바꿈 결함을 헤드리스 Chrome 으로 실측해 원인 확정 후 규칙화(D-022, 지적받지 않은 1건 추가 발견), 출제 가능 수 안내와 시작 직전 재검증(Q-21), 경험률 표시(Q-12, 분모 0 방어), 카운트다운 T01~T04 + 클라이언트는 표시만(guide 45절), ★ PAUSED 를 Phase 2에서 구현하지 않는 판단(D-023), ★ Phase 3 제거 대상 임시 코드 목록(TEMP-P3), 봇 단정 72건, Q-61 |
| R006.txt | **`npm run dev` 실행 불가 사고 처리.** 원인은 `moduleResolution: NodeNext`(.js 지정자)와 `node --experimental-strip-types`(재작성 안 함)의 조합 불가. ★ R005에서 못 잡은 이유 규명(테스트가 사람과 다른 실행 경로를 썼다), 실행 경로를 컴파일 산출물 하나로 통일(D-020), 실행 확인 게이트 `npm run verify`/smoke 도입(D-021), 봇 자체 기동, ★ 건우용 브라우저 확인 절차 |
| R005.txt | **Phase 1 (계정/세션/방) 구현.** Gemini 키·모델·프롬프트 실측 검증(★ R002 전제 모델 사용 불가 발견), Q-53 터널 URL 유지 실측 확정, RULES 상수 오류 수정, 메모리 방 관리 구조와 전역 tick(Phase 3 뼈대), 봇 클라이언트 5종, Q-59~Q-60 |
| R004.txt | **Phase 0 실제 구현.** Cloudflare quick tunnel 검증(WebSocket 통과 확인), docs 12개 문서 골격 + PAUSED 반영 상태 머신, 모노레포 스캐폴딩, DB(스키마·마이그레이션·부팅 정리·백업·시드 53문제), shared 순수 함수 구현(테스트 97건), 서버·클라이언트 최소 동작, 파이프라인 파일 기반 재설계, Q-53~Q-58 |
| R003.txt | 저장소 초기화, 배포/DB 최종 확정(Render Free + Neon Free 권장), 게임 상태 머신(상태 5종·전이 20종·동시 발생 11케이스·타이머), Socket 이벤트 프로토콜, DB 스키마 초안 + guide 40절 통계 역검증, Q-47~Q-52 |
| R002.txt | 배포/DB 재조사(유료 최저 티어 포함), OpenTDB 실측 파일럿(전량 수확 4,500건·생존율 분석), 가공 파이프라인 설계, 문제 데이터 구조, 정답 정규화·힌트·마스킹 명세, Q-36~Q-46 |
| R001.txt | guide.md 전문 분석, 요구사항 인벤토리 약 160건, 결정 필요 항목 Q-01~Q-35, 기술 스택 검증, 무료 티어 조사, 문서 체계·개발 계획 제안 |

최신 라운드가 위쪽이다.

## 관련 문서

- `guide.md` — 최초 요구사항 원본. **수정하지 않고 원형을 보존한다.**
  단 이후 라운드에서 개정된 규칙이 있으므로, guide.md 단독으로 현재 규칙을 판단하면 안 된다.
- `docs/` — 현재 유효한 규칙과 구현 상태의 정본. (Phase 0에서 생성 예정)
