===========================================
ROUND: 015
DATE: 2026-09-11
TASK: 확정 사항 반영(Q-82·Q-56·Q-83·Q-84) / ★★ Phase 5 재접속 복구 · 일시정지(PAUSED)
STATUS: 완료 — ★ 브라우저 수동 확인만 남았다 (7장에 절차)
===========================================


┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ ★★ 먼저 말할 것                                                     ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

★★★ **자동 재개를 없앤 것이 이번 라운드의 본체입니다.**
   R014 의 근사 구현(D-061)은 사람이 돌아오면 **자동으로** 게임을 진행했습니다.
   ★ 그것은 Q-30 확정 규칙이 금지한 바로 그 동작이었습니다 —
     "친구 한 명이 먼저 접속한 순간 게임이 돌아가면 나머지가 새 URL 을 입력하는 동안 문제가 소모된다."
   ★★ 이제 **방장이 재개를 눌러야** 이어집니다. 3초·6초·30초를 기다려도 재개되지 않는 것을 실측했습니다.

★★ **방 폭파가 생겼습니다** (Q-82). 게임 중 마지막 활성자가 **나가기 버튼**을 누르면 방이 즉시 사라집니다.
   ★ 끊긴 것은 다릅니다 — 일시정지가 되고 5분을 기다립니다.

★★ **실행해 보고 정보 누출을 찾았습니다.** PAUSED 중에는 `endsAt` 이 낡은 값이라,
   오래 멈춰 두면 재접속 스냅샷이 "남은 10초 이하" 로 오판해 **힌트를 공개**합니다.
   ★ 실측: 같은 시점에 낡은 계산은 7.1초, 실제 남은 시간은 27.9초였습니다.
     ★★ 아직 28초가 남은 문제의 힌트를 공짜로 얻는 것입니다. `remainingMs` 로 판단하도록 고쳤습니다.

★ **Q-84 부하를 실측했습니다.** 봇 5명이 1,100개를 쏟아붓는 중에도 판정 지연이 **2ms** 로
  부하 없는 기준선과 같았습니다. 완화해도 판정 성능에 영향이 없습니다.

★ **단축키는 전부 Alt 조합입니다.** 이 게임은 채팅 입력창이 항상 포커스이므로
  "입력창에 포커스가 없을 때만" 이라는 흔한 해법을 쓸 수 없습니다. 근거는 1-2 에 적었습니다.

verify 통과 — vitest **314건**(292 → 314, 13 skipped) / smoke 10/10 / ui-check **101/101**(68 → 101).
봇 시나리오 **18종 전부 통과** (신설 5종: pause 28 / abandon 9 / pausehost 8 / pausehint 21 / flood 10).
태그 `v0.5-phase5`. 커밋 4개 push 완료.


═══════════════════════════════════════════════════════════════════════
1. 확정 사항 반영
═══════════════════════════════════════════════════════════════════════

──────────────────────────────────────────
1-1. Q-82 — 방 종료 두 갈래 (A-4 / B-3) — 완료
──────────────────────────────────────────

건우 지적: "게임 중간에 나가려는데 ★ 모두가 나가기를 했는데 왜 게임이 살아있는 거냐?
★ 방 폭파 기능이 없는 것 같다."

★ 원인은 규칙 두 개가 겹친 자리였습니다.
  · Q-15  "명시적 나가기와 예기치 못한 끊김을 구분하지 않는다"
  · D-060 "게임 중 퇴장은 슬롯을 유지한다" (R014)
  ★★ 둘을 합치면 **전원이 나가기 버튼을 눌러도 방과 게임이 그대로 남습니다.**

구현한 규칙 (D-064)

  ┌────────────────────────────────────┬──────────────────────────────────────┐
  │ 게임 중 마지막 활성자가 나가기 버튼 │ ★★ 즉시 방 폭파. PAUSED 로 가지 않고 │
  │                                    │ 5초도 기다리지 않는다                │
  │ 그 외 전원 이탈                    │ ★ PAUSED → 5분 뒤 방 폭파            │
  │ (끊김 / 새로고침 / 터널 / 창 닫기) │                                      │
  └────────────────────────────────────┴──────────────────────────────────────┘

★ **뒤집은 것은 방 종료 판단 하나뿐입니다.** 점수·경험 기록·슬롯 유지·결과 포함 여부는
  여전히 구분하지 않습니다. Q-15 의 취지(끊긴 사람을 불리하게 하지 않는다)는 그대로입니다.

★ 30분 → 5분 (Q-30 개정). 설정값 `PAUSE_ABANDON_MS` 로 뺐습니다 (D-071).
  ★ 근거: 터널이 끊기면 새 URL 을 뿌리고 다시 들어오는 데 몇 분이면 충분합니다.
    아무도 없는 방을 30분 붙잡아 둘 이유가 없습니다.

★★ 경험 기록 — **자동으로 충족됩니다.** 두 경로 모두 정답을 공개하지 않으므로
  Q-47 의 조건("정답이 공개되는 순간 그 자리에 있었던 사람")이 성립하지 않습니다.
  ★ 이미 기록된 것은 지우지 않습니다 (guide 25절). 이 사실을 01-GAME-RULES 3장과 D-064 에 명시했습니다.

★ 판단한 것 — **로비·결과 화면에서는 즉시 폭파하지 않습니다.**
  ★ 근거: 살아 있는 게임이 없고, 남은 자리는 **그 게임 결과를 보기 위한 자리**입니다.
    기존 경로(활성 0명 10분)로 정리됩니다. 이것도 봇으로 단정했습니다.

★ 판단한 것 — **게임 중 나가기에만 확인창을 둡니다.** 로비·결과 화면에는 두지 않았습니다.
  ★ 근거: 로비·결과 화면에서 나가는 것은 잃을 것이 없습니다.
    ★★ 그리고 확인창이 남발되면 정작 위험한 확인창도 습관적으로 넘기게 됩니다.
  ★ 확인창은 `Enter` 만으로 확정됩니다.

★ 구현 주의 — "내가 마지막 활성자인가" 는 **끊기 전에** 판정해야 합니다.
  끊은 뒤에 세면 자기 자신이 이미 빠져 활성 0명이 되어 나가기와 끊김을 구분할 수 없습니다.

──────────────────────────────────────────
1-2. Q-56 — 단축키 (A-3) — 완료
──────────────────────────────────────────

건우 확정: "웬만하면 마우스 사용 없이 돌아가야 해. 단축키 설명도 어딘가에 같이 적어놔."

★★★ **판단한 것 — 단독 문자키를 쓰지 않습니다. 전부 `Alt` 조합입니다.** (D-065)

  ★ 왜 흔한 해법을 쓸 수 없는가 —
    보통 웹앱은 "입력창에 포커스가 없을 때만 단축키를 받는다" 로 충돌을 피합니다.
    ★★ 그런데 이 게임은 **채팅 입력창이 항상 포커스**입니다. 정답을 치는 것이 게임 그 자체입니다.
      → ★ 그 해법을 쓰면 단축키가 **영원히 동작하지 않습니다.**

  ★★ `Alt` 조합은 어떤 IME 에서도 **문자를 만들지 않습니다.** 한글 조합 중에도 안전합니다.
    → ★ 그래서 **포커스를 구분할 필요 자체가 없어집니다.** 이것이 이 설계의 핵심입니다.

  ★ `Ctrl` 을 쓰지 않은 근거: 브라우저·OS 단축키와 충돌이 심합니다
    (`Ctrl+R` 새로고침 / `Ctrl+W` 탭 닫기 / `Ctrl+S` 저장). ★ 실수로 눌리면 판이 날아갑니다.
  ★ `Esc` 는 단독 키지만 문자를 만들지 않으므로 예외로 허용했습니다 (입력창 포커스 복귀 전용).
  ★ IME 조합 중(`isComposing`)에는 어떤 단축키도 실행하지 않습니다.

배정 (01-GAME-RULES 16장에 정본)

  Enter    메시지 전송 / 확인창 확정          Esc      채팅 입력창으로 포커스 복귀
  Alt+S    넘기기 투표 / 취소        (F2)     Alt+K    (방장) 이 문제 넘기기      (F4)
  Alt+R    (방장) 일시정지 재개      (F8)     Alt+Q    (방장) 게임 강제 종료
  Alt+A    (방장) 다시 하기                   Alt+L    (방장) 로비로
  Alt+X    방 나가기                          Alt+G    단축키 전체 보기 / 접기    (F9)

★ Q-33("조합키와 F키를 모두 지원")의 부분 이행 — 근거를 밝힙니다.
  ★★ 안전한 F키가 **F2 / F4 / F8 / F9 넷뿐**입니다.
    (F1 도움말 / F3 찾기 / F5 새로고침 / F6 주소창 / F7 캐럿 / F10 메뉴 / F11 전체화면 / F12 개발자도구)
  ★ 나머지에 F키를 억지로 배정하면 **브라우저 기능을 빼앗습니다. 그것이 더 나쁩니다.**
  → ★ 자주 쓰는 넷에만 F키를 주고, 그 사실을 화면에 적었습니다.

★ 화면 표시 위치 판단 — **채팅 입력창 바로 아래.**
  (1) ★ 게임 중 시선과 손이 그곳에 있습니다. 정답을 치는 자리입니다.
  (2) ★ 상시 전체 표시는 자리를 먹습니다 — 건우의 "스크롤 없이 한 화면" 목표와 충돌합니다.
  → ★★ **지금 쓸 수 있는 것만** 한 줄로 보여주고 `Alt+G` 로 전체를 펼칩니다.
     ★ 상황별로 유효한 것만 보이면 학습이 쉽고, 방장 전용 키가 비방장에게 보이지 않습니다.

★ 접근성 — 확인창이 열리면 포커스가 확인창 안으로 이동하고(Enter 로 바로 확정),
  닫히면 **채팅 입력창으로 돌아옵니다.**
  ★ 근거: 포커스가 사라진 채로 정답을 치면 아무 데도 들어가지 않습니다. 치명적입니다.

──────────────────────────────────────────
1-3. Q-83 — 타이머 정수 표시 (A-1) — 완료
──────────────────────────────────────────

건우 확정: "시간 줄어드는 게 ★ 소수점 단위는 안 보여줘도 된다. 눈만 아프다."

★ 문제 타이머 / 카운트다운 / ★ PAUSED 만료 카운트다운까지 전부 `Math.ceil(ms / 1000)` 입니다.
★ **내부 계산은 ms 그대로입니다.** 표시만 바꿨습니다 (D-070).
  ★ 근거: 정답 인정 경계가 `endsAt` 이고 판정은 ms 단위입니다.
    표시를 위해 내부 정밀도를 버리면 **0.1초로 갈리는 선착순 판정이 망가집니다.**
★ `ceil` 을 쓴 근거: 남은 0.4초를 "0초" 로 쓰면 이미 끝난 것처럼 보입니다.
  1초로 올려 보여 주고, 실제 종료는 서버 이벤트로 알립니다.
★ ui-check 가 `/^\d+초$/` 로 검사합니다. 소수점이 돌아오면 게이트에서 걸립니다.

──────────────────────────────────────────
1-4. Q-84 — 도배 제한 완화 (A-2) — 완료
──────────────────────────────────────────

건우 확정: "도배 제한 넉넉하게 해도 된다. ★ 도배하는 것도 나름 재밌는 포인트다.
★ 시스템에 부하 갈 정도로 도배하는 것만 막자."

★ 기준을 **예절 → 서버 보호**로 바꿨습니다 (D-069). 값은 설정값입니다.

  윈도    3초 → ★ **1초**   근거: 짧은 윈도는 "순간 폭주" 만 자릅니다.
                             긴 윈도는 꾸준한 연타까지 막아 재미를 깎습니다.
  개수    10  → ★ **20**    근거: 사람이 Enter 를 연타하는 상한이 초당 8~10개입니다(추정).
                             20은 그 두 배가 넘으므로 사람은 걸리지 않습니다.

★ 판단한 것 — **100자 제한은 유지합니다.**
  ★ 근거: 이 값의 목적이 도배 억제가 아닙니다 —
    (1) 화면 레이아웃 붕괴 방지 (2) 정답 문자열이 100자를 넘을 일이 없습니다.
  ★ Q-84 의 완화 취지와 무관한 값이므로 건드리지 않았습니다.

★★ 부하 실측 (건우 지시: "봇으로 부하를 만들어 판정 성능 실측")

  재는 값: **정답을 보낸 순간부터 `question.resolved` 를 받기까지.**
  ★ 그것이 사용자가 체감하는 지연이고 선착순 승패를 가르는 값입니다.

  기준선 (부하 없음)                          2ms
  ★★ 도배 중 (5명이 1,100개 투입)            ★ **2ms** — 기준선과 같다
  억제 발동                                   180회 (서버 보호선이 살아 있다)
  ★ 사람 속도 (초당 5개)                      한 번도 막히지 않았다
  ★ 억제 안내 수신자                          도배한 본인만 (다른 사람 0회)

★ 판정이 동기 블록이라 부하가 걱정됐지만, 판정 자체가 Set 조회 수준이라 호출이 잦아도 늘지 않았습니다.
★ 병목이 생긴다면 판정이 아니라 브로드캐스트 쪽입니다. 그때 다시 측정하겠습니다.

──────────────────────────────────────────
1-5. 접수만 — 스크롤 없이 한 화면 (Phase 7)
──────────────────────────────────────────

건우: "★ 스크롤 없이 한 화면에 모든 요소가 다 보여야 한다. 스크롤 있으면 굉장히 조잡하다."
     "★ 일단은 웹 UI 가 우선이다. 모바일은 천천히 생각해보자."

★ 지시대로 **구현하지 않았습니다.** 요소 목록과 우선순위만 09-BACKLOG 1-3 에 정리했습니다.

★ 그리고 **실측값을 남겼습니다** (ui-check 가 매 실행마다 콘솔에 남깁니다).

  320px   문서 1,708px / 뷰포트 1,165px → ★ **1.47배**
  720px   문서 1,498px / 뷰포트   900px → ★ **1.66배**

★ 목표는 이 비율을 1.0 이하로 만드는 것입니다. 우선순위 1은 문제 지문 / 남은 시간 / 채팅 입력창입니다.
★ 접기 후보로 분류한 것: 점수판 / 스킵 카드 / 방장 카드 / 라이선스 고지 / 서버 시각 오프셋.
  ★ 방장 카드와 스킵 카드는 **단축키가 있으므로 접어도 조작이 가능합니다.**
★ Phase 5·6 에서 화면 요소가 늘어날 때마다 그 표를 갱신하겠습니다. 일시정지 화면은 이미 추가했습니다.


═══════════════════════════════════════════════════════════════════════
2. Phase 5 구현 — 재접속 복구 / 일시정지
═══════════════════════════════════════════════════════════════════════

──────────────────────────────────────────
2-1. R014 의 근사 구현에서 무엇이 달라졌는가
──────────────────────────────────────────

  항목          R014 근사 (D-061)          ★ R015 정식
  ──────────────────────────────────────────────────────────────────
  상태          QUESTION_ACTIVE 유지       ★ PAUSED 로 전이
  화면          아무 표시 없음             ★ "일시정지" 화면
  ★★ 재개       ★★ 자동                    ★★★ 방장이 눌러야 한다
  타이머 보존   endsAt 을 밀어 준다        ★ remainingMs 로 저장
  만료          없음 (방 삭제 10분)        ★ 5분 → 방 폭파 (Q-82)
  대상          QUESTION_ACTIVE 만         ★ COUNTDOWN / QUESTION_RESOLVED 도

──────────────────────────────────────────
2-2. 설계 판단 셋
──────────────────────────────────────────

★ (1) `endsAt` 을 밀지 않고 `remainingMs` 를 저장한다 (D-067). 근거 넷 —
  1. ★★ 밀기 방식은 **"지금 멈춰 있다" 를 표현하는 값이 없습니다.** 재접속자가 구분할 수 없습니다.
  2. ★★ 멈춘 동안에도 tick 이 그 방을 건드립니다. "PAUSED 방은 건너뛴다" 는 원칙과 어긋납니다.
  3. ★ 만료 검사(`abandonAt`)와 게임 타이머가 서로 다른 값이라 섞이지 않습니다.
  4. ★★★ **힌트 판단이 밀기 방식에서는 불가능합니다.** → 3장에서 실제로 발견한 결함입니다.

★ (2) 한 방에 두 개의 만료 타이머가 동시에 돌지 않게 한다 (D-066).
  PAUSED → 5분 하나만 / LOBBY·GAME_RESULT → 10분 하나만.
  ★ 상태가 배타적이므로 "어느 것이 이기는가" 를 정의할 필요가 없어집니다.

★ (3) 방 폭파 경로를 하나로 모았다 (`destroyRoom`). 진입로는 넷입니다 —
  마지막 활성자 나가기 / PAUSED 만료 / 로비·결과에서 마지막 참가자 퇴장 / 활성 0명 10분.

──────────────────────────────────────────
2-3. PAUSED 중 허용과 금지 (B-2)
──────────────────────────────────────────

  채팅          O   ★ 단 **정답 판정은 하지 않습니다** (실측으로 단정)
  정답 판정     X
  스킵 투표     X   → `INVALID_STATE`
  방장 액션     O   재개 / 강제 종료 / 접속 종료자 강퇴 / ★ 카운트다운 취소(COUNTDOWN 에서 멈춘 경우)
  입장·퇴장     O   ★ 퇴장이 마지막 활성자면 방이 폭파됩니다 (Q-82)

★ 방장 이전 타이머는 **PAUSED 중에도 돕니다** (활성 ≥ 1 일 때).
  ★★ 근거: 재개는 방장만 할 수 있는데 방장이 안 돌아오면 아무도 누를 수 없습니다.
    ★ 30초 뒤 이전되고 **새 방장이 재개에 성공하는 것**을 실측했습니다.

──────────────────────────────────────────
2-4. 화면 (B-4)
──────────────────────────────────────────

★★ D-030 교훈 — 화면에 아무 표시가 없으면 "멈췄다" 와 "고장났다" 가 구분되지 않습니다.
  ★ R014 의 근사 구현이 정확히 그 상태였습니다.

일시정지 화면은 네 가지를 보여줍니다.
  (1) ★ 왜 멈췄는가 (무엇을 하다 멈췄는지)
  (2) ★ 몇 명이 돌아왔는가 (N / M)
  (3) ★ 멈춘 시점의 남은 시간 — **고정값입니다. 흐르지 않습니다**
  (4) ★★ 방이 사라지기까지 남은 시간
      ★ 판단: **표시합니다.** 근거 — 숨기면 "언제까지 돌아와야 하는지" 를 알 수 없고
        방이 사라진 뒤에야 알게 됩니다. ★ 돌아올 사람에게 가장 필요한 정보입니다.
        ★ 60초 아래로 내려가면 강조 색으로 바뀝니다.

★ 방장에게만 재개 버튼이 보입니다. `canResume` 은 **서버가 계산해 보냅니다.**
  ★ 근거: 클라이언트가 "내가 방장인가" 로 유추하면 방장 이전 직후에 서버와 어긋납니다.
★★ 그리고 **자동으로 재개되지 않는다는 사실과 그 이유**를 화면에 적었습니다 (D-030).

──────────────────────────────────────────
2-5. 재개 후 힌트 (B-5) — ★ 여기서 결함을 찾았습니다
──────────────────────────────────────────

★ 3장에 적었습니다. ★ 결과만 적으면 —
  · 멈춘 시점에 20초 넘게 남았으면 **아무리 오래 멈춰 있어도 힌트가 공개되지 않습니다**
  · 재개하면 **다시 계산된 기준**으로 남은 10초에 힌트가 옵니다 (실측 9,983ms)
  · 이미 공개된 힌트는 재접속해도 그대로 보이고, 재개해도 **두 번 오지 않습니다**

──────────────────────────────────────────
2-6. 재접속 복구 (B-6)
──────────────────────────────────────────

★ 네트워크 단절과 프로세스 종료는 다릅니다. 이 구분이 PAUSED 설계의 근거입니다 (02-ARCHITECTURE 2장).

  서버 PC 네트워크 단절 → 프로세스 **생존** → 메모리의 방·게임 **그대로** → ★ 이어하기 가능
    복구 절차: 터널 재기동 → 새 링크 공유 → ★ **방장이 재개**
  서버 프로세스 종료     → 메모리 **소실** → 이어하기 불가
    복구 절차: 부팅 시 정리 절차가 방을 닫고 게임을 `server_restart` 로 종료

★★ PAUSED 는 **무한정 기다리지 않습니다** (R015 에서 바뀐 점). 5분 뒤 방을 폭파합니다.
  ★ 한 명이라도 돌아오면 그 시계는 처음부터 다시 셉니다.

★ 재접속 스냅샷에 담기는 것: 상태 / 현재 문제 / **`paused`(멈춘 남은 시간·폭파 시각·복귀 현황·canResume)** /
  점수 / 채팅 / 색상 / 스킵 현황. ★ 힌트는 조건을 만족할 때만입니다.


═══════════════════════════════════════════════════════════════════════
3. ★★★ 실행해 보고 찾은 것
═══════════════════════════════════════════════════════════════════════

──────────────────────────────────────────
3-1. ★★★ 힌트 정보 누출 — 코드를 쓰는 것만으로는 안 나왔습니다
──────────────────────────────────────────

★ 무엇이 문제였나
  PAUSED 중에는 `endsAt` 이 **멈춘 순간의 낡은 값**입니다. 시간이 갈수록 남은 시간이
  줄어든 것처럼 보입니다. ★★ 재접속 스냅샷이 그 값으로 힌트 조건을 판단하고 있었습니다.

★ 실측 (봇 `pausehint` 시나리오)
  문제 시작 2초 뒤 전원 이탈 → 20초 멈춰 둠
    낡은 endsAt 으로 계산한 남은 시간   7.1초   → ★ 힌트 기준선(10초) 아래다
    실제 남은 시간 (remainingMs)      27.9초
  ★★ 즉 `endsAt` 을 믿었다면 **아직 28초가 남은 문제의 힌트가 공개**됐습니다.

★ 어떻게 고쳤나: PAUSED 중에는 `paused.remainingMs` 로 판단합니다.
  ★ 그리고 그 사실을 04-PROTOCOL 9장(스냅샷 주의)에 못 박고, 봇 시나리오로 단정했습니다.

──────────────────────────────────────────
3-2. ★★ COUNTDOWN 에서 멈춘 방의 막다른 길
──────────────────────────────────────────

★ 카운트다운 중 전원 이탈 → PAUSED. 그 방에서 방장이 할 수 있는 것은 재개와 강제 종료뿐인데,
  ★★ **강제 종료는 게임 레코드가 없어서 실패합니다.** 아직 게임이 시작되지 않았기 때문입니다.
  → ★ 재개 말고는 빠져나갈 길이 없었습니다. 카운트다운을 취소하고 로비로 가고 싶어도 방법이 없었습니다.

★ 고침: `game.cancelCountdown` 을 PAUSED 에서도 허용합니다 (`pausedFrom === 'COUNTDOWN'` 일 때만). D-068.

──────────────────────────────────────────
3-3. ★★ PAUSED 중 방장 이전을 멈추면 게임이 죽습니다
──────────────────────────────────────────

★ 처음에는 "PAUSED 중에는 tick 이 다 건너뛴다" 로 만들었습니다.
  ★★ 그러면 방장이 안 돌아오는 경우 **아무도 재개를 누를 수 없습니다.** 게임이 되살아날 수 없습니다.
★ 고침: PAUSED 중에도 방장 이전 검사는 돌립니다. ★ 봇 `pausehost` 로 40초 실측했습니다.

──────────────────────────────────────────
3-4. ★★ ui-check 버튼 라벨 6건이 한꺼번에 깨졌습니다
──────────────────────────────────────────

★ 버튼에 단축키 배지 `<kbd>Alt+K</kbd>` 를 붙이자 `textContent` 가 `'이 문제 넘기기 Alt+K'` 가 되어
  기존 검사가 버튼을 찾지 못했습니다. ★ 연쇄로 결과 화면 검사까지 6건이 실패했습니다.
★ 고침: 라벨 추출에서 `<kbd>` 를 제외합니다.
  ★★ **사람이 읽는 버튼 이름은 배지와 무관합니다.** 검사 쪽을 고치는 것이 옳았습니다.

──────────────────────────────────────────
3-5. ★ 그 밖에
──────────────────────────────────────────

★ 오프라인 에뮬레이션으로는 PAUSED 를 만들 수 없었습니다 (실측).
  `Network.emulateNetworkConditions` 로 offline 을 켜도 **이미 열린 WebSocket 이 끊기지 않았습니다.**
  → ★ 새로고침으로 바꿨습니다. 사람이 실제로 하는 동작이고 소켓이 진짜로 끊깁니다.

★ DOM 노드를 그대로 돌려받아 CDP 가 `Object reference chain is too long` 으로 던졌습니다.
  → 페이지 안에서 `=== null` 까지 평가해 **boolean 만** 받습니다.

★ `tick.ts` 가 `socket/index.ts` 를 import 하는 역방향 의존이 생길 뻔했습니다.
  → 방 폭파 함수를 `rooms/lifecycle.ts` 로 옮겼습니다.

★ 시나리오 안에서 서버 환경 변수를 세워도 적용되지 않았습니다 — **서버가 시나리오보다 먼저 뜹니다.**
  → 모듈 최상단 `SCENARIO_ENV` 로 올렸습니다.

★★ `empty` / `countdown` 봇 시나리오가 **설계대로 실패했습니다.**
  Phase 5·Q-82 로 동작이 바뀌었기 때문입니다 (자동 시작 → 방장 재개 / 게임 중 퇴장 → 폭파).
  → ★ 새 동작을 단정하도록 다시 썼습니다. **바뀐 규칙을 테스트가 붙잡는 자리입니다.**


═══════════════════════════════════════════════════════════════════════
4. 테스트와 실측 (B-7)
═══════════════════════════════════════════════════════════════════════

──────────────────────────────────────────
4-1. 게이트
──────────────────────────────────────────

  npm run verify        ★ 통과
    typecheck           통과
    vitest              **314건** (292 → 314), 13 skipped, 14 파일
    smoke               10 / 10
    ui-check            ★ **101 / 101** (68 → 101)

★ 신설 `server/src/game/pause.test.ts` 22건. ★★ **자동 재개가 없다는 것을 테스트로 못 박았습니다.**
  ★ 근거: R014 의 근사 구현이 자동 재개였습니다. 회귀하면 이 테스트가 깨집니다.

──────────────────────────────────────────
4-2. 봇 시나리오 — 18종 전부 통과
──────────────────────────────────────────

  신설   pause      28   ★★ 일시정지 / 자동 재개 없음 / 권한 / PAUSED 중 금지 / Q-82 즉시 폭파
         abandon     9   ★★ 만료 → 방 폭파 (설정값을 6초로 줄여 검증)
         pausehost   8   ★★ PAUSED 중 방장 이전 → 새 방장이 재개 (약 40초)
         pausehint  21   ★★★ 일시정지와 힌트 — 정보 누출 방어선 (약 50초)
         flood      10   ★★ Q-84 완화 후 판정 성능 실측
  개정   empty       8   ★ COUNTDOWN 에서 멈춤 → 방장 재개 → Q-82 폭파
         countdown  45   ★ 게임 중 퇴장 세 갈래 (비마지막 / 끊김 / 결과 화면)
  회귀   game 58 / race 7 / epoch 10 / concur 25 / collide 20 / full 15 / lobby 37 /
         join / duplicate / reconnect / chat / host — ★ 전부 통과

★★ `abandon` 은 **설정값을 짧게 줄여** 검증했습니다 (건우 지시).
  ★ 5분을 기다리는 테스트는 아무도 돌리지 않습니다. **값만 다르고 코드 경로는 동일합니다.**
★ 관측은 `/debug/room/:id` (개발 전용·읽기 전용) 로 합니다.
  ★★ 근거: 소켓으로 보면 **관찰 행위가 activeCount 를 바꿔** 상태가 달라집니다 (D-072).

──────────────────────────────────────────
4-3. ★★★ 실측값
──────────────────────────────────────────

  ★★ 남은 시간 보존            멈출 때 27,969ms → 6초 뒤 재개 27,968ms   (1ms 차)
  ★★ 멈춘 동안 흐르지 않는다   6초 관측 — remainingMs 완전히 동일 / 문제 수 1 → 1
  ★★★ 자동 재개 없음           사람이 돌아온 뒤 3초 / 6초 / 30초 — 전부 PAUSED 유지
  ★★ 재개 권한                 비방장 game.resume → NOT_HOST / 방장만 canResume=true
  ★ PAUSED 중 금지             정답 판정 안 됨(채팅으로는 남는다) / 스킵 → INVALID_STATE
  ★★ PAUSED 중 방장 이전       30초 뒤 이전 → ★ 새 방장이 재개 성공
  ★★★ 힌트 정보 누출 방어      20초 멈춤 뒤 재접속 — hintRevealed=false, 힌트 문장도 안 내려온다
  ★ 재개 후 힌트               재계산 기준으로 남은 9,983ms 에 push / 두 번 오지 않는다
  ★★ 만료 폭파                 6초로 줄여 검증 — 방 사라짐 / abandoned / 경험 기록 **0행**
  ★★★ 마지막 활성자 나가기     ★ **즉시** 방이 사라진다 (PAUSED 로 가지 않는다)
  ★★ 마지막 활성자 끊김        ★ PAUSED (폭파되지 않는다)
  ★ 결과 화면에서 나가기       즉시 폭파되지 않는다 → 활성 0명 10분 경로 (판단대로)
  ★★ Q-84 판정 지연            부하 없음 2ms / 1,100개 투입 중 **2ms**

──────────────────────────────────────────
4-4. ui-check 에 추가한 것 (68 → 101)
──────────────────────────────────────────

★★ 단축키를 **실제 키 이벤트**로 확인합니다.
  ★ 근거: 자바스크립트로 만든 합성 이벤트는 브라우저 기본 동작(문자 입력·버튼 클릭)을
    일으키지 않아 "Alt 조합이 정말 문자를 만들지 않는가" 를 확인할 수 없습니다.
  ★ `Enter` 는 **문자 이벤트**로 보내야 포커스된 버튼이 눌립니다 (실측으로 알아냈습니다).

확인 항목
  ★ Q-83 정수 타이머 (`/^\d+초$/`)
  ★★ 문자키 `g` 를 치면 **입력창에 들어가고** 단축키 목록이 열리지 않는다
  ★★ `Alt+G` 뒤에도 입력창 값이 그대로다 (Alt 조합은 문자를 만들지 않는다)
  ★★ `Alt+K` → 확인창 → **Enter 만으로 확정** → 포커스가 채팅 입력으로 복귀
  ★ `Esc` 로 포커스 복귀
  ★★ Q-82 나가기 확인창 문구 ("방이 즉시 사라집니다" / "일시정지")
  ★★★ **새로고침으로 실제 PAUSED 를 만들고** 화면 4요소 + 재개 버튼 확인
  ★★★ 3초 기다려도 자동 재개되지 않는다
  ★★ `Alt+R` 로 재개 → 남은 시간이 이어진다
  ★ 새 문제 시작 시 지문이 화면 안에 들어온다 (R014 결함 회귀 방지)


═══════════════════════════════════════════════════════════════════════
5. 문서 갱신
═══════════════════════════════════════════════════════════════════════

  01-GAME-RULES    ★ 3장 방 종료 두 갈래 / 6장 PAUSED 5분 / 9장 도배 기준 + 부하 실측 /
                   13장 Q-15 의 예외 명시 / 14장 PAUSED 개정 / 16장 ★ 단축키 배정표 + 정수 타이머
                   ★ 0장 "guide 에서 개정된 규칙" 에 6번째(Q-15 부분 번복)를 추가
  02-ARCHITECTURE  ★ PAUSED 가 무한정 기다리지 않는다는 사실
  04-PROTOCOL      ★ T14 개정(결과 화면 → 방 폭파) / ★ T24 신설(마지막 활성자 나가기) /
                   tick 검사 순서 / game.pauseStatus / ★★ 스냅샷의 힌트 판단 /
                   ★ Phase 5 구현과 명세의 대조(달라진 점·추가한 것·실측)
  05-STATUS        ★ Phase 3 브라우저 확인 통과 / ★★ Phase 5 완료 / Phase 7 단축키 완료
  06-WORKLOG       R015 항목
  07-DECISIONS     ★★ **D-064 ~ D-072** (아래)
  08-OPEN-QUESTIONS ★ Q-56 종결 (목록에서 제거하고 해소 항목으로 이동) / Q-82~Q-84 기록
  09-BACKLOG       ★★ 1-3 스크롤 없이 한 화면 (요소 목록 + 우선순위 + 실측) /
                   ★ 모바일 전반 미확인 / 단축키 표기 항목은 완료로 표시
  10-TESTING       ★ 13-7 Phase 3 브라우저 확인 통과(건우 실측) / ★★ 14장 Phase 5 테스트 전체

새 결정 기록
  D-064  ★★ Q-15 의 "구분하지 않는다" 를 방 종료 판단에서만 뒤집는다 (Q-82)
  D-065  ★★ 단축키는 전부 Alt 조합으로 한다 (Q-56)
  D-066  ★ 한 방에 두 개의 만료 타이머가 동시에 돌지 않게 한다
  D-067  ★★ PAUSED 는 endsAt 을 밀지 않고 remainingMs 를 저장한다
  D-068  ★ COUNTDOWN 에서 멈춘 방의 막다른 길을 연다
  D-069  ★ 도배 제한의 기준을 예절에서 서버 보호로 바꾼다 (Q-84)
  D-070  ★ 타이머는 정수 초만 표시한다 (Q-83)
  D-071  ★ 운영 중 조정할 값은 환경 변수로 뺀다
  D-072  ★ PAUSED 관측용 읽기 전용 엔드포인트를 둔다

★★ 건우 지시대로 **D-064 에 "Q-15 를 왜 부분적으로 번복했는지" 를 적었습니다.**
  ★ 뒤집은 범위(방 종료 판단만)와 뒤집지 않은 범위(점수·경험·슬롯)를 함께 적었습니다.


═══════════════════════════════════════════════════════════════════════
6. 커밋과 태그
═══════════════════════════════════════════════════════════════════════

  a4ee966  feat(server): Phase 5 일시정지(PAUSED) 정식 구현 + Q-82 방 종료 두 갈래
  ff3dfb7  feat(client): 일시정지 화면 + Q-56 단축키 + Q-83 정수 타이머
  08458d2  test: Phase 5 실측 — pause.test.ts 22건 / 봇 시나리오 5종 / ui-check 101항목
  11d27ef  docs: R015 — Phase 5 / Q-82·Q-56·Q-83·Q-84 확정 반영 / D-064~D-072

  태그  v0.5-phase5   push 완료 (main + tag)

★ 비밀값 검사 (저장소가 Public 이므로 매번 합니다)
  · `git status` / `git diff --numstat` 에 `.env` · 키 파일 없음 확인
  · 변경분 전체를 `AIza… / sk-… / ghp_… / -----BEGIN / api_key= / password= / trycloudflare / ngrok`
    패턴으로 검사 — **일치 0건**
  · `.gitignore` 에 `.env` / `.env.*` / `secrets.json` 이 있는 것 재확인
  · ★ 봇 테스트 계정 비밀번호(`bot1234`)는 로컬 테스트 픽스처이며 이전 라운드부터 있던 값입니다


═══════════════════════════════════════════════════════════════════════
7. ★★ 건우가 브라우저에서 확인할 절차
═══════════════════════════════════════════════════════════════════════

──────────────────────────────────────────
7-0. ★★ 이 Phase 에 아직 없는 것 — 먼저 읽어 주십시오 (D-030)
──────────────────────────────────────────

★ **결과 화면이 최소 형태입니다.** 문제별 정답자·통계·애니메이션이 없습니다 → Phase 4
★ **정답 마스킹이 없습니다.** 경험자가 채팅에 정답을 쓰면 그대로 보입니다 → Phase 6
★ **"이미 풀어본 퀴즈입니다" 배지가 없습니다** → Phase 6
★★ **스크롤 없이 한 화면이 아닙니다.** 게임 화면이 뷰포트의 1.5~1.7배입니다 → Phase 7 (접수만 했습니다)
★★ **모바일은 확인되지 않았습니다.** 저도 PC(헤드리스 Chrome)에서만 확인했습니다
★ **터널 환경에서의 재접속은 확인하지 못했습니다.** 로컬호스트 기준입니다

──────────────────────────────────────────
7-1. 준비
──────────────────────────────────────────

  1. npm run db:up          (이미 떠 있으면 생략)
  2. npm run build
  3. npm run dev            → http://localhost:5173
  ★ 창 A = 방장 / 창 B = 게스트 (다른 프로필 또는 시크릿 창)
  ★ 문제 수는 3 정도, 즉시 시작으로 하시면 편합니다.

★★ **일시정지는 창이 두 개 필요하고 순서가 까다롭습니다. 7-3 을 순서대로 해 주십시오.**

──────────────────────────────────────────
7-2. 단축키와 표시 (혼자서 됩니다)
──────────────────────────────────────────

   1  채팅 입력창 아래 단축키 줄을 본다
        → ★ 지금 쓸 수 있는 것만 한 줄로 보인다 / `Alt+G` 로 전체가 펼쳐진다
   2  ★★ 한글을 치는 중에 `Alt+S` 를 누른다
        → ★★ 조합 중인 글자가 깨지지 않고 단축키가 동작한다
   3  문자키 `s` `k` `r` 을 채팅에 친다
        → ★★ 그대로 입력된다. 단축키로 가로채지 않는다
   4  버튼을 누른 직후 `Esc` 를 누른다
        → ★ 포커스가 채팅 입력창으로 돌아온다 (바로 정답을 칠 수 있다)
   5  타이머를 본다
        → ★★ 소수점이 없다. 카운트다운도 같다
   6  `Alt+K` (이 문제 넘기기) → 확인창에서 `Enter` 만 누른다
        → ★★ 마우스 없이 확정된다
   7  게임 중 `Alt+X` (방 나가기)
        → ★★ 확인창이 뜬다. "마지막 접속자면 방이 사라진다" 안내 / `취소` 로 돌아올 수 있다

──────────────────────────────────────────
7-3. ★★★ 일시정지 (창 두 개. 순서대로)
──────────────────────────────────────────

   8  게임 중에 ★ **두 창을 모두 닫는다**
   9  ★ 창 A(방장)만 다시 열어 방으로 들어간다
        → ★★★ **일시정지 화면**이 보인다
           · 왜 멈췄는지 · `1 / 2` 복귀 · 멈춘 시점의 남은 시간
           · ★ 방이 사라지기까지 남은 시간 (5분부터 줄어든다)
  10  ★★ 그 상태로 30초 기다린다
        → ★★★ **자동으로 재개되지 않는다.** 멈춘 남은 시간도 줄지 않는다
  11  창 B 도 다시 들어온다
        → ★ 복귀 표시가 `2 / 2` 로 바뀐다. ★★ 그래도 재개되지 않는다
  12  창 B(게스트) 화면을 본다
        → ★ 재개 버튼이 **없다.** "방장이 재개하기를 기다리는 중입니다"
  13  ★★ 창 A 에서 `Alt+R` 또는 "재개" 를 누른다
        → ★★ 멈춘 그 시간부터 이어진다. 창 B 도 함께 움직인다
  14  게임 중 창 A 에서 **새로고침** (혼자일 때)
        → ★ 잠깐 일시정지가 되고, 방장이 재개를 눌러야 이어진다
  15  ★★ 문제가 시작된 직후 두 창을 닫고 **20초 뒤** 다시 들어간다
        → ★★★ **힌트가 보이지 않는다** (멈춘 시점에 아직 20초 넘게 남았기 때문이다)
  16  ★ 재개한 뒤 기다린다
        → ★ 남은 10초에 힌트가 나타난다 (재개 기준으로 다시 계산된다)
  17  ★★★ 게임 중 **마지막 사람이 "방 나가기" 를 누른다**
        → ★★★ **방이 즉시 사라진다.** 다시 들어가면 "없는 방" 안내가 나온다
  18  ★ 카운트다운 중에 두 창을 닫고 방장만 돌아온다
        → ★ 일시정지 상태다. ★★ "카운트다운 취소" 로 로비로 돌아갈 수 있다
  19  ★ 채팅을 아주 빠르게 연타한다
        → ★★ 예전보다 훨씬 넉넉하다 (1초 20개). 한계를 넘으면 본인에게만 안내

★★ **8~13 이 이 Phase 의 본체입니다.** 특히 9·10·13 이 R014 와 달라진 핵심입니다 —
  예전에는 아무 표시 없이 사람이 돌아오면 **자동으로** 진행됐습니다.

★ 이상한 점이 있으면 **화면에 무엇이 보였는지** 그대로 알려주십시오.
  ★ 저는 소켓과 DOM 좌표까지는 볼 수 있지만, "조잡하다 / 헷갈린다" 는 판단은 건우만 할 수 있습니다.


═══════════════════════════════════════════════════════════════════════
8. 자체 판단 / 남은 문제 / 다음 라운드
═══════════════════════════════════════════════════════════════════════

──────────────────────────────────────────
8-1. 자체 판단한 것 (기술적이고 되돌리기 쉬운 것만)
──────────────────────────────────────────

  1. ★ `remainingMs` 저장 방식 (D-067) — 근거 넷을 07-DECISIONS 에 적었습니다
  2. ★ 타이머 배타성 (D-066) — 규칙 충돌을 상태로 나눴습니다
  3. ★ 방 폭파 단일 경로 `destroyRoom` — 진입로가 넷이라 흩어지면 위험합니다
  4. ★ COUNTDOWN 막다른 길 개방 (D-068) — 사용자가 원하는 것이 명확하고 수단이 없었습니다
  5. ★ 설정값 3개를 환경 변수로 (D-071) — 건우 지시("설정값으로 두어라")의 구현 형태입니다
  6. ★ `/debug/room/:id` (D-072) — 개발 전용·읽기 전용. 정답 문자열은 넣지 않았습니다
  7. ★ PAUSED 중에도 방장 이전을 돌린다 — 돌리지 않으면 게임이 되살아날 수 없습니다
  8. ★ ui-check 의 버튼 라벨에서 `<kbd>` 제외 — 사람이 읽는 이름은 배지와 무관합니다
  9. ★ 320px 검사 전에 스크롤을 맨 위로 — 레이아웃 결함이 아니라 스크롤 위치 문제였습니다
     ★★ 진짜 문제(한 화면에 안 들어온다)는 09-BACKLOG 로 올렸습니다. 여기서 고치지 않았습니다

──────────────────────────────────────────
8-2. 남은 문제
──────────────────────────────────────────

  ★★ 브라우저 수동 확인 대기 (7장)
  ★★ 모바일 미확인 — PC 에서만 확인했습니다. 09-BACKLOG 1-2 에 항목을 만들었습니다
  ★ 터널 환경 재접속 미확인 — 실제 URL 이 바뀌는 상황은 로컬에서 재현할 수 없습니다
  ★★ 스크롤 없이 한 화면이 아닙니다 (1.47~1.66배 실측) — Phase 7
  ★ 결과 화면은 최소 형태(TEMP-P4-01) — Phase 4
  ★ Q-81(Gemini → Sonnet 역할 재배치)의 실제 세션 운용은 아직입니다

──────────────────────────────────────────
8-3. 다음 라운드 제안
──────────────────────────────────────────

★ 건우가 이미 정한 방향(Phase 4 + Q-81 별도 세션 병행 + UTC 08:00 이후 Gemini 호출 시도)에
  ★ 제가 덧붙일 제안만 적습니다. **순서는 건우가 정해 주십시오.**

  (1) ★★ Phase 4 에서 결과 화면을 다시 만들 때, 09-BACKLOG 1-3 의 요소 표에
      결과 화면 항목을 함께 추가하겠습니다. ★ 지금 그 표에 결과 화면이 없습니다.
  (2) ★ 7장 확인에서 "일시정지 화면이 조잡하다" 는 판단이 나오면, 그것은 Phase 7 이 아니라
      **그 라운드에서 고쳐야 하는 것**일 수 있습니다 (D-031 분류 기준).
      ★ 멈춘 이유·복귀 현황·폭파 시각은 **정보 도달**의 문제라서 그렇습니다.
  (3) ★ 터널을 한 번 열어 **실기기(휴대폰)로 접속**해 보시는 것을 권합니다.
      ★ 모바일 미확인 항목이 쌓이고 있고, 특히 iOS 소프트 키보드는 실기기가 없으면 확인할 수 없습니다.

===========================================
